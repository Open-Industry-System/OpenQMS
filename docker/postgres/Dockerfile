FROM pgvector/pgvector:pg15
# zhparser/scws Chinese full-text search extension is optional;
# the repo has been removed from GitHub so we skip it for local dev.
# If you need Chinese full-text search, install zhparser manually.