# US-E2E-01.1 D3 遏制全链路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 CAPA 8D 的 D3 阶段实现「数据导入 → 受影响范围分析报告 → AI 遏制建议 → 采纳/执行记录 → D3→D4 闸口」全链路（AI_REQUIRED=true，无 LLM 凭证 BLOCKED）。

**Architecture:** 7 张不可变 generation 表（import_run→report→advice_generation→advice + adoption + execution），三阶段 LLM 短事务（锁+建 running artifact+提交 → 无锁 LLM → 重锁+attempt_token CAS 晋升/失败），事实确定性计算 + LLM 只做风险分级与建议文本，D3→D4 闸口 fail-closed。详见 `docs/superpowers/specs/2026-07-10-us-e2e-01.1-d3-containment-design.md`（v15 定稿）——**本计划是任务分解，所有字段/CHECK/CAS/枚举契约以 spec 为唯一真源，实现时逐条对照 spec §3/§4/§5/§6**。

**Tech Stack:** Python 3.11 + FastAPI 0.115 (async) + SQLAlchemy 2.0 (async) + PostgreSQL 15 + Pydantic v2 + React 18 + TypeScript 5.6 + Playwright E2E

## Global Constraints

- `factory_id` NOT NULL 全表（7 表）+ 复合 FK 保证父子 factory 一致（spec §3 表头 + §14）
- 统一 ON DELETE RESTRICT + CAPA 软删除（归档不物理删）；存在 D3 审计实体禁止物理删 CAPA/run/report/generation（spec §3 表头）
- 不可变 generation 覆盖 run→report→advice_generation→advice；每层 `is_current` partial unique index + `status` running/done/failed 三态 + 综合 CHECK（spec §3.1/§3.3/§3.5）
- attempt_token CAS：所有 terminal transition（成功 + 所有失败分支）用 `WHERE id AND status='running' AND attempt_token=:token`，行数 0 丢弃（spec §4.2/§4.3 + §14）
- 原子晋升分步顺序：run 5 步、report 4 步、advice generation 5 步；CAS done/is_current 保持 false→旧 false→新 true+completed_at→回滚（spec §3.1/§3.3/§3.5 + §14）
- done 完整性 CHECK：report done 必含 5 项确定性结果 + risk_floor + risk_explanation 非空 + llm_available=true + completed_at；advice done 必 advice_count>0 + llm_available=true（spec §3.3/§3.5 + §14）
- 失败分支审计全覆盖：error code 枚举 `superseded`/`llm_failed`/`schema_failed`/`all_provenance_mapping_failed`/`unknown_risk_mapping_version`/`stale`，写 stage_runs（spec §3.5 + §4.2/§4.3）
- 快照固化分析数据：shipment payload 固化 unit/customer_segment/customer_name/arrival_status；run.analysis_context 固化 capa_severity + risk_mapping_version（唯一真源）；报告只读 payload + run.analysis_context 不查源表（spec §3.1/§3.2 + §4.2）
- RISK_MAPPINGS 版本注册表：服务层常量，旧版本不可删，未知版本→failed 不静默（spec §3.3 + §14）
- record_key 永不为空只有 snapshot_id 可空；batch_key=hash(normalized_material+normalized_lot)，lot 缺失退化 `{snapshot_type}:{source_id}`（spec §3.2/§3.4）
- IQC 查 `iqc_inspections` JOIN suppliers（非 iqc_materials 主数据）（spec §4.1）
- AI_REQUIRED=true：无凭证 BLOCKED（422 blocked 独立端点；import 端点 report_status=blocked）；建议生成先检查凭证再建 generation（spec §4.2/§4.3）
- 重复请求返 202 Accepted + 现有 running artifact ID + status + Retry-After header（秒）（spec §3.3/§3.5 + §7）
- 状态机：D3_INTERIM→D4_ROOT_CAUSE 线性边已在 `eightd_state.py`，无需改状态机；D3 阶段守卫 `capa.status == D3_INTERIM`（spec §5 + §14）
- 发布策略：A+B 原子发布（Slice A/B 仅实现顺序，不分阶段上线）（spec §2 + §14）
- 复用：`provider_adapter.complete_json` + `provider_adapter.build_client`/`ProviderNotConfiguredError`（01.2/01.3 已迁移）、`capa_draft_service` 的 MAX_PROMPT_CHARS=8000 截断 + safety_trailer + DB-backed timeout 口径、`backend/tests/migrations/` 迁移测试基础设施（01.2/A3 已建 `mig_db_url` fixture + `_cfg` helper）、`capa_verification_service` 的 AuditLog 写法

**Spec 契约索引**（实现时逐条对照）：
- 数据模型 7 表：spec §3.1–§3.7（每列类型/约束 + partial UQ + 综合 CHECK + 复合 FK）
- 服务层：spec §4.1（导入 5 步 + 子源查询）、§4.2（报告三阶段 + 确定性 4 项 + risk floor）、§4.3（建议三阶段 + accepted_advice + provenance 映射）、§4.4（采纳/拒绝 + 文本修订）、§4.5（执行记录 + PATCH）
- 闸口：spec §5（fail-closed，先锁 current run，4 类齐全 + 报告 done + execution 有效）
- LLM 契约：spec §6.1（报告输入/输出）、§6.2（建议输出）、§6.3（客户名脱敏/还原）
- API 端点 9 个：spec §7
- 审计事件 8 个：spec §8
- 权限：spec §9
- 前端：spec §10
- E2E：spec §11
- 测试矩阵：spec §12

---

## File Structure

**Backend — 新建：**
- `backend/app/models/capa_d3.py` — 7 张 D3 表的 SQLAlchemy 2.0 ORM（一个文件，按表分 class）
- `backend/app/services/capa_d3_containment_service.py` — 导入 + 报告 + 建议 + 采纳 + 执行服务（按 spec §4 分函数，私有 `_query_*`/`_compute_*`/`_cas_*` helper）
- `backend/app/services/capa_d3_risk_mappings.py` — `RISK_MAPPINGS` 版本注册表常量（spec §3.3）+ 当前版本 `CURRENT_RISK_MAPPING_VERSION = "v1"`
- `backend/app/schemas/capa_d3.py` — Pydantic v2 请求/响应 schema（import/report/advice/decision/execution）
- `backend/alembic/versions/<rev>_d3_containment_tables.py` — 7 表 + partial UQ + CHECK + 复合 FK 迁移（手写）

**Backend — 修改：**
- `backend/app/models/__init__.py` — 注册 D3 模型
- `backend/app/api/capa.py` — 加 9 个 D3 端点 + D3 阶段守卫
- `backend/app/services/capa_service.py` — `advance_capa` 新增 D3→D4 分支调用 `_d3_to_d4_gate`（gate 实现在 d3 service，capa_service 只调）

**Backend 测试 — 新建：**
- `backend/tests/migrations/test_d3_containment_migration.py` — 7 表 + 约束迁移测试（复用 `mig_db_url`/`_cfg`）
- `backend/tests/capa/test_capa_d3_import.py`
- `backend/tests/capa/test_capa_d3_report.py`
- `backend/tests/capa/test_capa_d3_advice.py`
- `backend/tests/capa/test_capa_d3_adoption.py`
- `backend/tests/capa/test_capa_d3_execution.py`
- `backend/tests/capa/test_capa_d3_gate.py`
- `backend/tests/capa/test_capa_d3_api.py` — 9 端点 + 202/422/200 契约

**Frontend — 新建/修改：**
- `frontend/src/components/capa/D3ContainmentPanel.tsx` — D3 面板
- `frontend/src/api/capa.ts` — 加 D3 端点函数
- `frontend/src/types/index.ts` — 加 D3 类型
- `frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts` — E2E
- `frontend/e2e/seed_e2e` 扩展 — mock ERPConnection + ERPInventoryBalance + ERPShipment(arrival_status) + iqc_inspections + Customer.segment

**文档：**
- `docs/user-stories/US-E2E-01-capa-8d-closed-loop/US-E2E-01.1-d3-containment-ai.md` — 标"实现态"
- `PROGRESS.md` / `docs/ROADMAP.md` / `docs/permissions.md` — 同步（CI docs-check）

---

## Slice A — 非 AI 骨架（可独立落地测试，不依赖 LLM）

### Task 1: 7 张 D3 表 ORM 模型 + 迁移 + 迁移测试

**Files:**
- Create: `backend/app/models/capa_d3.py`
- Create: `backend/alembic/versions/<rev>_d3_containment_tables.py`
- Create: `backend/tests/migrations/test_d3_containment_migration.py`
- Modify: `backend/app/models/__init__.py`

**Interfaces:**
- Produces: `CapaD3ImportRun`/`CapaD3ContainmentSnapshot`/`CapaD3ImpactReport`/`CapaD3AdviceGeneration`/`CapaD3AiAdvice`/`CapaD3AdviceAdoption`/`CapaD3Execution` 模型类（后续任务用）

- [ ] **Step 1: 写 ORM 模型**

`backend/app/models/capa_d3.py`：按 spec §3.1–§3.7 定义 7 个 class。逐列对照 spec 表头——类型/约束/default/partial UQ/CHECK/复合 FK 全部照搬。关键点（spec 为准）：
- `CapaD3ImportRun`：`is_current` default false；`analysis_context` JSONB NOT NULL；`status` CHECK IN ('importing','completed','failed')；partial UQ `UNIQUE(capa_id) WHERE is_current=true`；CHECK `NOT (is_current=true AND (status!='completed' OR completed_at IS NULL))`；`UNIQUE(run_id, factory_id)`
- `CapaD3ContainmentSnapshot`：`UQ(run_id, snapshot_type)`；复合 FK `(run_id, factory_id)→import_run`
- `CapaD3ImpactReport`：`is_current` default false；`status` default 'running'；`attempt_token` UUID NOT NULL；`risk_floor` String(8) nullable；`started_at`/`completed_at`；综合 CHECK（spec §3.3 行 183-188）+ done 完整性 CHECK（行 191-198 含 risk_explanation+llm_available）；partial UQ `UNIQUE(run_id) WHERE is_current=true` + `UNIQUE(run_id) WHERE status='running'`；`UNIQUE(report_id, factory_id)`；复合 FK `(run_id, factory_id)→import_run`
- `CapaD3AdviceGeneration`：`is_current` default false；`status` default 'running'；`attempt_token`；`stage_runs` JSONB NOT NULL default `[]`；`rejected_advice_count`；综合 CHECK + `done→advice_count>0` + `done→llm_available=true`；partial UQ `UNIQUE(report_id) WHERE is_current=true` + `UNIQUE(report_id) WHERE status='running'`；`UNIQUE(generation_id, report_id, factory_id)`；复合 FK `(report_id, factory_id)→impact_report`
- `CapaD3AiAdvice`：`generation_id` FK RESTRICT NOT NULL（不存 report_id）；`source_provenance` JSONB NOT NULL；复合 FK `(generation_id, factory_id)→advice_generation`；`UNIQUE(advice_id, generation_id, factory_id)`
- `CapaD3AdviceAdoption`：`advice_id` FK RESTRICT NOT NULL；`UQ(advice_id)`；CHECK `(adopted AND adopted_text NOT NULL) OR (rejected AND adopted_text NULL)`；复合 FK `(advice_id, factory_id)→advice`
- `CapaD3Execution`：`report_id` FK RESTRICT NOT NULL；`generation_id`/`advice_id` FK RESTRICT nullable；CHECK manual/adopted 分支；复合 FK 三组（spec §3.7 行 316-319）
- 所有表 `factory_id` NOT NULL FK RESTRICT。`__table_args__` 含全部 Index/UniqueConstraint/CheckConstraint。

- [ ] **Step 2: 注册模型**

`backend/app/models/__init__.py`：import 7 个 class（仿现有 capa 模型注册）。

- [ ] **Step 3: 写迁移**

`backend/alembic/versions/<rev>_d3_containment_tables.py`：`upgrade()` 用 `op.create_table` 建 7 表 + 所有 Index（partial UQ 用 `postgresql_where=sa.text(...)`）+ CheckConstraint + 复合 ForeignKeyConstraint；`downgrade()` 逆序 drop。`capa_eightd` 加 `UNIQUE(report_id, factory_id)`（若不存在）。`factories` 已有 `UNIQUE(id, factory_id)` 否则补。

- [ ] **Step 4: 写迁移测试（失败）**

`backend/tests/migrations/test_d3_containment_migration.py`：复用 `mig_db_url`/`_cfg`（01.2/A3 模式）。测试：upgrade 后 7 表存在；partial UQ 拒双 current；综合 CHECK 拒非法组合（如 report is_current=true + status=running）；done 完整性 CHECK 拒 done 但 risk_explanation 空 / llm_available=false；execution CHECK 拒 manual+advice_id 非空 / adopted+advice_id 空；复合 FK 拒跨 factory；adoption UQ(advice_id) 拒双决策。

- [ ] **Step 5: 跑迁移测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/migrations/test_d3_containment_migration.py -v`
Expected: FAIL（模型/迁移未写完则报错；写完则 PASS）

- [ ] **Step 6: 跑 `make check` 确认无回归**

Run: `make check`（backend tests + frontend tsc + build）
Expected: PASS（新表不影响既有测试）

- [ ] **Step 7: Commit**

```bash
git add backend/app/models/capa_d3.py backend/app/models/__init__.py backend/alembic/versions/*_d3_containment_tables.py backend/tests/migrations/test_d3_containment_migration.py
git commit -m "feat(d3): 7 张 D3 containment 表 ORM + 迁移 + 迁移测试（不可变 generation/partial UQ/综合 CHECK/复合 FK）"
```

---

### Task 2: RISK_MAPPINGS 注册表 + 导入服务（子源查询 + 5 步 run 晋升）

**Files:**
- Create: `backend/app/services/capa_d3_risk_mappings.py`
- Create: `backend/app/services/capa_d3_containment_service.py`（先写导入部分）
- Create: `backend/tests/capa/test_capa_d3_import.py`

**Interfaces:**
- Produces: `RISK_MAPPINGS`（dict）、`CURRENT_RISK_MAPPING_VERSION`（str="v1"）、`import_containment_data(db, capa_id, user, request) -> dict`

- [ ] **Step 1: 写 RISK_MAPPINGS**

`backend/app/services/capa_d3_risk_mappings.py`：
```python
RISK_MAPPINGS = {
    "v1": {"critical": "high", "fatal": "high", "serious": "medium", "general": "low"},
}
CURRENT_RISK_MAPPING_VERSION = "v1"
```

- [ ] **Step 2: 写导入服务 + 子源查询**

`backend/app/services/capa_d3_containment_service.py`：实现 `import_containment_data`（spec §4.1 事务 A 5 步：锁 capa FOR UPDATE → 建新 run `is_current=false, status='importing', analysis_context={capa_severity: capa.severity, risk_mapping_version: CURRENT_RISK_MAPPING_VERSION}` → 调子源查询落 snapshot → run status='completed'+completed_at → 旧 current run is_current=false → 新 run is_current=true → 写 D3_DATA_IMPORTED 审计 → 提交，任一步失败回滚）。
私有 `_query_inventory`/`_query_shipment`/`_query_iqc`/`_query_spc`：product_line_code + factory_id 隔离；每条 payload 带 `record_key`+`source_id`。shipment unit 从 `erp_raw_data["unit"]`→物料主数据→缺失 'unknown'；arrival_status 从 `erp_raw_data["arrival_status"]` 读，枚举校验，缺失/非法→'unknown'（**不靠日期/地址推导**）；customer_segment/customer_name 从 customers join 固化。iqc 查 `IqcInspection` JOIN suppliers（非 iqc_materials）。spc 复用 SPCAnomalySource 30 天口径。
**事务 B（报告）暂留 TODO 占位**——本任务只 import，报告生成在 Task 4。

- [ ] **Step 3: 写导入测试（失败）**

`backend/tests/capa/test_capa_d3_import.py`：4 子源（inventory/shipment/iqc/spc）查询 + payload 固化（unit/customer_segment/customer_name/arrival_status）+ IQC 查 iqc_inspections + 空数据 record_count=0 落行 + run UQ + is_current partial UQ + 双击锁（两并发 import 产生两代 run，旧 is_current=false）+ **import_run 5 步晋升不撞 partial UQ**（已有旧 current 时新建成功）+ analysis_context 固化 capa_severity+risk_mapping_version + 两阶段事务 A 不回滚 + D3_DATA_IMPORTED 审计 + arrival_status 从 erp_raw_data 读 + 缺失→unknown。

- [ ] **Step 4: 跑测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_import.py -v`
Expected: 先 FAIL 后 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/capa_d3_risk_mappings.py backend/app/services/capa_d3_containment_service.py backend/tests/capa/test_capa_d3_import.py
git commit -m "feat(d3): RISK_MAPPINGS 注册表 + 导入服务（4 子源查询+5 步 run 晋升+analysis_context 固化）"
```

---

### Task 3: D3→D4 闸口（Slice A 基础版：4 类齐全 + 报告 done + execution 有效）

**Files:**
- Modify: `backend/app/services/capa_service.py`（`advance_capa` 加 D3→D4 分支）
- Create: `backend/tests/capa/test_capa_d3_gate.py`

**Interfaces:**
- Consumes: `_d3_to_d4_gate(db, capa)`（本任务在 d3 service 实现，capa_service 调用）
- Produces: D3→D4 推进时 fail-closed 校验

- [ ] **Step 1: 写闸口测试（失败）**

`backend/tests/capa/test_capa_d3_gate.py`：场景——未导入→拒；只导 3 类→拒（缺类）；有快照无报告 done→拒；有报告无 execution→拒；纯人工 execution + 报告 done→推进成功；从未生成 advice 纯 manual execution→推进成功（v6 P0）；旧 generation adopted execution 不满足闸口；先执行旧建议→重生成 advice→闸口失败；manual execution 不受建议重生成影响；execution 降级 completed→failed→闸口不再满足；报告 failed→拒；superseded 报告→拒。factory 一致校验。

- [ ] **Step 2: 实现闸口**

`capa_d3_containment_service.py` 加 `_d3_to_d4_gate(db, capa)`（spec §5 伪代码照搬）：先取 current run（partial UQ）+ factory 校验 → 4 类 snapshot_type 齐全 → current report status='done'（先锁 current run 再在其下取 is_current report）→ current advice_generation（可 null）→ 有效 execution 计数（`source='manual' OR (source='adopted' AND generation_id=current_gen)`，result_status IN completed/in_progress，failed 不计）→ 0 则 raise。
`capa_service.advance_capa` 加 `elif current == EightDState.D3_INTERIM and target == EightDState.D4_ROOT_CAUSE: await _d3_to_d4_gate(db, capa)`。

- [ ] **Step 3: 跑测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_gate.py -v`
Expected: PASS

- [ ] **Step 4: 跑 capa 既有测试无回归**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/ -v`
Expected: PASS（闸口只加分支，既有 D4/D5/D7 测试不受影响）

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/capa_service.py backend/app/services/capa_d3_containment_service.py backend/tests/capa/test_capa_d3_gate.py
git commit -m "feat(d3): D3→D4 闸口（fail-closed：4 类齐全+报告 done+execution 有效+generation 校验+factory 一致）"
```

---

### Task 4: 导入 + 报告 API 端点（import 始终 200；runs/snapshots GET）

**Files:**
- Create: `backend/app/schemas/capa_d3.py`（import/runs/snapshots 部分）
- Modify: `backend/app/api/capa.py`（加 import + runs + snapshots 端点）
- Create: `backend/tests/capa/test_capa_d3_api.py`（import 部分）

**Interfaces:**
- Produces: `POST /api/capa/{report_id}/d3/import`（始终 200 + report_status）、`GET /d3/runs`、`GET /d3/snapshots`

- [ ] **Step 1: 写 schema**

`backend/app/schemas/capa_d3.py`：`D3ImportRequest`（snapshot_types 默认全 4 类，params optional）、`D3SnapshotSummary`、`D3ImportResponse`（run_id, snapshots[], report_status: done/failed/blocked/superseded, report?, report_error?）、`D3RunResponse`、`D3SnapshotResponse`。

- [ ] **Step 2: 写 API 测试（失败）**

`test_capa_d3_api.py` import 部分：import 始终 200 + report_status；跨阶段守卫（非 D3_INTERIM → 403/400）；权限（viewer → 403）；factory 校验（跨工厂 capa → 404）；runs/snapshots GET 回读。

- [ ] **Step 3: 实现端点**

`api/capa.py`：`POST /d3/import` 调 `import_containment_data`（事务 A 提交后，事务 B 报告生成在本任务先接通——见 Task 6 完整报告，本任务 report_status 暂返 'pending' 或接通基础版）；`GET /d3/runs`、`GET /d3/snapshots`。D3 阶段守卫 helper（仿 `_assert_d7_stage`）。

- [ ] **Step 4: 跑测试 + Commit**

```bash
cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_api.py -v
git add backend/app/schemas/capa_d3.py backend/app/api/capa.py backend/tests/capa/test_capa_d3_api.py
git commit -m "feat(d3): 导入+runs+snapshots API 端点（始终 200+report_status+阶段守卫+factory 校验）"
```

---

> **Slice A 完成节点**：至此 7 表 + 导入 + 闸口 + 基础 API 可独立落地测试，不依赖 LLM。`make check` 应绿。

---

## Slice B — AI 链路（LLM 报告 + 建议 + 采纳 + 执行 + 前端 + E2E）

### Task 5: 确定性计算（4 项事实 + risk floor + batch_key + 单位数组）

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `_compute_*` helper）
- Create: `backend/tests/capa/test_capa_d3_report.py`（确定性计算部分，先不接 LLM）

**Interfaces:**
- Produces: `_compute_batches`/`_compute_impact_qty`/`_compute_customer_impact`/`_compute_time_window`/`_compute_risk_floor`（纯函数，输入 snapshot payload + analysis_context，输出确定性事实 dict）

- [ ] **Step 1: 写确定性计算测试（失败）**

`test_capa_d3_report.py` 计算部分：batch_key=hash(normalized_material+normalized_lot) 跨来源合并；lot 缺失退化 `{snapshot_type}:{source_id}`；qty_by_status 数组只 inventory/shipment 贡献数量（IQC/SPC 仅 source_refs 证据，防 lot_qty+库存+发货=200）；按 source_id 去重；shipment unit 从 payload 读；单位不混加（同状态多单位分组）；impact_qty 数组汇总；customer_impact quantities 数组 + customer_segment 从 payload 读（不查源表）；time_window alarm 最早~最晚；risk_floor unknown+受影响客户→high，否则按 RISK_MAPPINGS[version][severity]；未知 risk_mapping_version→返回 failed 信号。

- [ ] **Step 2: 实现 `_compute_*`**

`capa_d3_containment_service.py`：5 个纯函数（spec §4.2 行 370-375）。`_compute_risk_floor` 返回 `(floor_str|None, error_code)`——未知版本返回 `(None, 'unknown_risk_mapping_version')`。

- [ ] **Step 3: 跑测试 + Commit**

```bash
cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_report.py -v
git add backend/app/services/capa_d3_containment_service.py backend/tests/capa/test_capa_d3_report.py
git commit -m "feat(d3): 确定性计算 4 项事实 + risk floor（batch_key=hash+单位数组+IQC/SPC 仅证据+RISK_MAPPINGS 查表）"
```

---

### Task 6: 报告生成服务（三阶段短事务 + attempt_token CAS + risk floor + 脱敏还原 + prompt 截断）

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `generate_impact_report` + `_cas_report_*`）
- Modify: `backend/tests/capa/test_capa_d3_report.py`（加 LLM 三阶段测试）

**Interfaces:**
- Consumes: `provider_adapter.build_client`/`complete_json`/`ProviderNotConfiguredError`、`capa_draft_service` 的 MAX_PROMPT_CHARS + safety_trailer 口径
- Produces: `generate_impact_report(db, run_id, user) -> dict`

- [ ] **Step 1: 写报告三阶段测试（失败）**

`test_capa_d3_report.py` 加：短事务三阶段 LLM 不持锁（阶段①建 running+提交，阶段②无锁 LLM，阶段③重锁 CAS）；is_current default false + 综合 CHECK；attempt_token CAS 防 stale 复活（A running→B 标 A failed→A LLM 晚到→CAS 行数 0 丢弃不晋升）；重复请求 202 Accepted+Retry-After；LLM 成功/失败/BLOCKED/superseded 各分支 + 成功 CAS 写 llm_available=true+model+stage_runs+prompt_stats（满足 done CHECK）；running partial UQ 防双 current；stale 超时恢复 CAS（running→failed+stage_runs+error='stale'）；精简 prompt 不含全量快照 + MAX_PROMPT_CHARS 截断 + prompt_stats；脱敏+还原（risk_explanation 代号→真名）；risk floor max(llm,floor) + unknown→high；重检 is_current（superseded）；未知 risk_mapping_version→failed；快照后改 capa.severity→risk floor 用 import 时固化值（v9 F2）；快照后改源表→报告用快照旧值（v8 F3）；已有旧 current 时成功晋升不撞 partial UQ；stale+superseded 转 failed 验 completed_at 非空 + error code 可回读。

- [ ] **Step 2: 实现 `generate_impact_report`**

`capa_d3_containment_service.py`（spec §4.2 行 361-382）：三阶段函数。阶段①锁 capa+重检 run.is_current+建 report running 行（attempt_token=uuid4）+提交。阶段②无锁：读 snapshot payload + run.analysis_context → `_compute_*` → 精简 prompt（脱敏 customer_0X + 确定性事实 + 统计摘要 + analysis_context + 截断标记）+ safety_trailer → `complete_json`。阶段③重锁+重检：按 spec §4.2 行 365-369 五分支（成功/superseded/llm_failed/schema_failed/unknown_risk_mapping_version）CAS UPDATE，全部含 `WHERE report_id AND status='running' AND attempt_token`，行数 0 丢弃；成功 CAS 含 `llm_available=true, model, stage_runs, prompt_stats, batches, impact_qty, customer_impact, time_window, risk_level, risk_floor, risk_explanation`（还原真名）；成功后 4 步晋升（CAS done is_current=false→旧 false→新 true→回滚）。stale 恢复 CAS（error='stale'）。无凭证→BLOCKED 返 blocked 给 import 端点（不建 running report）。

- [ ] **Step 3: 接通 import 事务 B**

`import_containment_data` 事务 B 调 `generate_impact_report`，按返回填 report_status（done/failed/blocked/superseded）；事务 B 异常不回滚事务 A。

- [ ] **Step 4: 跑测试 + Commit**

```bash
cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_report.py tests/capa/test_capa_d3_import.py -v
git commit -m "feat(d3): 报告生成三阶段短事务（attempt_token CAS+stale 恢复+risk floor+脱敏还原+prompt 截断+done 完整性 CHECK）"
```

---

### Task 7: 报告 API 端点（POST /d3/report 422/202/200 + GET /d3/report）

**Files:**
- Modify: `backend/app/schemas/capa_d3.py`（report schema）
- Modify: `backend/app/api/capa.py`（report 端点）
- Modify: `backend/tests/capa/test_capa_d3_api.py`（report 部分）

- [ ] **Step 1: 写 API 测试（失败）**

`test_capa_d3_api.py` report 部分：无凭证→422 `detail.blocked`；已有 running→**202** body `{report_id, status:running}` + `Retry-After` header（正整数秒，断言 body+header）；完成→200 status=done/failed/superseded；GET 当前代报告。

- [ ] **Step 2: 实现端点**

`api/capa.py` `POST /d3/report`：调 `generate_impact_report`，按返回状态码（422 blocked / 202 running+Retry-After / 200 done/failed/superseded）。`GET /d3/report` 取当前代。

- [ ] **Step 3: 跑测试 + Commit**

```bash
cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_api.py -v
git commit -m "feat(d3): 报告 API（422 blocked + 202 running+Retry-After + 200 done/failed/superseded）"
```

---

### Task 8: 建议生成服务（advice_generation 三阶段 + accepted_advice + provenance 映射 + 空建议处理）

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `generate_advice` + provenance 映射）
- Create: `backend/tests/capa/test_capa_d3_advice.py`

- [ ] **Step 1: 写建议测试（失败）**

`test_capa_d3_advice.py`：短事务三阶段 + attempt_token CAS；generation 先 running 非 current 成功才原子切 current；running partial UQ；stale 恢复 CAS（error='stale'）；重复请求 202 Accepted；失败旧代保持 current+落 failed 行+200；综合 CHECK + done→advice_count>0 + done→llm_available=true；先检查凭证再建 generation（无凭证→422 不建 generation）；provenance 按 advice_type 映射用 record_key（recall/notify_customer→shipment，isolate→inventory，strict_inspection→iqc，alternative→report-level `record_key='report:{id}:summary'`）；record_key 永不为空只有 snapshot_id 可空；拒绝空 provenance；target_batch_refs 校验存在于 report.batches；advice_text 还原真名；**accepted_advice 为空→failed 不切 current**（全过滤）；**部分建议被过滤 accepted_advice 仍非空推进**；被过滤建议 stage_runs error 落 generation 表可回读（断言 DB 非只返回）；成功 CAS 写 llm_available=true+model+advice_count+rejected_advice_count+stage_runs；所有失败分支（superseded/llm_failed/schema_failed/all_provenance_mapping_failed）写 stage_runs+error code 可回读；所有 terminal transition attempt_token CAS 行数 0 丢弃。

- [ ] **Step 2: 实现 `generate_advice`**

`capa_d3_containment_service.py`（spec §4.3 行 389-408）：先检查凭证（无凭证→422 不建 generation）；阶段①锁 capa+读当前 report+建 generation running（attempt_token）+提交；阶段②无锁 LLM + 内存 schema/批次/provenance 映射得 accepted_advice + rejected 列表（映射规则按 spec §4.3 行 400-404）；阶段③五分支 CAS（成功/空/superseded/llm_failed/schema_failed），全部 `WHERE generation_id AND status='running' AND attempt_token` 行数 0 丢弃；成功 5 步晋升（CAS done is_current=false+advice_count+rejected_advice_count+stage_runs→逐条 INSERT accepted_advice→旧 false→新 true→回滚）。stale 恢复 CAS。写 D3_AI_ADVICE_GENERATED 审计。

- [ ] **Step 3: 跑测试 + Commit**

```bash
cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_advice.py -v
git commit -m "feat(d3): 建议生成三阶段（accepted_advice 驱动+provenance 按 advice_type 映射+空建议 failed 不切 current+过滤原因落 stage_runs）"
```

---

### Task 9: 建议列表 + 采纳/拒绝服务 + API

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `adopt_advice` + advice 列表查询）
- Create: `backend/tests/capa/test_capa_d3_adoption.py`
- Modify: `backend/app/schemas/capa_d3.py` + `backend/app/api/capa.py`（advice GET + decision POST + adoptions GET）

- [ ] **Step 1: 写采纳测试（失败）**

`test_capa_d3_adoption.py`：UQ(advice_id) 单一决策；INSERT（只 advice_id+冗余 advice_type/source_provenance，不写 report_id/generation_id）；UPDATE 改 decision + D3_ADVICE_DECISION_CHANGED 审计（old/new decision）；UPDATE 改 adopted_text + 审计 old/new 文本；CHECK（adopted→adopted_text 非空，rejected→空）；advice_id RESTRICT 不可空；跨 generation advice 拒绝（只当前 generation 可采纳）；幂等（同 decision+text 返回）；首次写 D3_ADVICE_ADOPTED/D3_ADVICE_REJECTED。

- [ ] **Step 2: 实现 `adopt_advice` + advice 列表**

`capa_d3_containment_service.py`（spec §4.4）：校验 advice 属当前 generation；查 adoption where advice_id → INSERT/幂等/UPDATE+审计。advice 列表查询当前 generation 的 advice + adoption 状态。

- [ ] **Step 3: 写 API + 测试 + Commit**

`test_capa_d3_api.py` advice/decision/adoptions 部分：GET 当前 generation 建议；POST decision 同值幂等/变化 UPDATE；GET adoptions 回读。
```bash
cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_adoption.py tests/capa/test_capa_d3_api.py -v
git commit -m "feat(d3): 建议列表+采纳/拒绝（UQ 单一决策+文本修订审计+generation 校验）"
```

---

### Task 10: 执行记录服务（record + update + evidence_refs scheme 校验）+ API

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `record_execution`/`update_execution`）
- Create: `backend/tests/capa/test_capa_d3_execution.py`
- Modify: `backend/app/schemas/capa_d3.py` + `backend/app/api/capa.py`（execution POST/PATCH/GET）

- [ ] **Step 1: 写执行测试（失败）**

`test_capa_d3_execution.py`：report_id NOT NULL 归属；generation_id nullable；CHECK manual/adopted（manual→advice_id/generation_id null；adopted→都非空）；advice_id 与 generation_id 同代复合 FK；**从未生成 AI 建议→manual execution 可建**（v6 P0）；PATCH 状态降级 completed→failed；PATCH 全字段审计（result_status+measure_text/evidence_refs old/new 或哈希）；evidence_refs scheme 白名单（https/相对 通过，javascript:/file:/data:→422）；最多 10 条；name≤200/url≤500；闸口读最新版本；executions GET 回读。

- [ ] **Step 2: 实现 `record_execution`/`update_execution`**

`capa_d3_containment_service.py`（spec §4.5）：manual→advice_id/generation_id null；adopted→advice_id 必填+generation_id=advice.generation_id+有 adopted 决策+属当前 generation。PATCH 全字段 + evidence_refs scheme 校验。写 D3_EXECUTION_RECORDED/D3_EXECUTION_UPDATED 审计。

- [ ] **Step 3: 写 API + 测试 + Commit**

```bash
cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_execution.py tests/capa/test_capa_d3_api.py -v
git commit -m "feat(d3): 执行记录（report_id 归属+manual/adopted CHECK+PATCH 全字段审计+evidence scheme 白名单）"
```

---

### Task 11: 前端 D3ContainmentPanel + types + api

**Files:**
- Create: `frontend/src/components/capa/D3ContainmentPanel.tsx`
- Modify: `frontend/src/types/index.ts`（D3 类型）
- Modify: `frontend/src/api/capa.ts`（D3 端点函数）
- Create: `frontend/src/components/capa/__tests__/D3ContainmentPanel.test.tsx`

- [ ] **Step 1: 写类型 + api 函数**

`types/index.ts`：`D3ImportRun`/`D3ContainmentSnapshot`/`D3ImpactReport`/`D3AiAdvice`/`D3AdviceAdoption`/`D3Execution` + 请求类型（对照 spec §3 字段）。
`api/capa.ts`：`importD3Containment`/`getD3Runs`/`getD3Snapshots`/`generateD3Report`/`getD3Report`/`generateD3Advice`/`getD3Advice`/`decideD3Advice`/`getD3Adoptions`/`recordD3Execution`/`updateD3Execution`/`getD3Executions`。

- [ ] **Step 2: 写组件 vitest（失败）**

`D3ContainmentPanel.test.tsx`：按钮门控（canEdit('capa')+status==D3_INTERIM）；导入后展示 4 类快照卡片；报告 5 项展示（批追溯/影响数量/客户影响真名/风险分级 Tag/时间窗口+risk_explanation）；blocked/failed/superseded banner+重试；建议列表 advice_type Tag（`data-e2e="d3-advice-type-{type}"`）+ provenance 徽标（`data-e2e="d3-advice-provenance"` 展开 snapshot_id+record_key+source_type）+ 采纳/拒绝按钮；采纳弹窗编辑 adopted_text；执行记录区（`data-e2e="d3-execution-list"`）+ PATCH 状态；代次切换（历史代只读）；superseded 刷新。

- [ ] **Step 3: 实现组件 + 跑 vitest + tsc + Commit**

```bash
cd frontend && npm run test -- D3ContainmentPanel && npx tsc --noEmit
git add frontend/src/components/capa/D3ContainmentPanel.tsx frontend/src/types/index.ts frontend/src/api/capa.ts frontend/src/components/capa/__tests__/D3ContainmentPanel.test.tsx
git commit -m "feat(d3): 前端 D3ContainmentPanel（导入/报告/建议/采纳/执行+代次切换+superseded 刷新）"
```

---

### Task 12: E2E seed 扩展 + spec + 主流程

**Files:**
- Modify: `frontend/e2e/seed_e2e`（或对应 seed 模块）
- Create: `frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts`

- [ ] **Step 1: 扩 seed**

`seed_e2e`：产品线 `DC-DC-100-E2E` 加 iqc_inspections 记录（含 supplier_id）+ mock `ERPConnection` + `ERPInventoryBalance` + `ERPShipment`（erp_raw_data 显式带 `arrival_status` in_transit/signed + `unit`）+ 关联 ShipmentRecord + `Customer.segment='key'`。

- [ ] **Step 2: 写 E2E 主流程（失败）**

`capa-story-d3-containment.spec.ts`：AI spec，无凭证 skip 整测（`_guards/ai-credentials.guard` 模式）。主流程 9 步（spec §11 行 619-628）：engineer D2→D3 → import 200+run+4 类快照+report_status=done → 报告 5 项齐全+risk_level 枚举+status=done+客户名真名 → advice 列表非空+advice_type 枚举+source_provenance 非空（snapshot_id+record_key+source_type+stage）+target_batch_refs 存在于 batches → 采纳 1 条→adoptions 回读 → 记录 execution→executions 回读 → D3→D4 推进成功 → 审计回读（D3_DATA_IMPORTED+D3_REPORT_GENERATED+D3_AI_ADVICE_GENERATED+D3_ADVICE_ADOPTED+D3_EXECUTION_RECORDED+TRANSITION）→ viewer 只读。

- [ ] **Step 3: 跑 E2E 主流程（凭证可用时）+ Commit**

```bash
make e2e TEST_ARGS="--grep d3-containment"
git add frontend/e2e/seed_e2e frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts
git commit -m "test(d3): E2E seed 扩展+主流程 spec（import→报告→建议→采纳→执行→D3→D4+审计回读）"
```

---

### Task 13: E2E 反向/边界断言

**Files:**
- Modify: `frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts`

- [ ] **Step 1: 加反向/边界断言**

`capa-story-d3-containment.spec.ts` 加 spec §11 行 630-656 全部反向断言：未导入/缺类/无报告/无 execution→拒；纯人工推进；重复 import 新 run；报告重生成新 generation 旧 execution 不满足闸口；advice 重生成旧 advice 不可采纳；先执行旧建议→重生成→闸口失败；manual 不受重生成影响；从未生成 advice 纯 manual 推进；advice LLM 失败 200+旧 current 保持；并发报告重试 partial UQ 防双 current；双 import 交错 superseded；import blocked 快照仍提交；execution 降级闸口不再满足；adopt→reject+text 修订 D3_ADVICE_DECISION_CHANGED；无 batch provenance 从 IQC/report-level 生成；alternative record_key 非空；unknown arrival_status+受影响客户→risk_level=high；跨 CAPA/跨工厂 advice_id 拒绝；evidence_refs javascript:/file:→422；快照后改源表报告用旧值；stale worker 复活 CAS 行数 0 丢弃；无凭证 422 blocked + import 仍 200+blocked。

- [ ] **Step 2: 跑 E2E + Commit**

```bash
make e2e TEST_ARGS="--grep d3-containment"
git commit -m "test(d3): E2E 反向/边界断言（闸口/代次/stale/脱敏/跨工厂/scheme 全覆盖）"
```

---

### Task 14: 文档同步 + 最终 `make check`

**Files:**
- Modify: `docs/user-stories/US-E2E-01-capa-8d-closed-loop/US-E2E-01.1-d3-containment-ai.md`（标"实现态"）
- Modify: `PROGRESS.md` / `docs/ROADMAP.md` / `docs/permissions.md`

- [ ] **Step 1: 同步文档**

故事标"实现态 v4（实现于 <date>）"；PROGRESS.md 勾选 01.1 完成；ROADMAP 更新；permissions.md 若新增端点权限点则更新。

- [ ] **Step 2: `make check` + E2E 全量**

```bash
make check
make e2e
git commit -m "docs(d3): 文档同步（故事实现态+PROGRESS+ROADMAP）"
```

---

## Self-Review

**1. Spec coverage:**
- §3.1–§3.7 7 表 → Task 1 ✓
- §4.1 导入 5 步 + 子源 → Task 2 ✓
- §4.2 报告三阶段 + 确定性 + risk floor → Task 5+6 ✓
- §4.3 建议三阶段 + accepted_advice + provenance → Task 8 ✓
- §4.4 采纳/拒绝 → Task 9 ✓
- §4.5 执行记录 + PATCH → Task 10 ✓
- §5 闸口 → Task 3 ✓
- §6 LLM 契约 → Task 6+8 ✓
- §7 9 端点 → Task 4+7+9+10 ✓
- §8 审计事件 → 各服务任务内 ✓
- §10 前端 → Task 11 ✓
- §11 E2E → Task 12+13 ✓
- §12 测试矩阵各场景 → 分散到各任务测试 ✓
- §13 文档同步 → Task 14 ✓

**2. Placeholder scan:** 无 TBD/TODO（Task 2 事务 B 占位在 Task 6 接通，已在 Task 4 Step 3 说明）。

**3. Type consistency:** 模型类名（Task 1）→ 服务函数名（Task 2+5+6+8+9+10）→ API 端点（Task 4+7+9+10）→ 前端 api 函数名（Task 11）一致，均对照 spec。`generate_impact_report`/`generate_advice`/`import_containment_data`/`adopt_advice`/`record_execution`/`update_execution` 跨任务名一致。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-10-us-e2e-01.1-d3-containment.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?