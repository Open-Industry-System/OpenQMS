# US-E2E-01.1 D3 遏制全链路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 CAPA 8D 的 D3 阶段实现「数据导入 → 受影响范围分析报告 → AI 遏制建议 → 采纳/执行记录 → D3→D4 闸口」全链路（AI_REQUIRED=true，无 LLM 凭证 BLOCKED）。

**Architecture:** 7 张不可变 generation 表（import_run→report→advice_generation→advice + adoption + execution），三阶段 LLM 短事务（锁+建 running artifact+提交 → 无锁 LLM → 重锁+attempt_token CAS 晋升/失败），事实确定性计算 + LLM 只做风险分级与建议文本，D3→D4 闸口 fail-closed。详见 `docs/superpowers/specs/2026-07-10-us-e2e-01.1-d3-containment-design.md`（v15 定稿）——**本计划是任务分解，所有字段/CHECK/CAS/枚举契约以 spec 为唯一真源，实现时逐条对照 spec §3/§4/§5/§6**。

**Tech Stack:** Python 3.11 + FastAPI 0.115 (async) + SQLAlchemy 2.0 (async) + PostgreSQL 15 + Pydantic v2 + React 18 + TypeScript 5.6 + Playwright E2E

## Global Constraints

- `factory_id` NOT NULL 全表（7 表）+ 复合 FK 保证父子 factory 一致（spec §3 表头 + §14）
- 统一 ON DELETE RESTRICT + CAPA 软删除（归档不物理删）；存在 D3 审计实体禁止物理删 CAPA/run/report/generation（spec §3 表头）
- 不可变 generation 覆盖 run→report→advice_generation→advice；每层 `is_current` partial unique index + `status` running/done/failed 三态 + 综合 CHECK（spec §3.1/§3.3/§3.5）
- attempt_token CAS：所有 terminal transition（成功 + 所有失败分支）用 `WHERE id AND status='running' AND attempt_token=:token`，行数 0 丢弃（spec §4.2/§4.3 + §14）
- 原子晋升分步顺序：run 5 步、report 4 步、advice generation 5 步；CAS done/is_current 保持 false→旧 false→新 true+completed_at→回滚（spec §3.1/§3.3/§3.5 + §14）
- done 完整性 CHECK：report done 必含 5 项确定性结果 + risk_floor + risk_explanation 非空 + llm_available=true + completed_at；advice done 必 advice_count>0 + llm_available=true（spec §3.3/§3.5 + §14）
- 失败分支审计全覆盖：error code 枚举 `superseded`/`llm_failed`/`schema_failed`/`all_provenance_mapping_failed`/`unknown_risk_mapping_version`/`stale`，写 stage_runs（spec §3.5 + §4.2/§4.3）
- 快照固化分析数据：shipment payload 固化 unit/customer_segment/customer_name/arrival_status；run.analysis_context 固化 capa_severity + risk_mapping_version（唯一真源）；报告只读 payload + run.analysis_context 不查源表（spec §3.1/§3.2 + §4.2）
- RISK_MAPPINGS 版本注册表：服务层常量，旧版本不可删，未知版本→failed 不静默（spec §3.3 + §14）
- record_key 永不为空只有 snapshot_id 可空；batch_key=hash(normalized_material+normalized_lot)，lot 缺失退化 `{snapshot_type}:{source_id}`（spec §3.2/§3.4）
- IQC 查 `iqc_inspections` JOIN suppliers（非 iqc_materials 主数据）（spec §4.1）
- AI_REQUIRED=true：无凭证 BLOCKED（422 blocked 独立端点；import 端点 report_status=blocked）；建议生成先检查凭证再建 generation（spec §4.2/§4.3）
- 重复请求返 202 Accepted + 现有 running artifact ID + status + Retry-After header（秒）（spec §3.3/§3.5 + §7）
- 状态机：D3_INTERIM→D4_ROOT_CAUSE 线性边已在 `eightd_state.py`，无需改状态机；D3 阶段守卫 `capa.status == D3_INTERIM`（spec §5 + §14）
- 发布策略：A+B 原子发布（Slice A/B 仅实现顺序，不分阶段上线）（spec §2 + §14）
- 复用：`provider_adapter.complete_json` + `provider_adapter.build_client`/`ProviderNotConfiguredError`（01.2/01.3 已迁移）、`capa_draft_service` 的 MAX_PROMPT_CHARS=8000 截断 + safety_trailer + DB-backed timeout 口径、`backend/tests/migrations/` 迁移测试基础设施（01.2/A3 已建 `mig_db_url` fixture + `_cfg` helper）、`capa_verification_service` 的 AuditLog 写法
- seed 在 `backend/app/seed_e2e.py` + `backend/app/seed_e2e_constants.py`（**非** frontend/e2e 下）

**Spec 契约索引**（实现时逐条对照）：
- 数据模型 7 表：spec §3.1–§3.7（每列类型/约束 + partial UQ + 综合 CHECK + 复合 FK）
- 服务层：spec §4.1（导入 5 步 + 子源查询）、§4.2（报告三阶段 + 确定性 4 项 + risk floor）、§4.3（建议三阶段 + accepted_advice + provenance 映射）、§4.4（采纳/拒绝 + 文本修订）、§4.5（执行记录 + PATCH）
- 闸口：spec §5（fail-closed，先锁 current run，4 类齐全 + 报告 done + execution 有效）
- LLM 契约：spec §6.1（报告输入/输出）、§6.2（建议输出）、§6.3（客户名脱敏/还原）
- API 端点 **12 条**：spec §7（import/runs/snapshots 3 + report 2 + advice 2 + decision/adoptions 2 + execution 3）
- 审计事件 8 个：spec §8
- 权限：spec §9
- 前端：spec §10
- E2E：spec §11
- 测试矩阵：spec §12

---

## File Structure

**Backend — 新建：**
- `backend/app/models/capa_d3.py` — 7 张 D3 表的 SQLAlchemy 2.0 ORM（一个文件，按表分 class）
- `backend/app/services/capa_d3_containment_service.py` — 导入 + 报告 + 建议 + 采纳 + 执行服务（按 spec §4 分函数，私有 `_query_*`/`_compute_*`/`_cas_*` helper）
- `backend/app/services/capa_d3_risk_mappings.py` — `RISK_MAPPINGS` 版本注册表常量（spec §3.3）+ `CURRENT_RISK_MAPPING_VERSION = "v1"`
- `backend/app/schemas/capa_d3.py` — Pydantic v2 请求/响应 schema
- `backend/alembic/versions/<rev>_d3_containment_tables.py` — 7 表 + partial UQ + CHECK + 复合 FK 迁移（手写）

**Backend — 修改：**
- `backend/app/models/__init__.py` — 注册 D3 模型
- `backend/app/api/capa.py` — 加 12 个 D3 端点 + D3 阶段守卫
- `backend/app/services/capa_service.py` — `advance_capa` 新增 D3→D4 分支调用 `_d3_to_d4_gate`
- `backend/app/seed_e2e.py` + `backend/app/seed_e2e_constants.py` — 扩 E2E seed（mock ERP + iqc_inspections + Customer.segment）

**Backend 测试 — 新建：**
- `backend/tests/migrations/test_d3_containment_migration.py`
- `backend/tests/capa/test_capa_d3_import.py`
- `backend/tests/capa/test_capa_d3_report.py`
- `backend/tests/capa/test_capa_d3_advice.py`
- `backend/tests/capa/test_capa_d3_adoption.py`
- `backend/tests/capa/test_capa_d3_execution.py`
- `backend/tests/capa/test_capa_d3_gate.py`
- `backend/tests/capa/test_capa_d3_api.py` — 12 端点契约

**Frontend — 新建/修改：**
- `frontend/src/components/capa/D3ContainmentPanel.tsx` — D3 面板
- `frontend/src/pages/capa/CAPADetailPage.tsx` — **挂载** D3ContainmentPanel 到 D3 区域
- `frontend/src/pages/capa/CAPADetailPage.test.tsx` — 验证挂载/权限/状态/历史代只读
- `frontend/src/api/capa.ts` — 加 12 个 D3 端点函数
- `frontend/src/types/index.ts` — 加 D3 类型
- `frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts` — E2E

**文档：**
- `docs/user-stories/US-E2E-01-capa-8d-closed-loop/US-E2E-01.1-d3-containment-ai.md` — 标"实现态"
- `PROGRESS.md` / `docs/ROADMAP.md` / `docs/permissions.md` — 同步（CI docs-check）

---

## Task 依赖顺序（消除 pending/TODO 临时态）

> **关键**：为避免 import API 返回不存在的 `report_status='pending'`，import API 必须在「报告生成服务」完成后才开放。顺序：模型/迁移 → 导入服务(事务 A) → 确定性计算 → 报告生成(事务 B) → **import API（此时 report_status 只会是 done/failed/blocked/superseded）** → 闸口 → 报告 API → 建议服务 → 建议 API → 采纳 → 执行 → 前端 → E2E → 文档。

---

## Slice A — 非 AI 骨架

### Task 1: 7 张 D3 表 ORM 模型 + 迁移 + 迁移测试

**Files:**
- Create: `backend/app/models/capa_d3.py`
- Create: `backend/alembic/versions/<rev>_d3_containment_tables.py`
- Create: `backend/tests/migrations/test_d3_containment_migration.py`
- Modify: `backend/app/models/__init__.py`

**Interfaces:**
- Produces: `CapaD3ImportRun`/`CapaD3ContainmentSnapshot`/`CapaD3ImpactReport`/`CapaD3AdviceGeneration`/`CapaD3AiAdvice`/`CapaD3AdviceAdoption`/`CapaD3Execution` 模型类

- [ ] **Step 1: 写迁移测试（失败 — red）**

`backend/tests/migrations/test_d3_containment_migration.py`：复用 `mig_db_url`/`_cfg`（01.2/A3 模式）。写测试断言 7 表存在 + 关键约束：
```python
def test_d3_tables_created(mig_db_url):
    # upgrade 后查 information_schema 确认 7 张 capa_d3_* 表存在
    ...
def test_import_run_partial_uq_rejects_two_current(mig_db_url):
    # 插入两条同 capa_id is_current=true → IntegrityError
    ...
def test_report_done_check_requires_risk_explanation_and_llm(mig_db_url):
    # 插入 status='done', is_current=false, completed_at=now(), risk_explanation=NULL → IntegrityError
    # 插入 status='done', llm_available=false → IntegrityError
    ...
def test_execution_check_manual_requires_null_advice(mig_db_url):
    # source='manual' AND advice_id 非空 → IntegrityError
    ...
def test_adoption_uq_advice_id_single_decision(mig_db_url):
    # 同 advice_id 两条 → IntegrityError
    ...
```
其余约束（复合 FK 跨 factory 拒、adoption CHECK、advice done→advice_count>0、advice done→llm_available、import_run CHECK is_current→completed）各一个测试。

- [ ] **Step 2: 运行测试，确认失败（green 前的 red）**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/migrations/test_d3_containment_migration.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.models.capa_d3'`（模型未建）

- [ ] **Step 3: 写 ORM 模型（最小实现）**

`backend/app/models/capa_d3.py`：按 spec §3.1–§3.7 定义 7 个 class，逐列对照 spec 表头照搬类型/约束/default/partial UQ/CHECK/复合 FK。关键签名：
```python
class CapaD3ImportRun(Base):
    __tablename__ = "capa_d3_import_run"
    run_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    capa_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("capa_eightd.report_id", ondelete="RESTRICT"), nullable=False)
    factory_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False)
    is_current: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    status: Mapped[str] = mapped_column(String(12), nullable=False, default="importing")
    imported_types: Mapped[list] = mapped_column(JSONB, default=list)
    analysis_context: Mapped[dict] = mapped_column(JSONB, nullable=False)
    # ... started_at/completed_at/imported_by/created_at
    __table_args__ = (
        Index("uq_d3_run_current", "capa_id", unique=True, postgresql_where=text("is_current = true")),
        CheckConstraint("NOT (is_current = true AND (status != 'completed' OR completed_at IS NULL))", name="chk_d3_run_current_completed"),
        UniqueConstraint("run_id", "factory_id", name="uq_d3_run_factory"),
    )
```
其余 6 表同模式照搬 spec §3.2–§3.7（含 report/advice_generation 的综合 CHECK + done 完整性 CHECK + running partial UQ；advice 的复合 FK + UNIQUE(advice_id,generation_id,factory_id)；adoption 的 UQ(advice_id)+CHECK；execution 的 CHECK manual/adopted + 三组复合 FK）。`capa_eightd` 加 `UNIQUE(report_id, factory_id)`（若 spec §3 表头要求）。

`backend/app/models/__init__.py`：import 7 个 class。

- [ ] **Step 4: 写迁移**

`backend/alembic/versions/<rev>_d3_containment_tables.py`：`upgrade()` 用 `op.create_table` 建 7 表 + 所有 `Index`（partial UQ 用 `postgresql_where=sa.text(...)`）+ `CheckConstraint` + 复合 `ForeignKeyConstraint`；`capa_eightd` 加 `UNIQUE(report_id, factory_id)`；`downgrade()` 逆序 drop。

- [ ] **Step 5: 运行测试，确认通过（green）**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/migrations/test_d3_containment_migration.py -v`
Expected: PASS

- [ ] **Step 6: 跑 make check 确认无回归**

Run: `make check`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add backend/app/models/capa_d3.py backend/app/models/__init__.py backend/alembic/versions/*_d3_containment_tables.py backend/tests/migrations/test_d3_containment_migration.py
git commit -m "feat(d3): 7 张 D3 containment 表 ORM + 迁移 + 迁移测试（不可变 generation/partial UQ/综合 CHECK/复合 FK）"
```

---

### Task 2: RISK_MAPPINGS 注册表 + 导入服务（事务 A：4 子源查询 + 5 步 run 晋升）

**Files:**
- Create: `backend/app/services/capa_d3_risk_mappings.py`
- Create: `backend/app/services/capa_d3_containment_service.py`（导入部分）
- Create: `backend/tests/capa/test_capa_d3_import.py`

**Interfaces:**
- Produces: `RISK_MAPPINGS`（dict）、`CURRENT_RISK_MAPPING_VERSION="v1"`、`import_containment_data(db, capa_id, user, request) -> dict`（本任务只事务 A，事务 B 报告在 Task 4 接通；import API 在 Task 6 才开放，故本任务不暴露公共端点，无 pending 态）

- [ ] **Step 1: 写导入测试（失败 — red）**

`backend/tests/capa/test_capa_d3_import.py`：fixture 建 capa(D3_INTERIM)+factory+ERPInventoryBalance/ERPShipment(erp_raw_data arrival_status)+IqcInspection+SPCAlarm+Customer。测试：
```python
async def test_import_creates_run_with_4_snapshots(db, capa_d3_setup):
    result = await import_containment_data(db, capa.report_id, user, D3ImportRequest())
    assert set(s["snapshot_type"] for s in result["snapshots"]) == {"inventory","shipment","iqc","spc"}
    run = await db.get(CapaD3ImportRun, result["run_id"]); assert run.is_current is True; assert run.status == "completed"
async def test_shipment_payload_fixes_unit_customer_segment_arrival(db, ...):
    # snapshot payload 含 unit/customer_segment/customer_name/arrival_status（从 erp_raw_data 读，缺失→unknown）
async def test_iqc_queries_inspections_not_materials(db, ...):
    # 断言 iqc snapshot source_id 是 inspection_id（来自 iqc_inspections）
async def test_empty_source_still_persists_zero_count(db, ...):
async def test_new_run_demotes_old_current_5_step_no_uq_clash(db, ...):
    # 已有旧 current run，新 import 后旧 is_current=false，新 is_current=true，不撞 partial UQ
async def test_analysis_context_freezes_severity_and_mapping_version(db, ...):
async def test_double_click_lock_serializes_two_runs(db, ...):
async def test_d3_data_imported_audit_written(db, ...):
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_import.py -v`
Expected: FAIL — `ImportError: cannot import name 'import_containment_data'`

- [ ] **Step 3: 写 RISK_MAPPINGS + 导入服务（最小实现）**

`backend/app/services/capa_d3_risk_mappings.py`：
```python
RISK_MAPPINGS = {"v1": {"critical": "high", "fatal": "high", "serious": "medium", "general": "low"}}
CURRENT_RISK_MAPPING_VERSION = "v1"
```

`backend/app/services/capa_d3_containment_service.py`：实现 `import_containment_data`（spec §4.1 事务 A 5 步）。私有 `_query_inventory`/`_query_shipment`/`_query_iqc`/`_query_spc`：product_line_code + factory_id 隔离；每条 payload 带 `record_key`+`source_id`。shipment unit 从 `erp_raw_data["unit"]`→物料主数据→缺失 'unknown'；arrival_status 从 `erp_raw_data["arrival_status"]` 读，枚举校验，缺失/非法→'unknown'；customer_segment/customer_name 从 customers join 固化。iqc 查 `IqcInspection` JOIN suppliers。spc 复用 SPCAnomalySource 30 天口径。**本任务不调报告生成（事务 B 在 Task 4）**——返回 dict 不含 report_status（或仅事务 A 摘要）；import API 在 Task 6 开放时才接通事务 B 并返回 report_status。

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_import.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/capa_d3_risk_mappings.py backend/app/services/capa_d3_containment_service.py backend/tests/capa/test_capa_d3_import.py
git commit -m "feat(d3): RISK_MAPPINGS 注册表 + 导入服务事务 A（4 子源查询+5 步 run 晋升+analysis_context 固化）"
```

---

### Task 3: 确定性计算（4 项事实 + risk floor + batch_key + 单位数组，纯函数）

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `_compute_*`）
- Create: `backend/tests/capa/test_capa_d3_report.py`（计算部分）

**Interfaces:**
- Produces: `_compute_batches(snapshots)->list`、`_compute_impact_qty(batches)->dict`、`_compute_customer_impact(shipment_snapshot)->list`、`_compute_time_window(spc_snapshot)->dict`、`_compute_risk_floor(customer_impact, analysis_context)->tuple[str|None, str|None]`（返回 `(floor, error_code)`，未知版本返回 `(None, "unknown_risk_mapping_version")`）

- [ ] **Step 1: 写确定性计算测试（失败 — red）**

`backend/tests/capa/test_capa_d3_report.py`：
```python
def test_batch_key_hash_material_lot_merges_cross_source():
    # inventory + shipment 同 material+lot → 同 batch_key
def test_batch_key_degrades_when_lot_missing():
    # lot_no 缺失 → batch_key = "{snapshot_type}:{source_id}"
def test_qty_by_status_only_inventory_shipment_contribute():
    # IQC lot_qty + SPC 不进 qty_by_status（防 100+70+30=200，应 70+30）
def test_qty_by_status_multi_unit_not_merged():
    # 同状态 pcs + kg → 两元素数组，不合并
def test_dedup_by_source_id():
def test_customer_impact_quantities_array_with_segment_from_payload():
def test_time_window_alarm_min_max():
def test_risk_floor_unknown_arrival_with_customer_is_high():
def test_risk_floor_severity_via_risk_mappings_version():
    # analysis_context.risk_mapping_version="v1", capa_severity="serious" → "medium"
def test_risk_floor_unknown_mapping_version_returns_error_code():
    # version="v9" → (None, "unknown_risk_mapping_version")
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_report.py -v`
Expected: FAIL — `AttributeError: module ... has no attribute '_compute_batches'`

- [ ] **Step 3: 实现 `_compute_*`（最小实现）**

`capa_d3_containment_service.py`：5 个纯函数（spec §4.2 行 370-375 + §3.2 batch_key 规则）。`_compute_risk_floor` 返回 `(floor|None, error_code|None)`。

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_report.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/capa_d3_containment_service.py backend/tests/capa/test_capa_d3_report.py
git commit -m "feat(d3): 确定性计算 4 项事实 + risk floor（batch_key=hash+单位数组+IQC/SPC 仅证据+RISK_MAPPINGS 查表）"
```

---

### Task 4: 报告生成服务（事务 B：三阶段短事务 + attempt_token CAS + risk floor + 脱敏还原 + prompt 截断 + 接通 import 事务 B）

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `generate_impact_report` + `_cas_report_*` + 接通 `import_containment_data` 事务 B）
- Modify: `backend/tests/capa/test_capa_d3_report.py`（加 LLM 三阶段测试）
- Modify: `backend/tests/capa/test_capa_d3_import.py`（加事务 B 接通断言）

**Interfaces:**
- Consumes: `provider_adapter.build_client`/`complete_json`/`ProviderNotConfiguredError`、`capa_draft_service` MAX_PROMPT_CHARS + safety_trailer 口径
- Produces: `generate_impact_report(db, run_id, user) -> dict`（返回 `{status: done/failed/blocked/superseded, report?, error?}`）；`import_containment_data` 现返回完整 `{run_id, snapshots, report_status, report?, report_error?}`（report_status 只会是 done/failed/blocked/superseded，**无 pending**）

- [ ] **Step 1: 写报告三阶段测试（失败 — red）**

`test_capa_d3_report.py` 加：
```python
async def test_report_three_phase_running_then_done(db, llm_mock):
    # 阶段①建 running 行 is_current=false；阶段②无锁 LLM；阶段③CAS done+llm_available=true+model+stage_runs
async def test_done_check_rejects_without_llm_available(db, ...):
async def test_attempt_token_cas_stale_worker_discarded(db, llm_slow):
    # A running→B 标 A failed→A LLM 晚到→CAS 行数 0 丢弃不晋升
async def test_duplicate_request_returns_202_with_retry_after(db, ...):
    # 已有 running → 返回 (202, {report_id, status:running}, Retry-After header)
async def test_llm_failed_writes_stage_runs_error_code(db, ...):
async def test_superseded_writes_error_code_no_current_switch(db, ...):
async def test_unknown_risk_mapping_version_failed(db, ...):
async def test_stale_recovery_cas_running_to_failed(db, ...):
async def test_prompt_truncated_within_max_chars_with_prompt_stats(db, ...):
async def test_risk_explanation_customer_name_restored(db, ...):
async def test_risk_floor_max_llm_and_unknown_high(db, ...):
async def test_snapshot_only_no_source_table_query(db, ...):
    # 快照后改 capa.severity → risk floor 用 import 时固化值（v9 F2）
async def test_no_creds_returns_blocked_not_build_running(db, ...):
```
`test_capa_d3_import.py` 加：
```python
async def test_import_transaction_b_does_not_rollback_a_on_failure(db, ...):
    # 事务 B 报告失败 → run/快照仍提交（事务 A 独立）
async def test_import_report_status_only_done_failed_blocked_superseded(db, ...):
    # 断言 report_status ∈ {done,failed,blocked,superseded}，无 pending
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_report.py tests/capa/test_capa_d3_import.py -v`
Expected: FAIL — `AttributeError: ... has no attribute 'generate_impact_report'`

- [ ] **Step 3: 实现 `generate_impact_report` + 接通事务 B（最小实现）**

`capa_d3_containment_service.py`（spec §4.2 行 361-382）：三阶段函数。阶段①锁 capa+重检 run.is_current+建 report running 行（attempt_token=uuid4）+提交。阶段②无锁：读 snapshot payload + run.analysis_context → `_compute_*` → 精简 prompt（脱敏 customer_0X + 确定性事实 + 统计摘要 + analysis_context + 截断标记）+ safety_trailer → `complete_json`。阶段③重锁+重检：五分支（成功/superseded/llm_failed/schema_failed/unknown_risk_mapping_version）CAS UPDATE，全部含 `WHERE report_id AND status='running' AND attempt_token` 行数 0 丢弃；成功 CAS 含 `llm_available=true, model, stage_runs, prompt_stats, batches, impact_qty, customer_impact, time_window, risk_level, risk_floor, risk_explanation`（还原真名）；成功后 4 步晋升。stale 恢复 CAS（error='stale'）。无凭证→BLOCKED 返 blocked（不建 running report）。
`import_containment_data` 事务 B 调 `generate_impact_report`，按返回填 report_status（done/failed/blocked/superseded）；事务 B 异常不回滚事务 A。

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_report.py tests/capa/test_capa_d3_import.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/capa_d3_containment_service.py backend/tests/capa/test_capa_d3_report.py backend/tests/capa/test_capa_d3_import.py
git commit -m "feat(d3): 报告生成三阶段短事务（attempt_token CAS+stale 恢复+risk floor+脱敏还原+prompt 截断+done 完整性 CHECK+接通 import 事务 B 无 pending）"
```

---

### Task 5: D3→D4 闸口

**Files:**
- Modify: `backend/app/services/capa_service.py`（`advance_capa` 加 D3→D4 分支）
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `_d3_to_d4_gate`）
- Create: `backend/tests/capa/test_capa_d3_gate.py`

**Interfaces:**
- Produces: `_d3_to_d4_gate(db, capa)`（fail-closed）

- [ ] **Step 1: 写闸口测试（失败 — red）**

`backend/tests/capa/test_capa_d3_gate.py`：12 场景（spec §11 反向 + §12 gate 矩阵）：
```python
async def test_gate_rejects_no_import(db, capa_d3): ...  # 未导入→拒
async def test_gate_rejects_missing_one_type(db, ...): ...  # 只导 3 类→拒
async def test_gate_rejects_no_report_done(db, ...): ...
async def test_gate_rejects_no_execution(db, ...): ...
async def test_gate_passes_manual_execution_with_done_report(db, ...): ...  # 纯人工推进
async def test_gate_passes_never_generated_advice_pure_manual(db, ...): ...  # v6 P0
async def test_gate_rejects_old_generation_adopted_execution(db, ...): ...  # 旧 gen 不满足
async def test_gate_fails_after_regenerate_advice(db, ...): ...  # 先执行旧建议→重生成→失败
async def test_gate_manual_unaffected_by_advice_regenerate(db, ...): ...
async def test_gate_fails_after_execution_demote_to_failed(db, ...): ...
async def test_gate_rejects_failed_report(db, ...): ...
async def test_gate_rejects_superseded_report(db, ...): ...
async def test_gate_factory_mismatch_rejected(db, ...): ...
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_gate.py -v`
Expected: FAIL — 闸口未实现，advance_capa D3→D4 无校验直接推进

- [ ] **Step 3: 实现闸口（最小实现）**

`capa_d3_containment_service.py` 加 `_d3_to_d4_gate(db, capa)`（spec §5 伪代码照搬）：先取 current run（partial UQ）+ factory 校验 → 4 类 snapshot_type 齐全 → current report status='done'（先锁 current run 再在其下取 is_current report）→ current advice_generation（可 null）→ 有效 execution 计数（`source='manual' OR (source='adopted' AND generation_id=current_gen)`，result_status IN completed/in_progress，failed 不计）→ 0 则 raise ValueError。
`capa_service.advance_capa` 加：`elif current == EightDState.D3_INTERIM and target == EightDState.D4_ROOT_CAUSE: await _d3_to_d4_gate(db, capa)`。

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_gate.py -v`
Expected: PASS

- [ ] **Step 5: 跑 capa 既有测试无回归**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/ -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/capa_service.py backend/app/services/capa_d3_containment_service.py backend/tests/capa/test_capa_d3_gate.py
git commit -m "feat(d3): D3→D4 闸口（fail-closed：4 类齐全+报告 done+execution 有效+generation 校验+factory 一致）"
```

---

### Task 6: import + runs + snapshots + report API 端点（12 端点之 5）

**Files:**
- Create: `backend/app/schemas/capa_d3.py`（import/runs/snapshots/report 部分）
- Modify: `backend/app/api/capa.py`（import + runs + snapshots + report POST/GET 5 端点）
- Create: `backend/tests/capa/test_capa_d3_api.py`（import + report 部分）

> 此时报告服务已完成（Task 4），import API 返回的 report_status 只会是 done/failed/blocked/superseded，**无 pending**。

- [ ] **Step 1: 写 API 测试（失败 — red）**

`backend/tests/capa/test_capa_d3_api.py`：
```python
async def test_import_returns_200_with_report_status(client, ...):  # report_status ∈ {done,failed,blocked,superseded}
async def test_import_viewer_forbidden(client, ...):
async def test_import_non_d3_stage_rejected(client, ...):  # 跨阶段守卫
async def test_import_cross_factory_404(client, ...):
async def test_get_runs_lists_current(client, ...):
async def test_get_snapshots_current_run(client, ...):
async def test_report_post_no_creds_422_blocked(client, ...):
async def test_report_post_running_returns_202_body_and_retry_after_header(client, ...):  # body {report_id,status:running} + Retry-After 正整数秒
async def test_report_post_done_returns_200(client, ...):
async def test_report_get_current(client, ...):
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_api.py -v`
Expected: FAIL — 404（端点未注册）

- [ ] **Step 3: 写 schema + 端点（最小实现）**

`backend/app/schemas/capa_d3.py`：`D3ImportRequest`（snapshot_types 默认全 4 类，params optional）、`D3SnapshotSummary`、`D3ImportResponse`（run_id, snapshots[], report_status: Literal["done","failed","blocked","superseded"], report?, report_error?）、`D3RunResponse`、`D3SnapshotResponse`、`D3ReportResponse`、`D3ReportRunningResponse`（report_id, status:running）、`D3AdviceRequest`、`D3AdviceResponse`。
`backend/app/api/capa.py`：加 5 端点（`POST /d3/import`、`GET /d3/runs`、`GET /d3/snapshots`、`POST /d3/report`、`GET /d3/report`）。D3 阶段守卫 helper（仿 `_assert_d7_stage`）。`POST /d3/report` 按返回：无凭证→422 `detail.blocked`；已有 running→202 body `{report_id,status:running}` + `Retry-After` header（正整数秒）；完成→200 status=done/failed/superseded。

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_api.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/schemas/capa_d3.py backend/app/api/capa.py backend/tests/capa/test_capa_d3_api.py
git commit -m "feat(d3): import/runs/snapshots/report API 5 端点（始终 200+report_status+422 blocked+202 running+Retry-After+阶段守卫+factory 校验）"
```

---

> **Slice A 完成节点**：7 表 + 导入 + 确定性计算 + 报告 + 闸口 + 5 端点可独立落地，`make check` 绿。

---

## Slice B — AI 链路

### Task 7: 建议生成服务（advice_generation 三阶段 + accepted_advice + provenance 映射 + 空建议处理）

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `generate_advice` + provenance 映射）
- Create: `backend/tests/capa/test_capa_d3_advice.py`

**Interfaces:**
- Produces: `generate_advice(db, capa_id, report_id, user, request) -> dict`（返回 `{status: done/failed/blocked, generation_id?, advice_count?, rejected_advice_count?}`）

- [ ] **Step 1: 写建议服务测试（失败 — red）**

`backend/tests/capa/test_capa_d3_advice.py`：
```python
async def test_advice_three_phase_running_then_done(db, llm_mock): ...
async def test_advice_done_check_requires_advice_count_and_llm(db, ...): ...
async def test_attempt_token_cas_stale_worker_discarded(db, ...): ...
async def test_duplicate_request_202_with_retry_after(db, ...): ...
async def test_no_creds_422_no_generation_built(db, ...): ...  # 先检查凭证再建 generation
async def test_provenance_recall_maps_shipment_snapshot(db, ...): ...
async def test_provenance_isolate_maps_inventory(db, ...): ...
async def test_provenance_strict_inspection_maps_iqc(db, ...): ...
async def test_provenance_alternative_report_level_record_key_nonempty(db, ...): ...  # record_key='report:{id}:summary'
async def test_record_key_never_null_only_snapshot_id_nullable(db, ...): ...
async def test_target_batch_refs_must_exist_in_batches(db, ...): ...
async def test_advice_text_customer_name_restored(db, ...): ...
async def test_all_provenance_failed_returns_200_failed_no_current_switch(db, ...): ...  # accepted_advice 空→failed 不切 current
async def test_partial_filtered_accepted_nonempty_advances(db, ...): ...
async def test_filtered_advice_stage_runs_persisted_db_readable(db, ...): ...  # 断言 DB stage_runs 非只返回
async def test_success_cas_writes_llm_available_model_advice_count(db, ...): ...
async def test_all_failure_branches_write_stage_runs_error_code(db, ...): ...  # superseded/llm_failed/schema_failed/all_provenance_mapping_failed
async def test_stale_recovery_cas_running_to_failed(db, ...): ...
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_advice.py -v`
Expected: FAIL — `AttributeError: ... has no attribute 'generate_advice'`

- [ ] **Step 3: 实现 `generate_advice`（最小实现）**

`capa_d3_containment_service.py`（spec §4.3 行 389-408）：先检查凭证（无凭证→422 不建 generation）；阶段①锁 capa+读当前 report+建 generation running（attempt_token）+提交；阶段②无锁 LLM + 内存 schema/批次/provenance 映射得 accepted_advice + rejected 列表（映射规则按 spec §4.3 行 400-404）；阶段③五分支 CAS（成功/空/superseded/llm_failed/schema_failed），全部 `WHERE generation_id AND status='running' AND attempt_token` 行数 0 丢弃；成功 5 步晋升（CAS done is_current=false+llm_available=true+model+advice_count+rejected_advice_count+stage_runs→逐条 INSERT accepted_advice→旧 false→新 true→回滚）。stale 恢复 CAS（error='stale'）。写 D3_AI_ADVICE_GENERATED 审计。

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_advice.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/capa_d3_containment_service.py backend/tests/capa/test_capa_d3_advice.py
git commit -m "feat(d3): 建议生成三阶段（accepted_advice 驱动+provenance 按 advice_type 映射+空建议 failed 不切 current+过滤原因落 stage_runs+先检查凭证）"
```

---

### Task 8: 建议 API 端点（POST /d3/advice + GET /d3/advice，12 端点之 2）

> **P0 修复**：本任务独立覆盖建议**生成** POST API + schema + 422/202/200 测试（Task 7 只服务，Task 9 只 decision/adoptions，此前漏了 POST advice 端点）。

**Files:**
- Modify: `backend/app/schemas/capa_d3.py`（advice request/response，若 Task 6 未建）
- Modify: `backend/app/api/capa.py`（advice POST + GET 2 端点）
- Modify: `backend/tests/capa/test_capa_d3_api.py`（advice 部分）

- [ ] **Step 1: 写 API 测试（失败 — red）**

`backend/tests/capa/test_capa_d3_api.py` advice 部分：
```python
async def test_advice_post_no_creds_422_blocked(client, ...): ...
async def test_advice_post_running_returns_202_body_and_retry_after_header(client, ...):  # body {generation_id,status:running} + Retry-After 正整数秒
async def test_advice_post_done_returns_200_with_advice_list(client, ...): ...
async def test_advice_post_llm_failed_returns_200_failed(client, ...): ...
async def test_advice_post_all_filtered_returns_200_failed(client, ...): ...
async def test_advice_post_viewer_forbidden(client, ...): ...
async def test_advice_post_non_d3_stage_rejected(client, ...): ...
async def test_advice_post_cross_capa_404(client, ...): ...
async def test_advice_get_current_generation_list(client, ...): ...  # source_provenance 含 snapshot_id+record_key+source_type+stage
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_api.py -v -k advice`
Expected: FAIL — 404（advice POST 端点未注册）

- [ ] **Step 3: 写端点（最小实现）**

`backend/app/api/capa.py`：`POST /d3/advice` 调 `generate_advice`，按返回：无凭证→422 `detail.blocked`；已有 running→202 body `{generation_id,status:running}` + `Retry-After` header（正整数秒）；完成→200（done 含 advice 列表 / failed）。`GET /d3/advice` 取当前 generation 建议列表（含 provenance + adoption 状态）。阶段守卫 + factory 校验。

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_api.py -v -k advice`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/schemas/capa_d3.py backend/app/api/capa.py backend/tests/capa/test_capa_d3_api.py
git commit -m "feat(d3): 建议 API POST/GET 2 端点（422 blocked+202 running+Retry-After+200 done/failed+provenance 回读）"
```

---

### Task 9: 采纳/拒绝服务 + decision/adoptions API（12 端点之 2）

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `adopt_advice`）
- Create: `backend/tests/capa/test_capa_d3_adoption.py`
- Modify: `backend/app/schemas/capa_d3.py` + `backend/app/api/capa.py`（decision POST + adoptions GET）

- [ ] **Step 1: 写采纳测试（失败 — red）**

`backend/tests/capa/test_capa_d3_adoption.py`：
```python
async def test_adopt_inserts_only_advice_id_no_report_id_generation_id(db, ...): ...  # 不写 report_id/generation_id
async def test_adopt_uq_single_decision(db, ...): ...  # UQ(advice_id) 双决策→IntegrityError
async def test_adopt_idempotent_same_decision_text(db, ...): ...
async def test_adopt_change_decision_writes_decision_changed_audit(db, ...): ...  # old/new decision
async def test_adopt_change_adopted_text_writes_decision_changed_audit(db, ...): ...  # old/new text
async def test_adopt_check_adopted_requires_text(db, ...): ...
async def test_adopt_check_rejected_requires_null_text(db, ...): ...
async def test_adopt_cross_generation_advice_rejected(db, ...): ...  # 只当前 generation 可采纳
async def test_adopt_first_writes_adopted_audit(db, ...): ...
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_adoption.py -v`
Expected: FAIL — `AttributeError: ... has no attribute 'adopt_advice'`

- [ ] **Step 3: 实现 `adopt_advice` + 端点（最小实现）**

`capa_d3_containment_service.py`（spec §4.4）：校验 advice 属当前 generation；查 adoption where advice_id → INSERT（只 advice_id+冗余 advice_type/source_provenance）/幂等/UPDATE+`D3_ADVICE_DECISION_CHANGED` 审计。首次写 `D3_ADVICE_ADOPTED`/`D3_ADVICE_REJECTED`。
`backend/app/api/capa.py`：`POST /d3/advice/{advice_id}/decision`（body `{decision, adopted_text?}`）、`GET /d3/adoptions`。

- [ ] **Step 4: 写 API 测试 + 运行通过**

`test_capa_d3_api.py` decision/adoptions 部分：同值幂等/变化 UPDATE/跨 CAPA 404/adoptions 回读。
Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_adoption.py tests/capa/test_capa_d3_api.py -v -k "adopt or decision or adoptions"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/capa_d3_containment_service.py backend/app/schemas/capa_d3.py backend/app/api/capa.py backend/tests/capa/test_capa_d3_adoption.py backend/tests/capa/test_capa_d3_api.py
git commit -m "feat(d3): 采纳/拒绝服务+decision/adoptions API（UQ 单一决策+文本修订审计+generation 校验）"
```

---

### Task 10: 执行记录服务 + execution API（12 端点之 3）

**Files:**
- Modify: `backend/app/services/capa_d3_containment_service.py`（加 `record_execution`/`update_execution`）
- Create: `backend/tests/capa/test_capa_d3_execution.py`
- Modify: `backend/app/schemas/capa_d3.py` + `backend/app/api/capa.py`（execution POST/PATCH/GET）

- [ ] **Step 1: 写执行测试（失败 — red）**

`backend/tests/capa/test_capa_d3_execution.py`：
```python
async def test_manual_execution_null_advice_generation(db, ...): ...  # v6 P0
async def test_never_generated_advice_manual_execution_buildable(db, ...): ...
async def test_adopted_execution_requires_advice_and_generation_same_gen(db, ...): ...
async def test_check_manual_with_advice_id_rejected(db, ...): ...
async def test_patch_demote_completed_to_failed(db, ...): ...
async def test_patch_writes_full_field_audit(db, ...): ...  # result_status+measure_text/evidence_refs old/new 或哈希
async def test_evidence_refs_https_allowed(db, ...): ...
async def test_evidence_refs_relative_allowed(db, ...): ...
async def test_evidence_refs_javascript_rejected_422(db, ...): ...
async def test_evidence_refs_file_data_rejected_422(db, ...): ...
async def test_evidence_refs_max_10(db, ...): ...
async def test_evidence_refs_name_url_length_limits(db, ...): ...  # name≤200, url≤500
async def test_executions_get_returns_current_report(db, ...): ...
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_execution.py -v`
Expected: FAIL — `AttributeError: ... has no attribute 'record_execution'`

- [ ] **Step 3: 实现 `record_execution`/`update_execution` + 端点（最小实现）**

`capa_d3_containment_service.py`（spec §4.5）：manual→advice_id/generation_id null；adopted→advice_id 必填+generation_id=advice.generation_id+有 adopted 决策+属当前 generation。PATCH 全字段 + evidence_refs scheme 校验（https/相对 通过，javascript:/file:/data:→422；最多 10 条；name≤200/url≤500）。写 `D3_EXECUTION_RECORDED`/`D3_EXECUTION_UPDATED` 审计。
`backend/app/api/capa.py`：`POST /d3/execution`、`PATCH /d3/execution/{id}`、`GET /d3/executions`。

- [ ] **Step 4: 写 API 测试 + 运行通过**

`test_capa_d3_api.py` execution 部分：POST manual 无 generation / PATCH 降级 / scheme 422 / GET 回读。
Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_d3_execution.py tests/capa/test_capa_d3_api.py -v -k execution`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/capa_d3_containment_service.py backend/app/schemas/capa_d3.py backend/app/api/capa.py backend/tests/capa/test_capa_d3_execution.py backend/tests/capa/test_capa_d3_api.py
git commit -m "feat(d3): 执行记录服务+execution API 3 端点（report_id 归属+manual/adopted CHECK+PATCH 全字段审计+evidence scheme 白名单）"
```

---

### Task 11: 前端 D3ContainmentPanel + 挂载 CAPADetailPage + types + api

> **P1 修复**：不仅建组件，还挂载到 `CAPADetailPage.tsx` D3 区域 + 改其测试。

**Files:**
- Create: `frontend/src/components/capa/D3ContainmentPanel.tsx`
- Create: `frontend/src/components/capa/__tests__/D3ContainmentPanel.test.tsx`
- Modify: `frontend/src/pages/capa/CAPADetailPage.tsx` — D3 区域挂载 panel
- Modify: `frontend/src/pages/capa/CAPADetailPage.test.tsx` — 验证挂载/权限/状态/历史代只读
- Modify: `frontend/src/types/index.ts` — D3 类型
- Modify: `frontend/src/api/capa.ts` — 12 个 D3 端点函数

- [ ] **Step 1: 写类型 + api 函数（失败 — red 先 tsc）**

`frontend/src/types/index.ts`：`D3ImportRun`/`D3ContainmentSnapshot`/`D3ImpactReport`/`D3AiAdvice`/`D3AdviceAdoption`/`D3Execution` + 请求类型（对照 spec §3 字段）。
`frontend/src/api/capa.ts`：`importD3Containment`/`getD3Runs`/`getD3Snapshots`/`generateD3Report`/`getD3Report`/`generateD3Advice`/`getD3Advice`/`decideD3Advice`/`getD3Adoptions`/`recordD3Execution`/`updateD3Execution`/`getD3Executions`（12 个，对照 spec §7 端点）。
Run: `cd frontend && npx tsc --noEmit` → Expected: FAIL（类型未定义则组件引用报错）

- [ ] **Step 2: 写组件 vitest（失败 — red）**

`frontend/src/components/capa/__tests__/D3ContainmentPanel.test.tsx`：
```tsx
it('shows import button only when canEdit and status==D3_INTERIM', ...)
it('renders 4 snapshot cards after import', ...)
it('renders report 5 items + risk_explanation', ...)
it('shows blocked/failed/superseded banner + retry', ...)
it('renders advice list with advice_type tag + provenance badge', ...)  // data-e2e d3-advice-type-{type} / d3-advice-provenance
it('adopt dialog edits adopted_text', ...)
it('renders execution list + PATCH status', ...)  // data-e2e d3-execution-list
it('historical run read-only', ...)
it('superseded triggers refresh', ...)
```

- [ ] **Step 3: 实现 D3ContainmentPanel（最小实现）**

`frontend/src/components/capa/D3ContainmentPanel.tsx`（spec §10）：props `{capa, canEdit}`。import 按钮（canEdit+status==D3_INTERIM 门控）→ 4 类快照卡片 + run 代次切换；报告 5 项展示（批追溯/影响数量/客户影响真名/风险分级 Tag/时间窗口+risk_explanation）；blocked/failed/superseded banner+重试；建议列表 advice_type Tag + provenance 徽标 + 采纳/拒绝按钮；采纳弹窗编辑 adopted_text；执行记录区 + PATCH；历史代只读；superseded 刷新。

- [ ] **Step 4: 挂载到 CAPADetailPage + 改其测试**

`frontend/src/pages/capa/CAPADetailPage.tsx`：在 D3 区域（status D3_INTERIM 或查看历史时）渲染 `<D3ContainmentPanel capa={capa} canEdit={canEdit('capa')} />`。
`frontend/src/pages/capa/CAPADetailPage.test.tsx`：加测试——D3 阶段渲染 panel；viewer 只读（写按钮隐藏）；历史代只读；非 D3 阶段不渲染 panel。

- [ ] **Step 5: 运行 vitest + tsc，确认通过**

Run: `cd frontend && npm run test -- D3ContainmentPanel CAPADetailPage && npx tsc --noEmit`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/capa/D3ContainmentPanel.tsx frontend/src/components/capa/__tests__/D3ContainmentPanel.test.tsx frontend/src/pages/capa/CAPADetailPage.tsx frontend/src/pages/capa/CAPADetailPage.test.tsx frontend/src/types/index.ts frontend/src/api/capa.ts
git commit -m "feat(d3): 前端 D3ContainmentPanel + 挂载 CAPADetailPage（导入/报告/建议/采纳/执行+代次切换+superseded 刷新+权限/状态/历史代只读）"
```

---

### Task 12: E2E seed 扩展（backend/app/seed_e2e.py）

> **P1 修复**：seed 在 `backend/app/seed_e2e.py` + `seed_e2e_constants.py`（**非** frontend/e2e 下）。

**Files:**
- Modify: `backend/app/seed_e2e.py`
- Modify: `backend/app/seed_e2e_constants.py`（若需常量）

- [ ] **Step 1: 扩 seed**

`backend/app/seed_e2e.py`：产品线 `DC-DC-100-E2E` 加 iqc_inspections 记录（含 supplier_id）+ mock `ERPConnection`（connector_type='mock'）+ `ERPInventoryBalance`（material_code+unit）+ `ERPShipment`（`erp_raw_data` 显式带 `arrival_status` in_transit/signed + `unit` + customer_code）+ 关联 ShipmentRecord + `Customer.segment='key'`。常量入 `seed_e2e_constants.py`。

- [ ] **Step 2: 跑 seed 验证**

Run: `docker compose exec backend python -m app.seed_e2e`（或 e2e profile 等价命令）
Expected: seed 成功无报错；查询确认 7 表种子数据存在。

- [ ] **Step 3: Commit**

```bash
git add backend/app/seed_e2e.py backend/app/seed_e2e_constants.py
git commit -m "test(d3): E2E seed 扩展（mock ERP+iqc_inspections+ERPShipment arrival_status+Customer.segment）"
```

---

### Task 13: E2E 主流程 spec

**Files:**
- Create: `frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts`

- [ ] **Step 1: 写主流程 spec（失败 — red）**

`frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts`：AI spec，无凭证 skip 整测（`_guards/ai-credentials.guard` 模式）。主流程 9 步（spec §11 行 619-628）：
```ts
test('D3 containment main flow', async ({ page }) => {
  // 1. engineer D2→D3
  // 2. import → 200 + run + 4 类快照 + report_status='done'
  // 3. 报告 5 项齐全 + risk_level∈{high,medium,low} + status='done' + 客户名真名
  // 4. advice → 列表非空 + advice_type∈枚举 + source_provenance 非空 + target_batch_refs 存在
  // 5. 采纳 1 条 → adoptions 回读 adopted
  // 6. 记录 execution → executions 回读
  // 7. D3→D4 推进成功
  // 8. 审计回读：D3_DATA_IMPORTED + D3_REPORT_GENERATED + D3_AI_ADVICE_GENERATED + D3_ADVICE_ADOPTED + D3_EXECUTION_RECORDED + TRANSITION
  // 9. viewer 只读
})
```

- [ ] **Step 2: 运行主流程（凭证可用时），确认通过**

Run: `make e2e TEST_ARGS="--grep d3-containment"`
Expected: PASS（凭证可用）；无凭证 skip。

- [ ] **Step 3: Commit**

```bash
git add frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts
git commit -m "test(d3): E2E 主流程 spec（import→报告→建议→采纳→执行→D3→D4+审计回读）"
```

---

### Task 14: E2E 反向/边界断言（分组，每组独立 test）

> **P1 修复**：二十多个场景拆成多个 `test()`，不堆一个步骤。

**Files:**
- Modify: `frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts`

- [ ] **Step 1: 加反向/边界 test（按组）**

`capa-story-d3-containment.spec.ts` 加多个独立 test（spec §11 行 630-656 分组）：
```ts
test('gate rejections', async ({ page }) => { /* 未导入/缺类/无报告/无 execution→拒 */ })
test('manual execution advance', async ({ page }) => { /* 纯人工推进 + 从未生成 advice 纯 manual */ })
test('generation regeneration', async ({ page }) => { /* 重复 import 新 run + 报告重生成旧 execution 不满足 + advice 重生成旧 advice 不可采纳 + 先执行旧建议→重生成→闸口失败 + manual 不受影响 */ })
test('advice failure paths', async ({ page }) => { /* LLM 失败 200+旧 current 保持 + 全过滤 failed */ })
test('concurrency and stale', async ({ page }) => { /* 并发报告重试 partial UQ 防双 current + 双 import 交错 superseded + import blocked 快照仍提交 + stale worker 复活 CAS 行数 0 丢弃 */ })
test('execution lifecycle', async ({ page }) => { /* execution 降级闸口不再满足 + adopt→reject+text 修订 D3_ADVICE_DECISION_CHANGED */ })
test('provenance and risk floor', async ({ page }) => { /* 无 batch provenance 从 IQC/report-level + alternative record_key 非空 + unknown arrival+受影响客户→risk_level=high */ })
test('cross-tenant and scheme', async ({ page }) => { /* 跨 CAPA/跨工厂 advice_id 拒绝 + evidence_refs javascript:/file:→422 */ })
test('snapshot determinism', async ({ page }) => { /* 快照后改源表报告用旧值 */ })
test('no creds blocked', async ({ page }) => { /* /d3/report + /d3/advice 422 blocked + import 仍 200+blocked */ })
```

- [ ] **Step 2: 运行 E2E 全量，确认通过**

Run: `make e2e TEST_ARGS="--grep d3-containment"`
Expected: PASS（凭证可用时全部 test 绿；无凭证 skip-with-warning）

- [ ] **Step 3: Commit**

```bash
git add frontend/e2e/specs/m1-core/capa-story-d3-containment.spec.ts
git commit -m "test(d3): E2E 反向/边界断言分组（闸口/代次/stale/脱敏/跨工厂/scheme/快照确定性/无凭证 全覆盖）"
```

---

### Task 15: 文档同步 + 最终 make check

**Files:**
- Modify: `docs/user-stories/US-E2E-01-capa-8d-closed-loop/US-E2E-01.1-d3-containment-ai.md`（标"实现态"）
- Modify: `PROGRESS.md` / `docs/ROADMAP.md` / `docs/permissions.md`

- [ ] **Step 1: 同步文档**

故事标"实现态 v4（实现于 <date>）"；PROGRESS.md 勾选 01.1 完成 + refresh next steps；ROADMAP 更新；permissions.md 若新增端点权限点则更新。

- [ ] **Step 2: make check + E2E 全量**

Run: `make check && make e2e`
Expected: PASS（backend tests + frontend tsc + build + e2e 全绿）

- [ ] **Step 3: Commit**

```bash
git add docs/user-stories/US-E2E-01-capa-8d-closed-loop/US-E2E-01.1-d3-containment-ai.md PROGRESS.md docs/ROADMAP.md docs/permissions.md
git commit -m "docs(d3): 文档同步（故事实现态+PROGRESS+ROADMAP+permissions）"
```

---

## Self-Review

**1. Spec coverage:**
- §3.1–§3.7 7 表 → Task 1 ✓
- §4.1 导入 5 步 + 子源 → Task 2 ✓
- §4.2 报告三阶段 + 确定性 + risk floor → Task 3+4 ✓
- §4.3 建议三阶段 + accepted_advice + provenance → Task 7 ✓
- §4.4 采纳/拒绝 → Task 9 ✓
- §4.5 执行记录 + PATCH → Task 10 ✓
- §5 闸口 → Task 5 ✓
- §6 LLM 契约 → Task 4+7 ✓
- §7 **12 端点** → Task 6(import/runs/snapshots/report 5)+Task 8(advice 2)+Task 9(decision/adoptions 2)+Task 10(execution 3)=12 ✓
- §8 审计事件 → 各服务任务内 ✓
- §10 前端 + 挂载 → Task 11 ✓
- §11 E2E → Task 12(seed)+13(主流程)+14(反向) ✓
- §12 测试矩阵各场景 → 分散到各任务测试 ✓
- §13 文档同步 → Task 15 ✓

**2. Placeholder scan:** 无 TBD/TODO/pending——Task 2 导入服务明确不接通事务 B（只事务 A），Task 4 报告服务才接通事务 B 并返回完整 report_status（done/failed/blocked/superseded），Task 6 import API 在 Task 4 之后开放故无 pending 态。已逐一确认无"add appropriate"/"similar to Task N"/"implement later"。

**3. Type consistency:** 模型类名（Task 1）→ 服务函数名 `import_containment_data`/`generate_impact_report`/`generate_advice`/`adopt_advice`/`record_execution`/`update_execution`/`_d3_to_d4_gate`（Task 2-10）→ API 端点（Task 6/8/9/10）→ 前端 api 函数名（Task 11）跨任务一致，均对照 spec。端点数 12（Task 6 五个 + Task 8 两个 + Task 9 两个 + Task 10 三个）与 spec §7 一致。

**4. TDD 粒度:** 每个任务均为「写失败测试 → 运行确认失败（含预期错误信息）→ 最小实现 → 运行确认通过 → git add 精确文件 → commit」五步。E2E 反向场景拆成 10 个独立 test（Task 14），非堆一步。每个 commit 步骤含精确 git add。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-10-us-e2e-01.1-d3-containment.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?