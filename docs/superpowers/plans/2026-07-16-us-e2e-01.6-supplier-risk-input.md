# US-E2E-01.6 8D → 供应商风险评级输入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让涉及供应商的来料不良 8D 推进至 D7_COMPLETED 时，自动把 severity/disposition/repeat 作为风险评级输入落库（outbox 事实表），由可靠 worker 消费触发 R11 评级，评级快照回显 8D 侧并写审计，人工可确认 repeat。

**Architecture:** Outbox 模式——advance_capa 事务内写 `supplier_risk_capa_inputs`(pending) 不调用规则引擎；独立 worker（`SKIP LOCKED` + `claim_token` + retry）消费 pending → 调 `evaluate_supplier_risk_in_tx`（不 commit）→ 回填评估快照 + 写 SENT 审计；新增 R11 规则消费 capa_incidents；confirm-repeat 端点 409 门控 + 单事务重算。

**Tech Stack:** Python 3.11 / FastAPI / SQLAlchemy 2.0 async / Alembic / PostgreSQL 15 (JSONB, SKIP LOCKED) / React 18 + TS / Playwright E2E

**Spec:** `docs/superpowers/specs/2026-07-16-us-e2e-01.6-supplier-risk-input-design.md`（v6 定稿）

## Global Constraints

- **状态字面量以 `backend/app/state_machines/eightd_state.py` 为准**（D7_PREVENTION / D7_COMPLETED / D8_CLOSURE / ARCHIVED）。
- **Alembic 链式挂接**：实现每个迁移任务时先 `cd backend && alembic heads`，把新 revision 的 `down_revision` 挂到当前 head（01.5 可能已推进 head）。**禁止**把 parent 已有 revision 文件拷进本分支（01.4 教训）。
- **JSONB 序列化**：`AuditLog.changed_fields` 内所有 UUID 一律 `str(...)`（对齐 01.5 / `scar_service`）。
- **AI_REQUIRED=false**：消费失败走 error/retry，不阻断 8D 推进。
- **severity 取值**：系统中既有中文值（致命/严重/一般/轻微）也有默认 `"general"`；R11 须兼容两种，映射时同时识别中文与 `"general"`。
- **后端测试命令**：`cd backend && SECRET_KEY=test-secret-key pytest tests/<path> -x --tb=short`。
- **前端测试命令**：`cd frontend && npx vitest run <path>` / `npm run build`（tsc --noEmit + vite build）。
- **每任务一 commit**，commit subject 用 `feat(01.6):` / `fix(01.6):` / `test(01.6):` / `docs(01.6):` 前缀。

---

## File Structure

**新建文件：**
- `backend/app/models/supplier_risk_capa_input.py` — `SupplierRiskCapaInput` ORM 模型（输入事实表 + outbox 状态）
- `backend/app/services/supplier_risk/exceptions.py` — `SupplierRiskConfigurationError`
- `backend/app/services/supplier_risk/risk_input_worker.py` — 可靠 outbox worker（claim/recover/process）
- `backend/alembic/versions/<new>_supplier_risk_capa_inputs.py` — 建表 + capa_eightd.supplier_id + AlertResponse rule_results 规整
- `backend/alembic/versions/<new>_seed_r11_config.py` — R11 默认配置 INSERT…SELECT no-op
- `backend/tests/test_supplier_risk_capa_input.py` — 模型/服务单测
- `backend/tests/test_risk_input_worker.py` — worker 并发/所有权/重试单测
- `backend/tests/test_r11_rule.py` — R11 规则单测
- `backend/tests/migrations/test_supplier_risk_capa_inputs_migration.py` — 空库迁移测试
- `frontend/src/components/capa/SupplierRiskInputCard.tsx` — 8D 详情供应商风险输入卡片
- `frontend/e2e/specs/m1-core/capa-story-supplier-risk-input.spec.ts` — E2E

**修改文件：**
- `backend/app/models/capa.py` — 加 `supplier_id` 列
- `backend/app/models/supplier_risk.py` — （仅文档说明，模型本身不改）
- `backend/app/schemas/capa.py` — `CAPACreate/Update/Response` 加 `supplier_id`
- `backend/app/schemas/supplier_risk.py` — `AlertResponse.rule_results: list[dict]`
- `backend/app/services/capa_service.py` — `advance_capa` D7_COMPLETED 触发写 input；`update_capa` supplier_id 生命周期
- `backend/app/services/supplier_risk/service.py` — 抽 `_in_tx`；加 `_gather_capa_inputs`/`_batch_gather_capa_inputs`；`_upsert_alert` force_update
- `backend/app/services/supplier_risk/rule_engine.py` — R11 规则 + 注册 + capa_incidents
- `backend/app/services/supplier_risk/config.py` — R01–R11
- `backend/app/api/capa.py` — `confirm-repeat` 端点；CAPAResponse 投影组装
- `backend/app/main.py` — `_risk_input_outbox_loop` startup
- `backend/app/seed.py` / `backend/app/seed_e2e.py` — R11 默认配置 + factory_id 修复
- `frontend/src/types/index.ts` / `frontend/src/api/capa.ts` — 类型 + confirmRepeat
- `frontend/src/locales/{zh-CN,en-US}/capa.json` — i18n

---

## Task 1: 数据模型 — `SupplierRiskCapaInput` + `capa_eightd.supplier_id`

**Files:**
- Create: `backend/app/models/supplier_risk_capa_input.py`
- Modify: `backend/app/models/capa.py`（加 `supplier_id` 列，在 `created_by` 之后）
- Modify: `backend/app/models/__init__.py`（导出新模型，若存在）

**Interfaces:**
- Produces: `SupplierRiskCapaInput` ORM 类，表 `supplier_risk_capa_inputs`，字段见 spec §4.2（含 `claim_token`、`evaluated_*` 快照、`created_by`）。`capa_eightd.supplier_id` 列。

- [ ] **Step 1: 写 `SupplierRiskCapaInput` 模型**

```python
# backend/app/models/supplier_risk_capa_input.py
import uuid
from datetime import datetime
from typing import Optional

from sqlalchemy import Boolean, DateTime, Float, ForeignKey, Index, Integer, String, Text, func
from sqlalchemy.dialects.postgresql import JSONB, UUID
from sqlalchemy.orm import Mapped, mapped_column

from app.database import Base


class SupplierRiskCapaInput(Base):
    __tablename__ = "supplier_risk_capa_inputs"

    input_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    capa_id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), ForeignKey("capa_eightd.report_id", ondelete="CASCADE"), unique=True, nullable=False
    )
    supplier_id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), ForeignKey("suppliers.supplier_id", ondelete="RESTRICT"), nullable=False
    )
    factory_id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False
    )
    product_line_code: Mapped[Optional[str]] = mapped_column(String(20), nullable=True)
    created_by: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("users.user_id"), nullable=False)

    severity: Mapped[str] = mapped_column(String(20), nullable=False)
    disposition: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    repeat_suggested: Mapped[Optional[bool]] = mapped_column(Boolean, nullable=True)
    repeat_detection_status: Mapped[str] = mapped_column(String(20), nullable=False)  # matched|not_matched|unavailable
    repeat_confirmed: Mapped[Optional[bool]] = mapped_column(Boolean, nullable=True)
    matched_capa_nos: Mapped[list] = mapped_column(JSONB, nullable=False, server_default=func.text("'[]'::jsonb"))

    evaluated_risk_level: Mapped[Optional[str]] = mapped_column(String(10), nullable=True)
    evaluated_risk_score: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
    evaluated_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)

    status: Mapped[str] = mapped_column(String(20), nullable=False, default="pending")  # pending|processing|processed|error
    linked_alert_id: Mapped[Optional[uuid.UUID]] = mapped_column(
        UUID(as_uuid=True), ForeignKey("supplier_risk_alerts.alert_id", ondelete="SET NULL"), nullable=True
    )
    attempt_count: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    max_attempts: Mapped[int] = mapped_column(Integer, nullable=False, default=5)
    last_error: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    next_retry_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)
    locked_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)
    claim_token: Mapped[Optional[uuid.UUID]] = mapped_column(UUID(as_uuid=True), nullable=True)

    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), nullable=False
    )

    __table_args__ = (
        Index("ix_risk_input_status_retry", "status", "next_retry_at"),
    )
```

- [ ] **Step 2: `capa.py` 加 `supplier_id` 列**

在 `backend/app/models/capa.py` 的 `CAPAEightD` 类中，`created_by` 列之后加：

```python
    supplier_id: Mapped[uuid.UUID | None] = mapped_column(
        UUID(as_uuid=True), ForeignKey("suppliers.supplier_id", ondelete="RESTRICT"), nullable=True
    )
```

- [ ] **Step 3: 确保 `models/__init__.py` 导出**

检查 `backend/app/models/__init__.py`，若它显式列模型，加 `from app.models.supplier_risk_capa_input import SupplierRiskCapaInput`。

- [ ] **Step 4: 写迁移测试（空库可建表）**

```python
# backend/tests/migrations/test_supplier_risk_capa_inputs_migration.py
import pytest
from sqlalchemy import create_engine, text
from alembic import command
from alembic.config import Config

# 复用既有 mig_db_url fixture（见 backend/tests/migrations/，01.2 引入）
# 若不存在，参考 01.2 的 mig_db_url + _cfg helper


def test_migration_creates_supplier_risk_capa_inputs(mig_db_url):
    """空库 upgrade head 后 supplier_risk_capa_inputs 表存在 + capa_eightd.supplier_id 列存在。"""
    cfg = _cfg(mig_db_url)
    command.upgrade(cfg, "head")
    engine = create_engine(mig_db_url)
    with engine.connect() as conn:
        # 表存在
        assert conn.execute(text(
            "SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name='supplier_risk_capa_inputs')"
        )).scalar() is True
        # 列存在
        cols = [r[0] for r in conn.execute(text(
            "SELECT column_name FROM information_schema.columns WHERE table_name='capa_eightd' AND column_name='supplier_id'"
        ))]
        assert "supplier_id" in cols
        # 旧 rule_results='{}' 规整（若存在旧数据）
        assert conn.execute(text("SELECT 1")).scalar() == 1
```

> 注：`mig_db_url` / `_cfg` 是 01.2 在 `backend/tests/migrations/conftest.py` 引入的 fixture。若不存在，本任务先在 conftest 补（见 01.2 commit `20260709_capa_cache_stage_runs` 的测试基建）。

- [ ] **Step 5: 写迁移文件**

```bash
cd backend && alembic heads   # 确认当前 head（01.5 可能已推进）
```

新建 `backend/alembic/versions/20260716_supplier_risk_capa_inputs.py`（revision id 用日期+slug，`down_revision` = Step 5 查到的 head）：

```python
"""supplier risk capa inputs + capa supplier_id + rule_results normalize

Revision ID: 20260716_supplier_risk_capa_inputs
Revises: <当前 head>
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = "20260716_supplier_risk_capa_inputs"
down_revision = "<当前 head>"  # 替换为 alembic heads 查到的值
branch_labels = None
depends_on = None


def upgrade() -> None:
    # 1. capa_eightd.supplier_id
    op.add_column("capa_eightd", sa.Column(
        "supplier_id", postgresql.UUID(as_uuid=True),
        sa.ForeignKey("suppliers.supplier_id", ondelete="RESTRICT"), nullable=True
    ))
    # 2. 输入事实表
    op.create_table(
        "supplier_risk_capa_inputs",
        sa.Column("input_id", postgresql.UUID(as_uuid=True), primary_key=True),
        sa.Column("capa_id", postgresql.UUID(as_uuid=True),
                  sa.ForeignKey("capa_eightd.report_id", ondelete="CASCADE"), unique=True, nullable=False),
        sa.Column("supplier_id", postgresql.UUID(as_uuid=True),
                  sa.ForeignKey("suppliers.supplier_id", ondelete="RESTRICT"), nullable=False),
        sa.Column("factory_id", postgresql.UUID(as_uuid=True),
                  sa.ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False),
        sa.Column("product_line_code", sa.String(20), nullable=True),
        sa.Column("created_by", postgresql.UUID(as_uuid=True), sa.ForeignKey("users.user_id"), nullable=False),
        sa.Column("severity", sa.String(20), nullable=False),
        sa.Column("disposition", sa.Text(), nullable=True),
        sa.Column("repeat_suggested", sa.Boolean(), nullable=True),
        sa.Column("repeat_detection_status", sa.String(20), nullable=False),
        sa.Column("repeat_confirmed", sa.Boolean(), nullable=True),
        sa.Column("matched_capa_nos", postgresql.JSONB(astext_type=sa.Text()), nullable=False, server_default=sa.text("'[]'::jsonb")),
        sa.Column("evaluated_risk_level", sa.String(10), nullable=True),
        sa.Column("evaluated_risk_score", sa.Float(), nullable=True),
        sa.Column("evaluated_at", sa.DateTime(timezone=True), nullable=True),
        sa.Column("status", sa.String(20), nullable=False, server_default="pending"),
        sa.Column("linked_alert_id", postgresql.UUID(as_uuid=True),
                  sa.ForeignKey("supplier_risk_alerts.alert_id", ondelete="SET NULL"), nullable=True),
        sa.Column("attempt_count", sa.Integer(), nullable=False, server_default="0"),
        sa.Column("max_attempts", sa.Integer(), nullable=False, server_default="5"),
        sa.Column("last_error", sa.Text(), nullable=True),
        sa.Column("next_retry_at", sa.DateTime(timezone=True), nullable=True),
        sa.Column("locked_at", sa.DateTime(timezone=True), nullable=True),
        sa.Column("claim_token", postgresql.UUID(as_uuid=True), nullable=True),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
        sa.Column("updated_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
    )
    op.create_index("ix_risk_input_status_retry", "supplier_risk_capa_inputs", ["status", "next_retry_at"])
    # 3. 旧 rule_results='{}' 规整为 '[]'
    op.execute("UPDATE supplier_risk_alerts SET rule_results='[]'::jsonb WHERE rule_results='{}'::jsonb OR rule_results IS NULL")


def downgrade() -> None:
    op.drop_index("ix_risk_input_status_retry", table_name="supplier_risk_capa_inputs")
    op.drop_table("supplier_risk_capa_inputs")
    op.drop_column("capa_eightd", "supplier_id")
```

- [ ] **Step 6: 运行迁移测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/migrations/test_supplier_risk_capa_inputs_migration.py -x --tb=short`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add backend/app/models/supplier_risk_capa_input.py backend/app/models/capa.py backend/app/models/__init__.py backend/alembic/versions/20260716_supplier_risk_capa_inputs.py backend/tests/migrations/test_supplier_risk_capa_inputs_migration.py
git commit -m "feat(01.6): SupplierRiskCapaInput model + capa_eightd.supplier_id migration"
```

---

## Task 2: CAPA Schema + supplier_id 生命周期约束

**Files:**
- Modify: `backend/app/schemas/capa.py`
- Modify: `backend/app/services/capa_service.py`（`update_capa` 加 supplier_id 同厂校验 + D7_COMPLETED 后禁改）
- Test: `backend/tests/test_capa_supplier_id.py`

**Interfaces:**
- Consumes: Task 1 的 `capa_eightd.supplier_id` 列
- Produces: `CAPACreate.supplier_id`, `CAPAUpdate.supplier_id`, `CAPAResponse.supplier_id`；`update_capa` 生命周期校验

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_capa_supplier_id.py
import uuid
import pytest
from app.schemas.capa import CAPACreate, CAPAUpdate, CAPAResponse


def test_capa_create_accepts_supplier_id():
    sid = uuid.uuid4()
    c = CAPACreate(title="t", document_no="8D-T-001", supplier_id=sid)
    assert c.supplier_id == sid


def test_capa_create_supplier_id_optional():
    c = CAPACreate(title="t", document_no="8D-T-002")
    assert c.supplier_id is None


def test_capa_response_includes_supplier_id():
    # 验证 from_attributes 能读 supplier_id（用 dict 模拟）
    r = CAPAResponse.model_validate({
        "report_id": uuid.uuid4(), "document_no": "8D-T-003", "title": "t",
        "product_line_code": "DC-DC-100", "status": "D1_TEAM", "severity": "general",
        "created_at": "2026-01-01T00:00:00", "updated_at": "2026-01-01T00:00:00",
        "supplier_id": str(uuid.uuid4()),
    })
    assert r.supplier_id is not None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa_supplier_id.py -x --tb=short`
Expected: FAIL（`supplier_id` 字段不存在）

- [ ] **Step 3: 修改 Schema**

`backend/app/schemas/capa.py`：
- `CAPACreate` 加 `supplier_id: uuid.UUID | None = None`
- `CAPAUpdate` 加 `supplier_id: uuid.UUID | None = None`
- `CAPAResponse` 在 `d4_retry_count` 后加 `supplier_id: uuid.UUID | None = None`

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa_supplier_id.py -x --tb=short`
Expected: PASS

- [ ] **Step 5: 写 `update_capa` 生命周期约束测试**

在 `backend/tests/test_capa_supplier_id.py` 追加（用现有 factory_id fixture，参考既有 capa 测试）：

```python
@pytest.mark.asyncio
async def test_update_capa_rejects_cross_factory_supplier(db_session, capa_factory, supplier_factory):
    """supplier 与 capa 不同厂 → 400/ValueError。"""
    capa = await capa_factory(status="D4_ROOT_CAUSE", factory_id=uuid.uuid4())
    other_factory_supplier = await supplier_factory(factory_id=uuid.uuid4())  # 异厂
    from app.services.capa_service import update_capa
    from app.schemas.capa import CAPAUpdate
    with pytest.raises(ValueError):
        await update_capa(db_session, capa, CAPAUpdate(supplier_id=other_factory_supplier.supplier_id), user_id=uuid.uuid4())


@pytest.mark.asyncio
async def test_update_capa_locks_supplier_id_after_d7_completed(db_session, capa_factory, supplier_factory):
    """D7_COMPLETED 后改 supplier_id → ValueError。"""
    same_factory = uuid.uuid4()
    capa = await capa_factory(status="D7_COMPLETED", factory_id=same_factory)
    sup = await supplier_factory(factory_id=same_factory)
    from app.services.capa_service import update_capa
    from app.schemas.capa import CAPAUpdate
    with pytest.raises(ValueError):
        await update_capa(db_session, capa, CAPAUpdate(supplier_id=sup.supplier_id), user_id=uuid.uuid4())
```

> 注：`capa_factory` / `supplier_factory` / `db_session` 若不存在，参考 `backend/tests/conftest.py` 的 factory_id fixture 模式补（01.1 已有 D3 测试 fixture 可复用）。

- [ ] **Step 6: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa_supplier_id.py -x --tb=short`
Expected: FAIL（`update_capa` 未校验）

- [ ] **Step 7: 实现 `update_capa` 约束**

在 `backend/app/services/capa_service.py` 的 `update_capa` 中（找到处理 `req.product_line_code` 等 mutable 字段处），加 supplier_id 处理：

```python
    # supplier_id 生命周期约束（01.6）
    if req.supplier_id is not None:
        from app.models.supplier import Supplier
        sup = await db.get(Supplier, req.supplier_id)
        if sup is None:
            raise ValueError("供应商不存在")
        if sup.factory_id != capa.factory_id:
            raise ValueError("供应商与 8D 不属于同一工厂")
        # D7_COMPLETED 及之后禁止改 supplier_id（D 步锁定）
        from app.state_machines.eightd_state import EightDState
        locked_states = {
            EightDState.D7_COMPLETED.value, EightDState.D8_GATE_PENDING.value,
            EightDState.D8_APPROVAL_PENDING.value, EightDState.D8_CLOSURE.value,
            EightDState.ARCHIVED.value,
        }
        if capa.status in locked_states and capa.supplier_id != req.supplier_id:
            raise ValueError("8D 已推进至 D7+，不可修改供应商")
        capa.supplier_id = req.supplier_id
```

- [ ] **Step 8: 运行测试确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa_supplier_id.py -x --tb=short`
Expected: PASS

- [ ] **Step 9: 跑既有 capa 测试无回归**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa*.py -x --tb=short -q`
Expected: 全 PASS（无回归）

- [ ] **Step 10: Commit**

```bash
git add backend/app/schemas/capa.py backend/app/services/capa_service.py backend/tests/test_capa_supplier_id.py
git commit -m "feat(01.6): CAPA supplier_id schema + lifecycle constraint"
```

---

## Task 3: R11 规则 + 注册 + config 范围

**Files:**
- Modify: `backend/app/services/supplier_risk/rule_engine.py`（R11 规则函数 + 注册 + `SupplierRiskInput.capa_incidents`）
- Modify: `backend/app/services/supplier_risk/config.py`（R01–R11）
- Test: `backend/tests/test_r11_rule.py`

**Interfaces:**
- Consumes: 无（首批）
- Produces: `SupplierRiskInput.capa_incidents: list[Any]`；`rule_r11_capa_issue(data, thresholds)` → `RuleResult`；R11 在 `RULE_REGISTRY` / `_REGISTRY_MAP`；`get_effective_configs` 含 R11。

- [ ] **Step 1: 写 R11 单测**

```python
# backend/tests/test_r11_rule.py
import pytest
from app.services.supplier_risk.rule_engine import SupplierRiskInput, run_all_rules, rule_r11_capa_issue


def _incident(severity="严重", repeat_confirmed=None, repeat_suggested=True, disposition="退货"):
    class _I:
        pass
    i = _I()
    i.severity = severity
    i.disposition = disposition
    i.repeat_confirmed = repeat_confirmed
    i.repeat_suggested = repeat_suggested
    i.repeat_detection_status = "matched"
    i.matched_capa_nos = ["8D-2025-001"]
    return i


class _Supplier:
    supplier_id = "x"
    factory_id = "y"


def test_r11_no_incidents_not_triggered():
    data = SupplierRiskInput(supplier=_Supplier(), capa_incidents=[])
    res = rule_r11_capa_issue(data, {})
    assert res.rule_id == "R11"
    assert res.triggered is False
    assert res.score == 0


def test_r11_severe_repeat_triggered_high_score():
    data = SupplierRiskInput(supplier=_Supplier(), capa_incidents=[_incident(severity="严重", repeat_confirmed=True)])
    res = rule_r11_capa_issue(data, {})
    assert res.triggered is True
    assert res.score > 0
    assert res.critical is True
    # disposition 入 detail 不计分但可追溯
    assert "退货" in res.detail


def test_r11_severity_mapping_chinese_and_general():
    # 兼容中文与 "general"
    for sev, expect_gt in [("致命", True), ("严重", True), ("一般", False), ("轻微", False), ("general", False)]:
        data = SupplierRiskInput(supplier=_Supplier(), capa_incidents=[_incident(severity=sev, repeat_confirmed=False)])
        res = rule_r11_capa_issue(data, {})
        assert res.triggered is True
        if expect_gt:
            assert res.score > 20
        else:
            assert res.score <= 20


def test_r11_repeat_uses_confirmed_over_suggested_and_marks_provisional():
    # confirmed=None 时用 suggested + 标 provisional
    data = SupplierRiskInput(supplier=_Supplier(), capa_incidents=[_incident(repeat_confirmed=None, repeat_suggested=True)])
    res = rule_r11_capa_issue(data, {})
    assert "provisional" in res.detail.lower() or res.detail  # provisional 标记入 detail


def test_r11_registered_and_runs_via_run_all_rules():
    class _Cfg:
        rule_id = "R11"
        enabled = True
        thresholds = {}
        weight = 1.0
        category = "quality"
    data = SupplierRiskInput(supplier=_Supplier(), capa_incidents=[_incident()])
    results, failed = run_all_rules(data, [_Cfg()])
    r11 = next((r for r in results if r.rule_id == "R11"), None)
    assert r11 is not None
    assert "R11" not in failed
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_r11_rule.py -x --tb=short`
Expected: FAIL（`rule_r11_capa_issue` 不存在 / `capa_incidents` 不存在）

- [ ] **Step 3: 扩展 `SupplierRiskInput` + 实现 R11**

`backend/app/services/supplier_risk/rule_engine.py`：

`SupplierRiskInput` 加字段：
```python
@dataclass
class SupplierRiskInput:
    supplier: Any
    inspections: list[Any] = field(default_factory=list)
    scars: list[Any] = field(default_factory=list)
    evaluations: list[Any] = field(default_factory=list)
    certifications: list[Any] = field(default_factory=list)
    capa_incidents: list[Any] = field(default_factory=list)  # 01.6
```

在 R10 之后、Registry 之前加 R11 规则：

```python
# ── R11  CAPA 来料不良影响（severity + repeat 计分；disposition 仅追溯）──

_SEVERITY_RANK = {"致命": 4, "严重": 3, "一般": 2, "轻微": 1, "general": 2, "critical": 4, "major": 3, "minor": 1}


def rule_r11_capa_issue(data: SupplierRiskInput, thresholds: dict) -> RuleResult:
    base_score: int = thresholds.get("base_score", 10)
    severe_bonus: int = thresholds.get("severe_bonus", 10)
    repeat_bonus: int = thresholds.get("repeat_bonus", 10)

    incidents = data.capa_incidents
    if not incidents:
        return RuleResult(rule_id="R11", triggered=False, score=0,
                          detail="无 CAPA 来料不良输入", category="quality", critical=False)

    # 取最高严重度 incident（致命>严重>一般>轻微；同级 created_at DESC, input_id ASC 由调用方排序保证）
    def _rank(i):
        return _SEVERITY_RANK.get(getattr(i, "severity", ""), 2)
    top = max(incidents, key=_rank)
    rank = _rank(top)

    repeat_confirmed = getattr(top, "repeat_confirmed", None)
    repeat_suggested = getattr(top, "repeat_suggested", None)
    if repeat_confirmed is not None:
        repeat = repeat_confirmed
        provisional = False
    elif repeat_suggested is not None:
        repeat = repeat_suggested
        provisional = True
    else:
        repeat = False
        provisional = False

    score = float(base_score)
    critical = rank >= 3  # 致命/严重
    if critical:
        score += severe_bonus
    if repeat:
        score += repeat_bonus
    score = min(score, 100.0)

    disp = getattr(top, "disposition", "") or ""
    matched = getattr(top, "matched_capa_nos", []) or []
    detail_parts = [f"严重度: {top.severity}", f"处置: {disp}"]
    if matched:
        detail_parts.append(f"关联历史: {', '.join(matched)}")
    detail_parts.append(f"重复: {'是' if repeat else '否'}" + ("（待确认，provisional）" if provisional else ""))
    return RuleResult(rule_id="R11", triggered=True, score=round(score, 2),
                      detail="; ".join(detail_parts), category="quality", critical=critical)
```

注册（`RULE_REGISTRY` 末尾加）：
```python
    ("R11", rule_r11_capa_issue, "quality", 1, False),
```

- [ ] **Step 4: 扩展 `config.py` 范围**

`backend/app/services/supplier_risk/config.py` 的 `get_effective_configs` 与 `get_effective_configs_batch`：
- `range(1, 11)` → `range(1, 12)`（R01–R11）

- [ ] **Step 5: 运行 R11 单测**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_r11_rule.py -x --tb=short`
Expected: PASS

- [ ] **Step 6: 跑既有风险规则测试无回归**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_supplier_risk*.py -x --tb=short -q`
Expected: 全 PASS

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/supplier_risk/rule_engine.py backend/app/services/supplier_risk/config.py backend/tests/test_r11_rule.py
git commit -m "feat(01.6): R11 capa-issue rule + config R01-R11"
```

---

## Task 4: 评估引擎 — `_in_tx` 抽离 + `_gather_capa_inputs` + 强制契约 + `_upsert_alert` force_update

**Files:**
- Modify: `backend/app/services/supplier_risk/service.py`
- Create: `backend/app/services/supplier_risk/exceptions.py`
- Test: `backend/tests/test_supplier_risk_eval_in_tx.py`

**Interfaces:**
- Consumes: Task 3 的 `SupplierRiskInput.capa_incidents` / R11
- Produces: `SupplierRiskConfigurationError`；`evaluate_supplier_risk_in_tx(db, supplier_id, product_line_code, *, force_update, trigger_input)` → `(alert, risk_score)` 不 commit；`_gather_capa_inputs(db, supplier_id, product_line_code)`；`_batch_gather_capa_inputs(db, supplier_ids, product_line_code)`；`_upsert_alert(..., force_update=False)`。

- [ ] **Step 1: 写异常类**

```python
# backend/app/services/supplier_risk/exceptions.py
class SupplierRiskConfigurationError(Exception):
    """风险规则配置错误（如 R11 缺失/disabled/未注册/执行异常）。"""
```

- [ ] **Step 2: 写 `_in_tx` + 强制契约 + gather 测试**

```python
# backend/tests/test_supplier_risk_eval_in_tx.py
import uuid, pytest
from datetime import datetime, timedelta, timezone
from app.services.supplier_risk.service import evaluate_supplier_risk_in_tx, _gather_capa_inputs
from app.services.supplier_risk.exceptions import SupplierRiskConfigurationError


@pytest.mark.asyncio
async def test_in_tx_does_not_commit(db_session, supplier_with_r11_config):
    """_in_tx 不 commit（调用方负责）。"""
    supplier, pl = supplier_with_r11_config
    alert, score = await evaluate_supplier_risk_in_tx(db_session, supplier.supplier_id, pl)
    # 不 commit 时，新 alert 未持久化（session 内可见但 db 层无）——用 fresh session 验证
    from app.models.supplier_risk import SupplierRiskAlert
    from sqlalchemy import select
    cnt = (await db_session.execute(select(SupplierRiskAlert).where(SupplierRiskAlert.supplier_id == supplier.supplier_id))).scalars().all()
    # 未 commit 前在 session 内可见（identity map），但这里仅验证不抛 + 返回类型
    assert alert is not None or score is not None


@pytest.mark.asyncio
async def test_in_tx_raises_when_r11_missing_for_trigger(db_session, supplier_without_r11_config, capa_input_factory):
    """trigger_input 评估时缺 R11 → raise（不假成功）。"""
    supplier, pl = supplier_without_r11_config
    inp = await capa_input_factory(supplier_id=supplier.supplier_id, product_line_code=pl)
    with pytest.raises(SupplierRiskConfigurationError):
        await evaluate_supplier_risk_in_tx(db_session, supplier.supplier_id, pl, trigger_input=inp)


@pytest.mark.asyncio
async def test_in_tx_raises_when_r11_disabled(db_session, supplier_with_r11_disabled, capa_input_factory):
    """R11 disabled → run_all_rules 静默跳过 → results 无 R11 → raise。"""
    supplier, pl = supplier_with_r11_disabled
    inp = await capa_input_factory(supplier_id=supplier.supplier_id, product_line_code=pl)
    with pytest.raises(SupplierRiskConfigurationError):
        await evaluate_supplier_risk_in_tx(db_session, supplier.supplier_id, pl, trigger_input=inp)


@pytest.mark.asyncio
async def test_in_tx_no_trigger_tolerates_missing_r11(db_session, supplier_without_r11_config):
    """无 trigger_input 的普通评估容忍缺 R11（不 raise）。"""
    supplier, pl = supplier_without_r11_config
    # 不抛异常即可
    await evaluate_supplier_risk_in_tx(db_session, supplier.supplier_id, pl)


@pytest.mark.asyncio
async def test_gather_capa_inputs_filters_window_and_status(db_session, supplier_factory, capa_input_factory):
    """_gather_capa_inputs 读 processed/error + 90 天窗口。"""
    sup = await supplier_factory()
    # processed 在窗口内
    await capa_input_factory(supplier_id=sup.supplier_id, status="processed", created_at=datetime.now(timezone.utc))
    # error 在窗口内
    await capa_input_factory(supplier_id=sup.supplier_id, status="error", created_at=datetime.now(timezone.utc))
    # pending 不应被读（worker 处理中的由 trigger 注入）
    await capa_input_factory(supplier_id=sup.supplier_id, status="pending", created_at=datetime.now(timezone.utc))
    # 超期
    await capa_input_factory(supplier_id=sup.supplier_id, status="processed", created_at=datetime.now(timezone.utc) - timedelta(days=100))
    incidents = await _gather_capa_inputs(db_session, sup.supplier_id, None)
    assert len(incidents) == 2  # processed + error，超期与 pending 排除


@pytest.mark.asyncio
async def test_in_tx_injects_trigger_input_into_incidents(db_session, supplier_with_r11_config, capa_input_factory):
    """trigger_input 被显式注入 incidents（即使 status=processing，gather 读不到也能消费）。"""
    supplier, pl = supplier_with_r11_config
    inp = await capa_input_factory(supplier_id=supplier.supplier_id, product_line_code=pl, status="processing", severity="严重")
    alert, score = await evaluate_supplier_risk_in_tx(db_session, supplier.supplier_id, pl, trigger_input=inp, force_update=True)
    # input 快照回填
    assert inp.evaluated_risk_level is not None
    assert inp.evaluated_at is not None
```

> 注：`supplier_with_r11_config` / `supplier_without_r11_config` / `supplier_with_r11_disabled` / `capa_input_factory` / `supplier_factory` 为本任务新建 fixture（放 `tests/conftest.py` 或本测试文件），需插入 R01+R11 / 仅 R01 / R01+R11(disabled) 配置 + SupplierRiskCapaInput 行。参考既有 supplier_risk 测试 fixture 模式。

- [ ] **Step 3: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_supplier_risk_eval_in_tx.py -x --tb=short`
Expected: FAIL（`evaluate_supplier_risk_in_tx` 不存在）

- [ ] **Step 4: 实现 `_gather_capa_inputs` + `_in_tx` + 强制契约 + `force_update`**

`backend/app/services/supplier_risk/service.py`：

加 import 与 gatherer：
```python
from datetime import datetime, timedelta, timezone
from app.models.supplier_risk_capa_input import SupplierRiskCapaInput
from app.services.supplier_risk.exceptions import SupplierRiskConfigurationError

CAPA_INPUT_WINDOW_DAYS = 90


async def _gather_capa_inputs(db: AsyncSession, supplier_id: uuid.UUID, product_line_code: Optional[str]):
    """读该 supplier 的 processed/error capa inputs，90 天窗口。"""
    cutoff = datetime.now(timezone.utc) - timedelta(days=CAPA_INPUT_WINDOW_DAYS)
    query = select(SupplierRiskCapaInput).where(
        SupplierRiskCapaInput.supplier_id == supplier_id,
        SupplierRiskCapaInput.status.in_(("processed", "error")),
        SupplierRiskCapaInput.created_at >= cutoff,
    )
    if product_line_code:
        query = query.where(SupplierRiskCapaInput.product_line_code == product_line_code)
    query = query.order_by(SupplierRiskCapaInput.created_at.desc(), SupplierRiskCapaInput.input_id.asc())
    result = await db.execute(query)
    return list(result.scalars().all())


async def _batch_gather_capa_inputs(db: AsyncSession, supplier_ids: list[uuid.UUID], product_line_code: Optional[str]):
    """批量版：一次查询按 supplier 分组。"""
    if not supplier_ids:
        return {}
    cutoff = datetime.now(timezone.utc) - timedelta(days=CAPA_INPUT_WINDOW_DAYS)
    query = select(SupplierRiskCapaInput).where(
        SupplierRiskCapaInput.supplier_id.in_(supplier_ids),
        SupplierRiskCapaInput.status.in_(("processed", "error")),
        SupplierRiskCapaInput.created_at >= cutoff,
    )
    if product_line_code:
        query = query.where(SupplierRiskCapaInput.product_line_code == product_line_code)
    result = await db.execute(query)
    by_supplier: dict[uuid.UUID, list] = {}
    for inp in result.scalars().all():
        by_supplier.setdefault(inp.supplier_id, []).append(inp)
    return by_supplier
```

`_upsert_alert` 加 `force_update=False` 参数（签名 + 低风险建 + 非升级覆盖逻辑）：
```python
async def _upsert_alert(db, supplier_id, product_line_code, risk_score, results, failed_ids, *, factory_id=None, force_update=False):
    # ... 原有查 existing 逻辑 ...
    # 低风险无 existing 分支改为：
    if risk_score.risk_level == "low" and not existing:
        if not force_update:
            return None, None
        # force_update=True 时即使 low 也建一条 routine
    # existing 分支：force_update=True 时无论升降级都覆盖
    if existing:
        if force_update or level_order.get(risk_score.risk_level, 0) > level_order.get(existing.risk_level, 0):
            existing.risk_level = risk_score.risk_level
            existing.risk_score = risk_score.risk_score
            existing.quality_score = risk_score.quality_score
            existing.delivery_score = risk_score.delivery_score
            existing.compliance_score = risk_score.compliance_score
            existing.rule_results = rule_results_data
            existing.alert_type = "escalated" if (not force_update) else existing.alert_type
            await db.flush()
            return existing, "escalated" if not force_update else "updated"
        return existing, "unchanged"
    # ... 原 create new 逻辑（force_update=True 时 alert_type 可为 'routine'）...
```

抽 `_in_tx`（从 `evaluate_supplier_risk` 复制核心，不 commit）：
```python
async def evaluate_supplier_risk_in_tx(
    db: AsyncSession,
    supplier_id: uuid.UUID,
    product_line_code: Optional[str] = None,
    *,
    force_update: bool = False,
    trigger_input: Optional[SupplierRiskCapaInput] = None,
):
    """事务内核心：不 commit。消费者与 confirm 共用。"""
    supplier = await db.get(Supplier, supplier_id)
    if not supplier:
        raise ValueError("供应商不存在")

    configs = await get_effective_configs(db, product_line_code, supplier_id)
    if not configs:
        raise ValueError("无有效的风险规则配置")

    inspections = await _gather_inspections(db, supplier_id, product_line_code)
    scars = await _gather_scars(db, supplier_id, product_line_code)
    evaluations = await _gather_evaluations(db, supplier_id)
    certifications = await _gather_certifications(db, supplier_id)
    incidents = await _gather_capa_inputs(db, supplier_id, product_line_code)

    # 显式注入 trigger_input（绕过 status 限制，但已在 gather 的 90 天窗口内时也保留）
    if trigger_input is not None:
        # 替换/追加：若 gather 已含同 input_id 则替换，否则追加
        incidents = [i for i in incidents if i.input_id != trigger_input.input_id]
        # trigger_input 仍须满足 90 天窗口才纳入评分；超期则不注入（spec §5.5 step 2）
        cutoff = datetime.now(timezone.utc) - timedelta(days=CAPA_INPUT_WINDOW_DAYS)
        if trigger_input.created_at >= cutoff:
            incidents.append(trigger_input)

    input_data = SupplierRiskInput(
        supplier=supplier, inspections=inspections, scars=scars,
        evaluations=evaluations, certifications=certifications, capa_incidents=incidents,
    )
    results, failed_ids = run_all_rules(input_data, configs)

    # 强制契约：trigger_input 评估时必须实际产出 R11（spec §5.5 step 6）
    if trigger_input is not None:
        r11 = next((r for r in results if r.rule_id == "R11"), None)
        if r11 is None:
            raise SupplierRiskConfigurationError(
                f"R11 not executed (missing/disabled/failed: r11_in_failed={'R11' in failed_ids})"
            )

    risk_score = calculate_risk_score(results, configs)
    alert, event_type = await _upsert_alert(
        db, supplier_id, product_line_code, risk_score, results, failed_ids,
        factory_id=supplier.factory_id, force_update=force_update,
    )

    # 回填 trigger_input 快照
    if trigger_input is not None and alert is not None:
        trigger_input.evaluated_risk_level = alert.risk_level
        trigger_input.evaluated_risk_score = alert.risk_score
        trigger_input.evaluated_at = datetime.now(timezone.utc)
        trigger_input.linked_alert_id = alert.alert_id
    elif trigger_input is not None:
        # force_update=True 下 alert 可能 low 也建了；若仍 None 则用 risk_score 直接写快照
        trigger_input.evaluated_risk_level = risk_score.risk_level
        trigger_input.evaluated_risk_score = risk_score.risk_score
        trigger_input.evaluated_at = datetime.now(timezone.utc)

    return alert, risk_score
```

改 `evaluate_supplier_risk`（保留 commit 的公开入口）内部调 `_in_tx`：
```python
async def evaluate_supplier_risk(db, supplier_id, product_line_code=None):
    alert, risk_score = await evaluate_supplier_risk_in_tx(db, supplier_id, product_line_code)
    await db.commit()
    if alert:
        await db.refresh(alert)
    # ... 通知逻辑不变 ...
```

- [ ] **Step 5: 批量路径填 capa_incidents**

`evaluate_all_suppliers` 与 `calculate_all_supplier_scores`：
- 在 gather 阶段加 `capa_inputs_by_supplier = await _batch_gather_capa_inputs(db, supplier_ids, product_line_code)`
- `SupplierRiskInput(...)` 加 `capa_incidents=capa_inputs_by_supplier.get(supplier.supplier_id, [])`

- [ ] **Step 6: 运行单测**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_supplier_risk_eval_in_tx.py -x --tb=short`
Expected: PASS

- [ ] **Step 7: 跑既有 supplier_risk 测试无回归**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_supplier_risk*.py tests/test_r11_rule.py -x --tb=short -q`
Expected: 全 PASS

- [ ] **Step 8: Commit**

```bash
git add backend/app/services/supplier_risk/exceptions.py backend/app/services/supplier_risk/service.py backend/tests/test_supplier_risk_eval_in_tx.py
git commit -m "feat(01.6): evaluate_in_tx + capa_inputs gather + R11 contract + force_update"
```

---

## Task 5: advance_capa D7_COMPLETED 触发写 input（outbox 落库）

**Files:**
- Modify: `backend/app/services/capa_service.py`（`advance_capa` 内 D7→D7_COMPLETED 时写 input + QUEUED 审计，commit 前）
- Test: `backend/tests/test_capa_advance_supplier_risk_trigger.py`

**Interfaces:**
- Consumes: Task 1 的 `SupplierRiskCapaInput`；Task 2 的 `capa.supplier_id`
- Produces: advance 到 D7_COMPLETED 时若 `capa.supplier_id` 非空 → 写一条 pending input + `SUPPLIER_RISK_INPUT_QUEUED` 审计（同事务）。

- [ ] **Step 1: 写触发测试**

```python
# backend/tests/test_capa_advance_supplier_risk_trigger.py
import uuid, pytest
from app.services.capa_service import advance_capa
from app.schemas.capa import AdvanceRequest
from app.state_machines.eightd_state import EightDState
from app.models.supplier_risk_capa_input import SupplierRiskCapaInput
from app.models.audit import AuditLog
from sqlalchemy import select


@pytest.mark.asyncio
async def test_advance_to_d7_completed_writes_pending_input(db_session, capa_with_supplier_and_fmea):
    """D7_PREVENTION→D7_COMPLETED 且有 supplier_id → 写 pending input + QUEUED 审计。"""
    capa, sup = capa_with_supplier_and_fmea  # status=D7_PREVENTION, supplier_id 非空, fmea_node_id 非空
    await advance_capa(db_session, capa, uuid.uuid4(), AdvanceRequest(target_state=EightDState.D7_COMPLETED))
    await db_session.commit()

    inp = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.capa_id == capa.report_id))).scalar_one()
    assert inp.status == "pending"
    assert inp.supplier_id == sup.supplier_id
    assert inp.severity == capa.severity
    assert inp.repeat_detection_status in ("matched", "not_matched", "unavailable")
    # 审计
    logs = (await db_session.execute(select(AuditLog).where(AuditLog.record_id == capa.report_id, AuditLog.action == "SUPPLIER_RISK_INPUT_QUEUED"))).scalars().all()
    assert len(logs) == 1
    assert "supplier_id" in logs[0].changed_fields
    assert "disposition" in logs[0].changed_fields


@pytest.mark.asyncio
async def test_advance_skips_when_no_supplier(db_session, capa_factory):
    """capa.supplier_id 为空 → 不写 input。"""
    capa = await capa_factory(status="D7_PREVENTION", supplier_id=None)
    await advance_capa(db_session, capa, uuid.uuid4(), AdvanceRequest(target_state=EightDState.D7_COMPLETED))
    await db_session.commit()
    cnt = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.capa_id == capa.report_id))).scalars().all()
    assert len(cnt) == 0


@pytest.mark.asyncio
async def test_repeat_detection_matched_with_history(db_session, capa_with_supplier_and_fmea, archived_capa_same_fmea_node):
    """同 supplier + 同 fmea_node_id 的归档 8D 存在 → repeat_suggested=True + matched_capa_nos 含历史单号。"""
    capa, sup = capa_with_supplier_and_fmea
    archived = archived_capa_same_fmea_node  # 同 supplier_id, 同 fmea_node_id, status=ARCHIVED
    await advance_capa(db_session, capa, uuid.uuid4(), AdvanceRequest(target_state=EightDState.D7_COMPLETED))
    await db_session.commit()
    inp = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.capa_id == capa.report_id))).scalar_one()
    assert inp.repeat_suggested is True
    assert inp.repeat_detection_status == "matched"
    assert archived.document_no in inp.matched_capa_nos


@pytest.mark.asyncio
async def test_repeat_detection_unavailable_without_fmea(db_session, capa_with_supplier_no_fmea):
    """capa 无 fmea_node_id → repeat_detection_status='unavailable'。"""
    capa, sup = capa_with_supplier_no_fmea
    await advance_capa(db_session, capa, uuid.uuid4(), AdvanceRequest(target_state=EightDState.D7_COMPLETED))
    await db_session.commit()
    inp = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.capa_id == capa.report_id))).scalar_one()
    assert inp.repeat_detection_status == "unavailable"
    assert inp.repeat_suggested is None
    assert inp.matched_capa_nos == []


@pytest.mark.asyncio
async def test_advance_other_transitions_no_input(db_session, capa_with_supplier_and_fmea):
    """非 D7_COMPLETED 的推进不写 input。"""
    capa, sup = capa_with_supplier_and_fmea
    # D1→D2 等不触发
    capa.status = EightDState.D1_TEAM.value
    await advance_capa(db_session, capa, uuid.uuid4())  # 线性 next D2
    await db_session.commit()
    cnt = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.capa_id == capa.report_id))).scalars().all()
    assert len(cnt) == 0
```

> 注：`capa_with_supplier_and_fmea` / `archived_capa_same_fmea_node` / `capa_with_supplier_no_fmea` 为本任务新建 fixture。

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa_advance_supplier_risk_trigger.py -x --tb=short`
Expected: FAIL（无 input 写入）

- [ ] **Step 3: 实现 advance_capa 触发**

在 `backend/app/services/capa_service.py` 的 `advance_capa` 中，找到写 `D8_APPROVED`/`D8_REJECTED` 审计之后、MES outbox 之前（L638 附近），加 D7_COMPLETED 触发逻辑。先在该文件顶部加 repeat 探测 helper：

```python
async def _detect_repeat_capa(db, capa) -> tuple[bool | None, str, list[str]]:
    """返回 (repeat_suggested, detection_status, matched_capa_nos)。"""
    from app.models.capa import CAPAEightD
    from app.state_machines.eightd_state import EightDState
    if not capa.fmea_node_id:
        return None, "unavailable", []
    rows = (await db.execute(
        select(CAPAEightD).where(
            CAPAEightD.supplier_id == capa.supplier_id,
            CAPAEightD.product_line_code == capa.product_line_code,
            CAPAEightD.fmea_node_id == capa.fmea_node_id,
            CAPAEightD.status.in_((EightDState.D8_CLOSURE.value, EightDState.ARCHIVED.value)),
            CAPAEightD.report_id != capa.report_id,
        )
    )).scalars().all()
    if rows:
        return True, "matched", [r.document_no for r in rows]
    return False, "not_matched", []
```

在 advance_capa 的 `await db.commit()`（L640）之前插入：

```python
    # 01.6: D7_PREVENTION→D7_COMPLETED 时写供应商风险输入 outbox（pending）
    if old_status == EightDState.D7_PREVENTION.value and capa.status == EightDState.D7_COMPLETED.value and capa.supplier_id is not None:
        from app.models.supplier_risk_capa_input import SupplierRiskCapaInput
        repeat_suggested, det_status, matched_nos = await _detect_repeat_capa(db, capa)
        risk_input = SupplierRiskCapaInput(
            capa_id=capa.report_id,
            supplier_id=capa.supplier_id,
            factory_id=capa.factory_id,
            product_line_code=capa.product_line_code,
            created_by=user_id,
            severity=capa.severity,
            disposition=capa.d7_prevention,
            repeat_suggested=repeat_suggested,
            repeat_detection_status=det_status,
            repeat_confirmed=None,
            matched_capa_nos=matched_nos,
            status="pending",
            attempt_count=0,
            max_attempts=5,
        )
        db.add(risk_input)
        db.add(AuditLog(
            table_name="capa_eightd",
            record_id=capa.report_id,
            action="SUPPLIER_RISK_INPUT_QUEUED",
            operated_by=user_id,
            factory_id=capa.factory_id,
            changed_fields={
                "capa_id": str(capa.report_id),
                "supplier_id": str(capa.supplier_id),
                "severity": capa.severity,
                "disposition": capa.d7_prevention or "",
                "repeat_suggested": repeat_suggested,
                "repeat_detection_status": det_status,
                "matched_capa_nos": matched_nos,
            },
        ))
```

- [ ] **Step 4: 运行测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa_advance_supplier_risk_trigger.py -x --tb=short`
Expected: PASS

- [ ] **Step 5: 跑 capa 全量无回归**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa*.py -x --tb=short -q`
Expected: 全 PASS

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/capa_service.py backend/tests/test_capa_advance_supplier_risk_trigger.py
git commit -m "feat(01.6): advance_capa D7_COMPLETED writes supplier risk input outbox"
```

---

## Task 6: 可靠 Outbox Worker（claim_token + retry + recover_stale）

**Files:**
- Create: `backend/app/services/supplier_risk/risk_input_worker.py`
- Modify: `backend/app/main.py`（startup loop）
- Test: `backend/tests/test_risk_input_worker.py`

**Interfaces:**
- Consumes: Task 4 的 `evaluate_supplier_risk_in_tx`
- Produces: `recover_stale_inputs(db)`, `claim_batch(db, batch_size)`, `process_one(db, claimed)`；main.py `_risk_input_outbox_loop`。

- [ ] **Step 1: 写 worker 单测**

```python
# backend/tests/test_risk_input_worker.py
import uuid, pytest
from datetime import datetime, timedelta, timezone
from app.services.supplier_risk.risk_input_worker import (
    recover_stale_inputs, claim_batch, process_one,
)
from app.models.supplier_risk_capa_input import SupplierRiskCapaInput
from app.models.audit import AuditLog
from sqlalchemy import select


@pytest.mark.asyncio
async def test_claim_batch_locks_pending(db_session, pending_input_factory):
    inp = await pending_input_factory()
    claimed = await claim_batch(db_session, 10)
    await db_session.commit()
    assert len(claimed) == 1
    assert claimed[0]["input_id"] == str(inp.input_id)
    assert claimed[0]["status"] == "processing"
    assert claimed[0]["claim_token"] is not None


@pytest.mark.asyncio
async def test_process_one_uses_claim_token_and_processes(db_session, pending_input_with_r11):
    """process_one 重锁 + claim_token 校验 + 成功 processed + 写 SENT。"""
    inp, supplier = pending_input_with_r11
    claimed = await claim_batch(db_session, 10)
    await db_session.commit()
    await process_one(db_session, claimed[0])
    await db_session.commit()
    fresh = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.input_id == inp.input_id))).scalar_one()
    assert fresh.status == "processed"
    assert fresh.claim_token is None
    assert fresh.evaluated_risk_level is not None
    assert fresh.evaluated_at is not None
    sent = (await db_session.execute(select(AuditLog).where(AuditLog.record_id == inp.capa_id, AuditLog.action == "SUPPLIER_RISK_INPUT_SENT"))).scalars().all()
    assert len(sent) == 1
    cf = sent[0].changed_fields
    assert cf["risk_level"] == fresh.evaluated_risk_level
    assert "disposition" in cf


@pytest.mark.asyncio
async def test_process_one_aborts_when_claim_token_mismatch(db_session, pending_input_factory):
    """token 不匹配 → 放弃不处理。"""
    inp = await pending_input_factory()
    claimed = await claim_batch(db_session, 10)
    await db_session.commit()
    fake = dict(claimed[0])
    fake["claim_token"] = str(uuid.uuid4())  # 篡改
    await process_one(db_session, fake)
    await db_session.commit()
    fresh = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.input_id == inp.input_id))).scalar_one()
    assert fresh.status == "processing"  # 未被处理


@pytest.mark.asyncio
async def test_process_one_failure_retries_then_error(db_session, pending_input_without_r11):
    """缺 R11 → raise → attempt 累加 + pending+next_retry；超 max → error。"""
    inp, supplier = pending_input_without_r11
    claimed = await claim_batch(db_session, 10)
    await db_session.commit()
    for _ in range(inp.max_attempts + 1):
        await process_one(db_session, claimed[0])
        await db_session.commit()
        # 重新 claim（若回 pending）
        reclaimed = await claim_batch(db_session, 10)
        await db_session.commit()
        if not reclaimed:
            break
        claimed[0] = reclaimed[0]
    fresh = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.input_id == inp.input_id))).scalar_one()
    assert fresh.status == "error"
    assert fresh.last_error is not None
    # 不写 SENT
    sent = (await db_session.execute(select(AuditLog).where(AuditLog.record_id == inp.capa_id, AuditLog.action == "SUPPLIER_RISK_INPUT_SENT"))).scalars().all()
    assert len(sent) == 0


@pytest.mark.asyncio
async def test_recover_stale_resets_old_processing(db_session, input_factory):
    """processing 超 10min → reset pending；达 max_attempts 的不重置。"""
    stale = await input_factory(status="processing", locked_at=datetime.now(timezone.utc) - timedelta(minutes=11), attempt_count=2, max_attempts=5)
    terminal = await input_factory(status="processing", locked_at=datetime.now(timezone.utc) - timedelta(minutes=11), attempt_count=6, max_attempts=5)
    await recover_stale_inputs(db_session)
    await db_session.commit()
    s = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.input_id == stale.input_id))).scalar_one()
    t = (await db_session.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.input_id == terminal.input_id))).scalar_one()
    assert s.status == "pending"
    assert t.status == "error"  # 达 max 不重置为 pending，标 error
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_risk_input_worker.py -x --tb=short`
Expected: FAIL（worker 模块不存在）

- [ ] **Step 3: 实现 worker**

```python
# backend/app/services/supplier_risk/risk_input_worker.py
"""Reliable outbox worker for supplier_risk_capa_inputs.

Pattern mirrors embedding_sync_worker.py: SELECT ... FOR UPDATE SKIP LOCKED claim,
claim_token ownership check on process, stale recovery, exponential backoff retry.
"""
import logging
import uuid
from datetime import datetime, timedelta, timezone

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.supplier_risk_capa_input import SupplierRiskCapaInput
from app.models.audit import AuditLog
from app.services.supplier_risk.service import evaluate_supplier_risk_in_tx
from app.services.supplier_risk.exceptions import SupplierRiskConfigurationError

logger = logging.getLogger(__name__)
STALE_THRESHOLD_MINUTES = 10


async def recover_stale_inputs(db: AsyncSession) -> None:
    """Reset processing > 10min to pending; terminal (attempt_count>=max) → error."""
    result = await db.execute(text("""
        UPDATE supplier_risk_capa_inputs
        SET status = CASE WHEN attempt_count >= max_attempts THEN 'error' ELSE 'pending' END,
            locked_at = NULL, claim_token = NULL
        WHERE status = 'processing' AND locked_at < NOW() - INTERVAL '10 minutes'
    """))
    if result.rowcount > 0:
        logger.warning("Recovered %s stale risk inputs", result.rowcount)
    await db.commit()


async def claim_batch(db: AsyncSession, batch_size: int) -> list[dict]:
    token = uuid.uuid4()
    result = await db.execute(text("""
        UPDATE supplier_risk_capa_inputs
        SET status = 'processing', locked_at = NOW(), attempt_count = attempt_count + 1, claim_token = :token
        WHERE input_id IN (
            SELECT input_id FROM supplier_risk_capa_inputs
            WHERE status = 'pending' AND (next_retry_at IS NULL OR next_retry_at <= NOW())
            ORDER BY next_retry_at NULLS FIRST
            LIMIT :batch_size FOR UPDATE SKIP LOCKED
        )
        RETURNING input_id, claim_token, capa_id, supplier_id, product_line_code
    """), {"token": token, "batch_size": batch_size})
    await db.commit()
    return [dict(row._mapping) for row in result.fetchall()]


async def process_one(db: AsyncSession, claimed: dict) -> None:
    """Process a single claimed input in its own transaction. Idempotent via claim_token."""
    # 重新锁 + claim_token 校验
    inp = (await db.execute(
        text("""SELECT * FROM supplier_risk_capa_inputs
                WHERE input_id = :id AND claim_token = :token
                FOR UPDATE"""),
        {"id": claimed["input_id"], "token": claimed["claim_token"]},
    )).first()
    if inp is None:
        # token 不匹配 / 已被 recovery 重置 → 放弃
        return
    inp = inp._mapping
    if inp["status"] != "processing":
        return
    # max_attempts 终态
    if inp["attempt_count"] > inp["max_attempts"]:
        await db.execute(text(
            "UPDATE supplier_risk_capa_inputs SET status='error', claim_token=NULL WHERE input_id=:id"
        ), {"id": claimed["input_id"]})
        await db.commit()
        return

    # 加载 ORM 对象用于 evaluate
    input_obj = await db.get(SupplierRiskCapaInput, uuid.UUID(claimed["input_id"]))
    capa_id = input_obj.capa_id
    try:
        await evaluate_supplier_risk_in_tx(
            db, input_obj.supplier_id, input_obj.product_line_code,
            force_update=True, trigger_input=input_obj,
        )
        input_obj.status = "processed"
        input_obj.claim_token = None
        input_obj.last_error = None
        db.add(AuditLog(
            table_name="capa_eightd", record_id=capa_id,
            action="SUPPLIER_RISK_INPUT_SENT",
            operated_by=input_obj.created_by,
            factory_id=input_obj.factory_id,
            changed_fields={
                "capa_id": str(capa_id),
                "input_id": str(input_obj.input_id),
                "supplier_id": str(input_obj.supplier_id),
                "severity": input_obj.severity,
                "disposition": input_obj.disposition or "",
                "repeat_suggested": input_obj.repeat_suggested,
                "repeat_confirmed": input_obj.repeat_confirmed,
                "repeat_detection_status": input_obj.repeat_detection_status,
                "matched_capa_nos": input_obj.matched_capa_nos,
                "risk_level": input_obj.evaluated_risk_level,
                "alert_id": str(input_obj.linked_alert_id) if input_obj.linked_alert_id else None,
            },
        ))
        await db.commit()
    except Exception as e:
        logger.exception("process_one failed for input %s", claimed["input_id"])
        await db.rollback()
        # 新事务写 retry/error
        backoff = 2 ** min(input_obj.attempt_count, 6)
        is_terminal = input_obj.attempt_count >= input_obj.max_attempts
        await db.execute(text("""
            UPDATE supplier_risk_capa_inputs
            SET status = :status, last_error = :err, claim_token = NULL,
                next_retry_at = CASE WHEN :terminal THEN NULL ELSE NOW() + (:backoff || ' seconds')::interval END
            WHERE input_id = :id
        """), {
            "status": "error" if is_terminal else "pending",
            "err": f"{type(e).__name__}: {e}"[:1000],
            "terminal": is_terminal, "backoff": backoff, "id": claimed["input_id"],
        })
        await db.commit()
```

- [ ] **Step 4: main.py 注册 loop**

在 `backend/app/main.py` 的 startup（仿 `_mes_outbox_loop`，约 L168 后）加：

```python
    # 01.6: supplier risk input outbox processor loop (every 30s)
    async def _risk_input_outbox_loop():
        while True:
            await asyncio.sleep(30)
            try:
                async for tenant, db in run_for_each_tenant():
                    try:
                        from app.services.supplier_risk.risk_input_worker import (
                            recover_stale_inputs, claim_batch, process_one,
                        )
                        await recover_stale_inputs(db)
                        claimed = await claim_batch(db, 20)
                        for c in claimed:
                            await process_one(db, c)
                    except Exception as e:
                        logger.error("[risk_input_loop] error for tenant %s: %s", tenant.slug, e)
            except Exception as e:
                logger.error("[risk_input_loop] error: %s", e)

    risk_input_task = asyncio.create_task(_risk_input_outbox_loop())
```

> 注：`run_for_each_tenant` / `logger` 是 main.py 既有（startup 末尾的风险评估 loop 已用）。若多租户 helper 名不同，对齐既有 `_risk_eval` loop 的调用方式。

- [ ] **Step 5: 运行 worker 单测**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_risk_input_worker.py -x --tb=short`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/supplier_risk/risk_input_worker.py backend/app/main.py backend/tests/test_risk_input_worker.py
git commit -m "feat(01.6): reliable risk input outbox worker + main loop"
```

---

## Task 7: confirm-repeat API + CAPAResponse 投影

**Files:**
- Modify: `backend/app/api/capa.py`（`confirm-repeat` 端点 + GET 投影组装）
- Modify: `backend/app/schemas/capa.py`（投影 schema）
- Modify: `backend/app/schemas/supplier_risk.py`（`AlertResponse.rule_results: list[dict]`）
- Test: `backend/tests/test_capa_confirm_repeat.py`

**Interfaces:**
- Consumes: Task 4 的 `evaluate_supplier_risk_in_tx`
- Produces: `POST /api/capa/{report_id}/confirm-repeat`（409 门控）；`CAPAResponse.supplier_risk_input` 投影。

- [ ] **Step 1: 写 API 测试**

```python
# backend/tests/test_capa_confirm_repeat.py
import uuid, pytest
from app.services.supplier_risk.service import evaluate_supplier_risk_in_tx


@pytest.mark.asyncio
async def test_confirm_repeat_409_when_not_processed(client, processed_input_with_capa, admin_token):
    """input 非 processed → 409。"""
    capa, inp = processed_input_with_capa  # inp.status="pending" or "processing"
    # 先把它设回 pending
    inp.status = "pending"
    resp = await client.post(f"/api/capa/{capa.report_id}/confirm-repeat",
                             json={"repeat_confirmed": True}, headers={"Authorization": f"Bearer {admin_token}"})
    assert resp.status_code == 409


@pytest.mark.asyncio
async def test_confirm_repeat_success_writes_changed_audit(client, processed_input_with_capa, admin_token, db_session):
    """processed → confirm → SUPPLIER_RISK_CHANGED + 快照重写。"""
    capa, inp = processed_input_with_capa
    inp.status = "processed"
    inp.evaluated_risk_level = "medium"
    old_eval_at = inp.evaluated_at
    await db_session.commit()
    resp = await client.post(f"/api/capa/{capa.report_id}/confirm-repeat",
                             json={"repeat_confirmed": True}, headers={"Authorization": f"Bearer {admin_token}"})
    assert resp.status_code == 200
    from app.models.audit import AuditLog
    from sqlalchemy import select
    changed = (await db_session.execute(select(AuditLog).where(AuditLog.record_id == capa.report_id, AuditLog.action == "SUPPLIER_RISK_CHANGED"))).scalars().all()
    assert len(changed) == 1
    cf = changed[0].changed_fields
    assert "old_level" in cf and "new_level" in cf and cf["repeat_confirmed"] is True
    # 快照重写
    assert changed[0].operated_by is not None


@pytest.mark.asyncio
async def test_get_capa_includes_supplier_risk_input_projection(client, processed_input_with_capa, admin_token):
    """GET /capa/{id} 含 supplier_risk_input 投影。"""
    capa, inp = processed_input_with_capa
    inp.status = "processed"
    inp.evaluated_risk_level = "high"
    # commit via db
    resp = await client.get(f"/api/capa/{capa.report_id}", headers={"Authorization": f"Bearer {admin_token}"})
    assert resp.status_code == 200
    body = resp.json()
    assert body.get("supplier_risk_input") is not None
    assert body["supplier_risk_input"]["evaluated_risk_level"] == "high"
    assert body["supplier_risk_input"]["status"] == "processed"
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa_confirm_repeat.py -x --tb=short`
Expected: FAIL（端点不存在 / 投影不存在）

- [ ] **Step 3: 修 `AlertResponse.rule_results`**

`backend/app/schemas/supplier_risk.py` L18：`rule_results: dict` → `rule_results: list[dict]`

- [ ] **Step 4: 加投影 schema + 端点**

`backend/app/schemas/capa.py` 加：
```python
class SupplierRiskInputProjection(BaseModel):
    input_id: uuid.UUID
    status: str
    repeat_suggested: bool | None
    repeat_detection_status: str
    repeat_confirmed: bool | None
    matched_capa_nos: list[str]
    evaluated_risk_level: str | None
    evaluated_risk_score: float | None
    linked_alert: dict | None = None
    model_config = {"from_attributes": True}
```

`CAPAResponse` 加 `supplier_risk_input: SupplierRiskInputProjection | None = None`。

`backend/app/api/capa.py` 加端点（`require_advance_permission` 附近，但需自定义权限 `canEdit('capa') + canEdit('supplier_risk')`）：

```python
@router.post("/{report_id}/confirm-repeat", response_model=CAPAResponse)
async def confirm_repeat(
    report_id: uuid.UUID,
    body: ConfirmRepeatRequest,
    db: AsyncSession = Depends(get_db),
    scope: RequestScope = Depends(get_request_scope),
):
    from app.core.permissions import Module, get_user_permission, PermissionLevel
    if get_user_permission(scope.user, Module.CAPA) < PermissionLevel.EDIT:
        raise HTTPException(status_code=403, detail="无 CAPA 编辑权限")
    if get_user_permission(scope.user, Module.SUPPLIER_RISK) < PermissionLevel.EDIT:
        raise HTTPException(status_code=403, detail="无供应商风险编辑权限")
    from sqlalchemy import select
    # 锁 capa
    capa = (await db.execute(select(CAPAEightD).where(CAPAEightD.report_id == report_id).with_for_update().execution_options(populate_existing=True))).scalar_one_or_none()
    if capa is None:
        raise HTTPException(status_code=404, detail="8D 不存在")
    # 工厂可见性（404 口径，同 advance）
    if scope.effective_factory_id and capa.factory_id != scope.effective_factory_id:
        raise HTTPException(status_code=404, detail="8D 不存在")
    if scope.factory_scope.accessible_factory_ids is not None and capa.factory_id not in scope.factory_scope.accessible_factory_ids:
        raise HTTPException(status_code=404, detail="8D 不存在")
    from app.models.supplier_risk_capa_input import SupplierRiskCapaInput
    inp = (await db.execute(select(SupplierRiskCapaInput).where(SupplierRiskCapaInput.capa_id == report_id).with_for_update())).scalar_one_or_none()
    if inp is None:
        raise HTTPException(status_code=404, detail="无供应商风险输入记录")
    if inp.status != "processed":
        raise HTTPException(status_code=409, detail="风险输入尚未处理完成，无法确认")
    if inp.supplier_id != capa.supplier_id:
        raise HTTPException(status_code=400, detail="输入记录与 8D 供应商不一致")
    old_level = inp.evaluated_risk_level
    inp.repeat_confirmed = body.repeat_confirmed
    from app.services.supplier_risk.service import evaluate_supplier_risk_in_tx
    await evaluate_supplier_risk_in_tx(db, inp.supplier_id, inp.product_line_code, force_update=True, trigger_input=inp)
    new_level = inp.evaluated_risk_level
    db.add(AuditLog(
        table_name="capa_eightd", record_id=report_id, action="SUPPLIER_RISK_CHANGED",
        operated_by=scope.user.user_id, factory_id=inp.factory_id,
        changed_fields={
            "old_level": old_level, "new_level": new_level,
            "repeat_confirmed": body.repeat_confirmed,
            "alert_id": str(inp.linked_alert_id) if inp.linked_alert_id else None,
        },
    ))
    await db.commit()
    await db.refresh(capa)
    return CAPAResponse.model_validate(capa)
```

`ConfirmRepeatRequest` 在 `schemas/capa.py`：
```python
class ConfirmRepeatRequest(BaseModel):
    repeat_confirmed: bool
```

GET by id 投影：在 `GET /api/capa/{report_id}` 的 handler 中，加载 capa 后查 input 组装投影塞进 response（参考 01.5 linked_scar 模式）。

- [ ] **Step 5: 运行 API 测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_capa_confirm_repeat.py -x --tb=short`
Expected: PASS

- [ ] **Step 6: 跑全量回归**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/ -x --tb=short -q -k "capa or supplier_risk or r11"`
Expected: 全 PASS

- [ ] **Step 7: Commit**

```bash
git add backend/app/api/capa.py backend/app/schemas/capa.py backend/app/schemas/supplier_risk.py backend/tests/test_capa_confirm_repeat.py
git commit -m "feat(01.6): confirm-repeat endpoint + supplier_risk_input projection"
```

---

## Task 8: R11 默认配置迁移 + seed

**Files:**
- Create: `backend/alembic/versions/20260716_seed_r11_config.py`
- Modify: `backend/app/seed.py`（`seed_supplier_risk_configs` 加 R11 + factory_id 修复）
- Modify: `backend/app/seed_e2e.py`（同步）
- Test: `backend/tests/test_seed_r11_config.py`

**Interfaces:**
- Consumes: Task 1 的迁移链
- Produces: R11 默认配置（迁移 no-op 兜底 + seed 幂等补齐，含 factory_id）。

- [ ] **Step 1: 写 seed 测试**

```python
# backend/tests/test_seed_r11_config.py
import pytest
from app.models.supplier_risk import SupplierRiskConfig
from sqlalchemy import select


@pytest.mark.asyncio
async def test_seed_includes_r11_with_factory_id(db_session, default_factory):
    """seed 后 R11 存在且 factory_id 非空。"""
    from app.seed import seed_supplier_risk_configs
    await seed_supplier_risk_configs(db_session, default_factory.factory_id)
    r11 = (await db_session.execute(select(SupplierRiskConfig).where(SupplierRiskConfig.rule_id == "R11", SupplierRiskConfig.supplier_id.is_(None)))).scalar_one()
    assert r11.enabled is True
    assert r11.category == "quality"
    assert r11.factory_id == default_factory.factory_id
    assert r11.updated_by is not None


@pytest.mark.asyncio
async def test_seed_idempotent_r11(db_session, default_factory):
    from app.seed import seed_supplier_risk_configs
    await seed_supplier_risk_configs(db_session, default_factory.factory_id)
    await seed_supplier_risk_configs(db_session, default_factory.factory_id)
    r11s = (await db_session.execute(select(SupplierRiskConfig).where(SupplierRiskConfig.rule_id == "R11", SupplierRiskConfig.supplier_id.is_(None)))).scalars().all()
    assert len(r11s) == 1
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_seed_r11_config.py -x --tb=short`
Expected: FAIL（R11 未插）

- [ ] **Step 3: 改 seed**

`backend/app/seed.py` 的 `DEFAULT_CONFIGS` 列表加 R11：
```python
    {"rule_id": "R11", "enabled": True, "category": "quality", "weight": 1.0,
     "thresholds": {"base_score": 10, "severe_bonus": 10, "repeat_bonus": 10}},
```

`seed_supplier_risk_configs` 签名加 `factory_id`，构造时传：
```python
async def seed_supplier_risk_configs(db, factory_id):
    admin = ...  # 既有
    for cfg in DEFAULT_CONFIGS:
        existing = ...
        if not existing:
            db.add(SupplierRiskConfig(
                rule_id=cfg["rule_id"], enabled=cfg["enabled"], category=cfg["category"],
                weight=cfg["weight"], thresholds=cfg["thresholds"],
                factory_id=factory_id, updated_by=admin.user_id,
            ))
    await db.commit()
```

> 注：检查 `seed.py` 调用 `seed_supplier_risk_configs` 的地方，传入默认 factory_id（既有 seed 流程已有默认工厂对象）。

`seed_e2e.py` 同步加 R11 + factory_id。

- [ ] **Step 4: 写 R11 配置迁移（no-op）**

新建 `backend/alembic/versions/20260716_seed_r11_config.py`（`down_revision` = Task 1 的 revision）：

```python
"""seed r11 default config (no-op if R01 absent)

Revision ID: 20260716_seed_r11_config
Revises: 20260716_supplier_risk_capa_inputs
"""
from alembic import op

revision = "20260716_seed_r11_config"
down_revision = "20260716_supplier_risk_capa_inputs"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.execute("""
        INSERT INTO supplier_risk_configs
          (config_id, rule_id, enabled, thresholds, weight, supplier_id, factory_id, category, product_line_code, updated_by, updated_at)
        SELECT gen_random_uuid(), 'R11', true, '{}'::jsonb, 1.0, NULL, factory_id, 'quality', NULL, updated_by, NOW()
        FROM supplier_risk_configs
        WHERE rule_id = 'R01' AND supplier_id IS NULL AND product_line_code IS NULL
          AND NOT EXISTS (SELECT 1 FROM supplier_risk_configs WHERE rule_id='R11' AND supplier_id IS NULL AND product_line_code IS NULL)
    """)


def downgrade() -> None:
    op.execute("DELETE FROM supplier_risk_configs WHERE rule_id = 'R11'")
```

- [ ] **Step 5: 运行测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/test_seed_r11_config.py tests/migrations/test_supplier_risk_capa_inputs_migration.py -x --tb=short`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add backend/alembic/versions/20260716_seed_r11_config.py backend/app/seed.py backend/app/seed_e2e.py backend/tests/test_seed_r11_config.py
git commit -m "feat(01.6): R11 default config migration + seed factory_id"
```

---

## Task 9: 前端 — 类型 + API + SupplierRiskInputCard

**Files:**
- Modify: `frontend/src/types/index.ts`
- Modify: `frontend/src/api/capa.ts`
- Create: `frontend/src/components/capa/SupplierRiskInputCard.tsx`
- Modify: `frontend/src/locales/{zh-CN,en-US}/capa.json`
- Modify: CAPA 详情页接线
- Test: `frontend/src/components/capa/__tests__/SupplierRiskInputCard.test.tsx`

**Interfaces:**
- Consumes: Task 7 的后端投影 + confirm-repeat 端点
- Produces: `SupplierRiskInput` TS 类型；`confirmRepeat(reportId, body)`；`SupplierRiskInputCard` 组件。

- [ ] **Step 1: 写组件测试**

```tsx
// frontend/src/components/capa/__tests__/SupplierRiskInputCard.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { SupplierRiskInputCard } from '../SupplierRiskInputCard';

const base = {
  input_id: 'i1', status: 'processed', repeat_suggested: true,
  repeat_detection_status: 'matched', repeat_confirmed: null,
  matched_capa_nos: ['8D-2025-001'], evaluated_risk_level: 'high',
  evaluated_risk_score: 80, linked_alert: null,
};

test('shows confirm buttons when processed and repeat_confirmed null and editable', () => {
  const onConfirm = vi.fn();
  render(<SupplierRiskInputCard input={base} canEdit={true} onConfirm={onConfirm} />);
  expect(screen.getByText(/系统判定曾复发/)).toBeInTheDocument();
  fireEvent.click(screen.getByText(/是，属复发/));
  expect(onConfirm).toHaveBeenCalledWith(true);
});

test('disables buttons when not processed', () => {
  render(<SupplierRiskInputCard input={{...base, status: 'pending'}} canEdit={true} onConfirm={vi.fn()} />);
  expect(screen.queryByText(/是，属复发/)).not.toBeInTheDocument();
});

test('shows unavailable message when no FMEA', () => {
  render(<SupplierRiskInputCard input={{...base, repeat_detection_status: 'unavailable'}} canEdit={true} onConfirm={vi.fn()} />);
  expect(screen.getByText(/未关联 FMEA/)).toBeInTheDocument();
});

test('shows fixed state when confirmed', () => {
  render(<SupplierRiskInputCard input={{...base, repeat_confirmed: true}} canEdit={true} onConfirm={vi.fn()} />);
  expect(screen.getByText(/已确认：复发/)).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd frontend && npx vitest run src/components/capa/__tests__/SupplierRiskInputCard.test.tsx`
Expected: FAIL（组件不存在）

- [ ] **Step 3: 加类型 + API**

`frontend/src/types/index.ts`：
```typescript
export interface SupplierRiskInputProjection {
  input_id: string;
  status: "pending" | "processing" | "processed" | "error";
  repeat_suggested: boolean | null;
  repeat_detection_status: "matched" | "not_matched" | "unavailable";
  repeat_confirmed: boolean | null;
  matched_capa_nos: string[];
  evaluated_risk_level: string | null;
  evaluated_risk_score: number | null;
  linked_alert: { alert_id: string; risk_level: string } | null;
}
```
在 `CAPAReport` 接口加 `supplier_risk_input?: SupplierRiskInputProjection | null;`。

`frontend/src/api/capa.ts`：
```typescript
export const confirmRepeat = (reportId: string, repeatConfirmed: boolean) =>
  api.post(`/capa/${reportId}/confirm-repeat`, { repeat_confirmed: repeatConfirmed });
```

- [ ] **Step 4: 实现组件**

```tsx
// frontend/src/components/capa/SupplierRiskInputCard.tsx
import { Tag, Space, Button, message } from 'antd';
import type { SupplierRiskInputProjection } from '../../types';
import { useTranslation } from 'react-i18next';

interface Props {
  input: SupplierRiskInputProjection;
  canEdit: boolean;
  onConfirm: (confirmed: boolean) => void;
}

export function SupplierRiskInputCard({ input, canEdit, onConfirm }: Props) {
  const { t } = useTranslation();
  if (!input) return null;
  const levelColor: Record<string, string> = { low: 'default', medium: 'orange', high: 'red', critical: 'red' };
  const showButtons = canEdit && input.status === 'processed' && input.repeat_confirmed === null;

  let prompt = '';
  if (input.repeat_confirmed === null) {
    if (input.repeat_detection_status === 'matched') prompt = t('capa:riskInput.matched', { nos: input.matched_capa_nos.join(', ') });
    else if (input.repeat_detection_status === 'not_matched') prompt = t('capa:riskInput.notMatched');
    else prompt = t('capa:riskInput.unavailable');
  }

  return (
    <div>
      <Space>
        {input.evaluated_risk_level && <Tag color={levelColor[input.evaluated_risk_level]}>{input.evaluated_risk_level}</Tag>}
        {input.status !== 'processed' && <Tag>{t(`capa:riskInput.status.${input.status}`)}</Tag>}
      </Space>
      {input.repeat_confirmed === null ? (
        <div>
          <p>{prompt}</p>
          {showButtons && (
            <Space>
              <Button type="primary" danger onClick={() => onConfirm(true)}>{t('capa:riskInput.confirmYes')}</Button>
              <Button onClick={() => onConfirm(false)}>{t('capa:riskInput.confirmNo')}</Button>
            </Space>
          )}
        </div>
      ) : (
        <p>{input.repeat_confirmed ? t('capa:riskInput.confirmedRecurrence') : t('capa:riskInput.confirmedNoRecurrence')}</p>
      )}
    </div>
  );
}
```

- [ ] **Step 5: 加 i18n**

`frontend/src/locales/zh-CN/capa.json` 加 `riskInput` 段（matched/notMatched/unavailable/confirmYes/confirmNo/confirmedRecurrence/confirmedNoRecurrence/status.*）。`en-US` 同步。

- [ ] **Step 6: 详情页接线**

在 CAPA 详情页「报告信息」DataCard（关联 SCAR 下方）加 `<SupplierRiskInputCard>`，`canEdit = canEdit('capa') && canEdit('supplier_risk')`，`onConfirm` 调 `confirmRepeat` 后 `getCAPA` 刷新。

- [ ] **Step 7: 运行组件测试**

Run: `cd frontend && npx vitest run src/components/capa/__tests__/SupplierRiskInputCard.test.tsx`
Expected: PASS

- [ ] **Step 8: tsc + build**

Run: `cd frontend && npm run build`
Expected: 无类型错误，build 成功

- [ ] **Step 9: Commit**

```bash
git add frontend/src/types/index.ts frontend/src/api/capa.ts frontend/src/components/capa/SupplierRiskInputCard.tsx frontend/src/locales frontend/src/components/capa/__tests__/SupplierRiskInputCard.test.tsx
# + 详情页接线文件
git commit -m "feat(01.6): frontend supplier risk input card + confirm API"
```

---

## Task 10: E2E + seed_e2e + PROGRESS 更新

**Files:**
- Modify: `backend/app/seed_e2e.py`（带 supplier_id + FMEA 的 8D + 历史归档 8D）
- Create: `frontend/e2e/specs/m1-core/capa-story-supplier-risk-input.spec.ts`
- Modify: `PROGRESS.md`

- [ ] **Step 1: 扩 seed_e2e**

在 `backend/app/seed_e2e.py` / `seed_e2e_constants.py` 加：一个带 `supplier_id` + 关联 FMEA node 的 8D（status D6_VERIFICATION，便于 e2e 推进 D7），以及同 supplier + 同 fmea_node_id 的归档历史 8D（status ARCHIVED）。确保 R11 配置 seed 已跑。

- [ ] **Step 2: 写 E2E spec**

```typescript
// frontend/e2e/specs/m1-core/capa-story-supplier-risk-input.spec.ts
import { test, expect } from '@playwright/test';
import { login, getApiContext } from '../../helpers';

test('US-E2E-01.6 8D→供应商风险输入闭环', async ({ page, request }) => {
  // 1. seed 已就绪（8D + supplier + FMEA + 历史 ARCHIVED 8D + R11 config）
  // 2. engineer 推进至 D7_COMPLETED
  const ctx = await getApiContext(request, 'engineer');
  await ctx.post(`/api/capa/${process.env.E2E_CAPA_RISK_ID}/advance`, { json: { target_state: 'D7_COMPLETED' } });

  // 3. 轮询 input 至 processed（worker 30s；测试可轮询最多 90s）
  let projection = null;
  for (let i = 0; i < 30; i++) {
    const r = await ctx.get(`/api/capa/${process.env.E2E_CAPA_RISK_ID}`);
    const body = await r.json();
    projection = body.supplier_risk_input;
    if (projection?.status === 'processed') break;
    await page.waitForTimeout(3000);
  }
  expect(projection).not.toBeNull();
  expect(projection.status).toBe('processed');
  expect(projection.repeat_suggested).toBe(true);
  expect(projection.matched_capa_nos.length).toBeGreaterThan(0);
  expect(projection.evaluated_risk_level).not.toBeNull();

  // 4. 后台断言 SENT 审计
  const adminCtx = await getApiContext(request, 'admin');
  const logs = await adminCtx.get(`/api/admin/logs/audit?table_name=capa_eightd&record_id=${process.env.E2E_CAPA_RISK_ID}`);
  const logBody = await logs.json();
  const sent = logBody.items.find((l: any) => l.action === 'SUPPLIER_RISK_INPUT_SENT');
  expect(sent).toBeTruthy();
  expect(sent.changed_fields.disposition).toBeDefined();
  expect(sent.changed_fields.risk_level).toBe(projection.evaluated_risk_level);

  // 5. confirm-repeat
  await ctx.post(`/api/capa/${process.env.E2E_CAPA_RISK_ID}/confirm-repeat`, { json: { repeat_confirmed: true } });

  // 6. 断言 CHANGED 审计（字段存在，不断言等级变化）
  const logs2 = await adminCtx.get(`/api/admin/logs/audit?table_name=capa_eightd&record_id=${process.env.E2E_CAPA_RISK_ID}`);
  const logBody2 = await logs2.json();
  const changed = logBody2.items.find((l: any) => l.action === 'SUPPLIER_RISK_CHANGED');
  expect(changed).toBeTruthy();
  expect(changed.changed_fields.old_level).toBeDefined();
  expect(changed.changed_fields.new_level).toBeDefined();
  expect(changed.changed_fields.repeat_confirmed).toBe(true);
});
```

- [ ] **Step 3: 运行 E2E（需 e2e 环境跑起）**

Run: `make e2e TEST_ARGS="--grep supplier-risk-input"`
Expected: PASS（若 worker 周期太慢，可在 e2e config 缩短 loop 间隔或加触发端点）

- [ ] **Step 4: 更新 PROGRESS.md**

在 PROGRESS.md 的「US-E2E-01 v8.1 待办任务」勾掉 01.6，加 deploy note（R11 配置须 seed / worker loop / supplier_id 生命周期）。

- [ ] **Step 5: 跑全量后端 + 前端回归**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/ -x --tb=short -q` && `cd frontend && npm run build`
Expected: 全绿

- [ ] **Step 6: Commit**

```bash
git add backend/app/seed_e2e.py backend/app/seed_e2e_constants.py frontend/e2e/specs/m1-core/capa-story-supplier-risk-input.spec.ts PROGRESS.md
git commit -m "feat(01.6): e2e + seed + PROGRESS update"
```

---

## Self-Review (已执行)

**Spec coverage 核对：**
- §4.1 capa.supplier_id → Task 1/2 ✓
- §4.2 SupplierRiskCapaInput 表 → Task 1 ✓
- §4.4 linked_capa_id 解耦 → Task 1（不写新值，仅历史兼容，迁移不动该列）✓
- §4.5 supplier_id 生命周期 → Task 2 ✓
- §4.6 AlertResponse.rule_results list + 旧数据归一化 → Task 1（迁移规整）+ Task 7（schema）✓
- §5.1 advance 触发 → Task 5 ✓
- §5.2 worker → Task 6 ✓
- §5.3 evaluate_in_tx → Task 4 ✓
- §5.4 confirm-repeat → Task 7 ✓
- §5.5 R11 全家桶 → Task 3/4/8 ✓
- §5.6 force_update → Task 4 ✓
- §5.7 审计契约 → Task 5/6/7 ✓
- §6 前端 + E2E → Task 9/10 ✓

**类型一致性：** `SupplierRiskCapaInput` 字段、`evaluate_supplier_risk_in_tx` 签名、`confirm-repeat` 请求体在各任务一致。

**无占位符：** 所有步骤含完整代码。
