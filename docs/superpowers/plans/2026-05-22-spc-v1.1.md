# SPC v1.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 扩展 SPC 模块，增加 P/NP/C/U 四种计数值控制图和控制限多版本管理功能。

**Architecture:** 最小扩展现有表结构（新增两列到 sample_batches，两列到 control_limit_snapshots），在计算引擎新增4个函数，服务层和 API 适配，前端在现有页面中扩展录入弹窗和新增版本管理面板。

**Tech Stack:** Python 3.11 + FastAPI + SQLAlchemy 2.0 (async) + Alembic | React 18 + TypeScript + Ant Design 5 + ECharts

**Worktree:** `.worktrees/spc-v1.1` (branch `feat/spc-v1.1`)  
所有命令在 worktree 根目录下执行：`/Users/sam/Documents/Code/OpenQMS/.worktrees/spc-v1.1`

---

## 文件结构

**修改（后端）：**
- `backend/alembic/versions/008_spc_v1_1_attribute_charts.py` — 新建 migration
- `backend/app/models/spc.py` — SampleBatch + ControlLimitSnapshot 新增字段
- `backend/app/services/spc_calculation_engine.py` — 新增4个计数值计算函数
- `backend/app/services/spc_service.py` — 扩展 add_sample_batch、lock_unlock_control_limits、_compute_chart_data、_reevaluate_alarms，新增 list_snapshots、activate_snapshot
- `backend/app/schemas/spc.py` — 扩展 SampleBatchCreate、SampleBatchOut、ChartDataResponse，新增 ControlLimitSnapshotOut
- `backend/app/api/spc.py` — 扩展 chart_type 校验，新增两个 snapshot 路由

**修改（前端）：**
- `frontend/src/types/spc.ts` — 扩展类型定义
- `frontend/src/api/spc.ts` — 新增两个 API 函数
- `frontend/src/pages/spc/SPCListPage.tsx` — 扩展 CreateModal
- `frontend/src/pages/spc/SPCDetailPage.tsx` — 扩展 AddBatch 逻辑 + 图表渲染 + 新增 VersionPanel

---

## Task 1: Alembic Migration

**Files:**
- Create: `backend/alembic/versions/008_spc_v1_1_attribute_charts.py`

- [ ] **Step 1: 创建 migration 文件**

```python
# backend/alembic/versions/008_spc_v1_1_attribute_charts.py
"""spc v1.1: attribute charts and control limit versioning

Revision ID: 008_spc_v1_1
Revises: 007_add_supplier_management
Create Date: 2026-05-22
"""
from alembic import op
import sqlalchemy as sa

revision = '008_spc_v1_1'
down_revision = '007_add_supplier_management'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # sample_batches: add attribute chart columns
    op.add_column('sample_batches', sa.Column('inspected_count', sa.Integer(), nullable=True))
    op.add_column('sample_batches', sa.Column('defect_count', sa.Integer(), nullable=True))

    # control_limit_snapshots: add versioning columns
    op.add_column('control_limit_snapshots', sa.Column('version_no', sa.Integer(), nullable=False, server_default='1'))
    op.add_column('control_limit_snapshots', sa.Column('is_active', sa.Boolean(), nullable=False, server_default='false'))


def downgrade() -> None:
    op.drop_column('control_limit_snapshots', 'is_active')
    op.drop_column('control_limit_snapshots', 'version_no')
    op.drop_column('sample_batches', 'defect_count')
    op.drop_column('sample_batches', 'inspected_count')
```

- [ ] **Step 2: 验证 migration 语法**

```bash
cd backend
python -c "import py_compile; py_compile.compile('alembic/versions/008_spc_v1_1_attribute_charts.py', doraise=True); print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add backend/alembic/versions/008_spc_v1_1_attribute_charts.py
git commit -m "feat(spc): add migration 008 for attribute charts and limit versioning"
```

---

## Task 2: 更新 ORM 模型

**Files:**
- Modify: `backend/app/models/spc.py`

- [ ] **Step 1: 在 SampleBatch 中新增两个可空列**

在 `SampleBatch` 类的 `is_locked` 字段之后添加：

```python
    inspected_count: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)
    defect_count: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)
```

确保文件顶部已有 `from typing import List, Optional`。

- [ ] **Step 2: 在 ControlLimitSnapshot 中新增版本列**

在 `ControlLimitSnapshot` 类的 `is_locked` 字段之后添加：

```python
    version_no: Mapped[int] = mapped_column(Integer, nullable=False, default=1)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
```

- [ ] **Step 3: 验证语法**

```bash
cd backend
python -c "from app.models.spc import SampleBatch, ControlLimitSnapshot; print('OK')"
```

Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add backend/app/models/spc.py
git commit -m "feat(spc): add inspected_count/defect_count to SampleBatch, version_no/is_active to ControlLimitSnapshot"
```

---

## Task 3: 计算引擎 — 新增4个计数值函数

**Files:**
- Modify: `backend/app/services/spc_calculation_engine.py`
- Modify: `backend/app/test_spc_engine.py`

- [ ] **Step 1: 先写测试（TDD）**

在 `backend/app/test_spc_engine.py` 末尾追加：

```python
# ============ Attribute Chart Tests ============

def test_calculate_p_limits_basic():
    batches = [
        {"inspected_count": 100, "defect_count": 5},
        {"inspected_count": 100, "defect_count": 3},
        {"inspected_count": 100, "defect_count": 7},
        {"inspected_count": 100, "defect_count": 4},
        {"inspected_count": 100, "defect_count": 6},
    ]
    result = calculate_p_limits(batches)
    assert "cl" in result
    assert "ucl_list" in result
    assert "lcl_list" in result
    assert len(result["ucl_list"]) == 5
    assert len(result["lcl_list"]) == 5
    assert abs(result["cl"] - 0.05) < 0.001
    # UCL = p_bar + 3*sqrt(p_bar*(1-p_bar)/n) = 0.05 + 3*sqrt(0.0475/100)
    expected_ucl = 0.05 + 3 * (0.05 * 0.95 / 100) ** 0.5
    assert abs(result["ucl_list"][0] - round(expected_ucl, 4)) < 0.001
    # LCL >= 0
    assert all(v >= 0 for v in result["lcl_list"])


def test_calculate_np_limits_basic():
    batches = [
        {"inspected_count": 50, "defect_count": 2},
        {"inspected_count": 50, "defect_count": 3},
        {"inspected_count": 50, "defect_count": 1},
        {"inspected_count": 50, "defect_count": 4},
        {"inspected_count": 50, "defect_count": 2},
    ]
    result = calculate_np_limits(batches)
    assert "cl" in result
    assert "ucl" in result
    assert "lcl" in result
    # np_bar = (2+3+1+4+2)/5 = 2.4
    assert abs(result["cl"] - 2.4) < 0.001
    assert result["lcl"] >= 0


def test_calculate_np_limits_variable_n_raises():
    batches = [
        {"inspected_count": 50, "defect_count": 2},
        {"inspected_count": 60, "defect_count": 3},
    ]
    try:
        calculate_np_limits(batches)
        assert False, "Should have raised ValueError"
    except ValueError as e:
        assert "固定" in str(e) or "fixed" in str(e).lower()


def test_calculate_c_limits_basic():
    batches = [
        {"inspected_count": 1, "defect_count": 3},
        {"inspected_count": 1, "defect_count": 5},
        {"inspected_count": 1, "defect_count": 2},
        {"inspected_count": 1, "defect_count": 4},
        {"inspected_count": 1, "defect_count": 6},
    ]
    result = calculate_c_limits(batches)
    assert abs(result["cl"] - 4.0) < 0.001
    # UCL = c_bar + 3*sqrt(c_bar) = 4 + 3*2 = 10
    assert abs(result["ucl"] - 10.0) < 0.001
    assert result["lcl"] >= 0


def test_calculate_u_limits_basic():
    batches = [
        {"inspected_count": 10, "defect_count": 3},
        {"inspected_count": 20, "defect_count": 8},
        {"inspected_count": 15, "defect_count": 5},
        {"inspected_count": 10, "defect_count": 2},
        {"inspected_count": 20, "defect_count": 6},
    ]
    result = calculate_u_limits(batches)
    assert "cl" in result
    assert "ucl_list" in result
    assert "lcl_list" in result
    assert len(result["ucl_list"]) == 5
    assert all(v >= 0 for v in result["lcl_list"])


def test_lcl_truncated_to_zero():
    # With very low defect rate, LCL could go negative — must be clamped to 0
    batches = [{"inspected_count": 100, "defect_count": 0} for _ in range(5)]
    batches[0]["defect_count"] = 1
    result = calculate_p_limits(batches)
    assert all(v >= 0 for v in result["lcl_list"])
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd backend
python -m pytest app/test_spc_engine.py -k "p_limits or np_limits or c_limits or u_limits or lcl_truncated" -v 2>&1 | head -30
```

Expected: `ImportError` 或 `NameError`（函数未定义）

- [ ] **Step 3: 实现4个计数值函数**

在 `backend/app/services/spc_calculation_engine.py` 末尾（`get_capability_advice` 函数之后）追加：

```python


def calculate_p_limits(batches: List[Dict[str, Any]]) -> Dict[str, Any]:
    """P chart: proportion nonconforming. Variable sample size supported."""
    if not batches:
        return {"cl": None, "ucl_list": [], "lcl_list": []}
    total_defects = sum(b["defect_count"] for b in batches)
    total_inspected = sum(b["inspected_count"] for b in batches)
    if total_inspected == 0:
        return {"cl": None, "ucl_list": [], "lcl_list": []}
    p_bar = total_defects / total_inspected
    ucl_list = []
    lcl_list = []
    for b in batches:
        n = b["inspected_count"]
        if n == 0:
            ucl_list.append(None)
            lcl_list.append(0.0)
            continue
        spread = 3 * math.sqrt(p_bar * (1 - p_bar) / n)
        ucl_list.append(round(p_bar + spread, 4))
        lcl_list.append(max(0.0, round(p_bar - spread, 4)))
    return {"cl": round(p_bar, 4), "ucl_list": ucl_list, "lcl_list": lcl_list}


def calculate_np_limits(batches: List[Dict[str, Any]]) -> Dict[str, Any]:
    """NP chart: number nonconforming. Fixed sample size required."""
    if not batches:
        return {"cl": None, "ucl": None, "lcl": None}
    n_values = [b["inspected_count"] for b in batches]
    if len(set(n_values)) > 1:
        raise ValueError("NP图要求每批次样本量固定一致")
    n = n_values[0]
    np_bar = sum(b["defect_count"] for b in batches) / len(batches)
    p_bar = np_bar / n if n > 0 else 0
    spread = 3 * math.sqrt(np_bar * (1 - p_bar))
    return {
        "cl": round(np_bar, 4),
        "ucl": round(np_bar + spread, 4),
        "lcl": max(0.0, round(np_bar - spread, 4)),
    }


def calculate_c_limits(batches: List[Dict[str, Any]]) -> Dict[str, Any]:
    """C chart: count of defects per inspection unit. Fixed unit size assumed."""
    if not batches:
        return {"cl": None, "ucl": None, "lcl": None}
    c_bar = sum(b["defect_count"] for b in batches) / len(batches)
    spread = 3 * math.sqrt(c_bar)
    return {
        "cl": round(c_bar, 4),
        "ucl": round(c_bar + spread, 4),
        "lcl": max(0.0, round(c_bar - spread, 4)),
    }


def calculate_u_limits(batches: List[Dict[str, Any]]) -> Dict[str, Any]:
    """U chart: defects per unit. Variable sample size supported."""
    if not batches:
        return {"cl": None, "ucl_list": [], "lcl_list": []}
    total_defects = sum(b["defect_count"] for b in batches)
    total_inspected = sum(b["inspected_count"] for b in batches)
    if total_inspected == 0:
        return {"cl": None, "ucl_list": [], "lcl_list": []}
    u_bar = total_defects / total_inspected
    ucl_list = []
    lcl_list = []
    for b in batches:
        n = b["inspected_count"]
        if n == 0:
            ucl_list.append(None)
            lcl_list.append(0.0)
            continue
        spread = 3 * math.sqrt(u_bar / n)
        ucl_list.append(round(u_bar + spread, 4))
        lcl_list.append(max(0.0, round(u_bar - spread, 4)))
    return {"cl": round(u_bar, 4), "ucl_list": ucl_list, "lcl_list": lcl_list}
```

- [ ] **Step 4: 在 test 文件顶部确认导入这4个函数**

检查 `backend/app/test_spc_engine.py` 的 import 行，确保包含新函数：

```python
from app.services.spc_calculation_engine import (
    calculate_xbar_r_limits,
    calculate_imr_limits,
    calculate_histogram_data,
    evaluate_western_electric,
    calculate_cp_cpk,
    calculate_pp_ppk,
    calculate_cm,
    calculate_ppm,
    get_capability_grade,
    get_capability_advice,
    calculate_p_limits,
    calculate_np_limits,
    calculate_c_limits,
    calculate_u_limits,
)
```

- [ ] **Step 5: 运行测试确认通过**

```bash
cd backend
python -m pytest app/test_spc_engine.py -k "p_limits or np_limits or c_limits or u_limits or lcl_truncated" -v
```

Expected: 全部 PASS

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/spc_calculation_engine.py backend/app/test_spc_engine.py
git commit -m "feat(spc): add P/NP/C/U attribute chart calculation functions with tests"
```

---

## Task 4: 更新 Pydantic Schemas

**Files:**
- Modify: `backend/app/schemas/spc.py`

- [ ] **Step 1: 扩展 SampleBatchCreate — 新增计数值字段**

将现有 `SampleBatchCreate` 替换为：

```python
class SampleBatchCreate(BaseModel):
    batch_no: str = Field(..., min_length=1, max_length=50)
    sampled_at: datetime
    values: List[float] = Field(default_factory=list)
    inspected_count: Optional[int] = Field(default=None, ge=1)
    defect_count: Optional[int] = Field(default=None, ge=0)
```

- [ ] **Step 2: 扩展 SampleBatchOut — 新增计数值字段**

将现有 `SampleBatchOut` 替换为：

```python
class SampleBatchOut(BaseModel):
    batch_id: UUID
    ic_id: UUID
    batch_no: str
    sampled_at: datetime
    subgroup_size: int
    values: List[float]
    inspected_count: Optional[int] = None
    defect_count: Optional[int] = None

    model_config = {"from_attributes": True}
```

- [ ] **Step 3: 新增 ControlLimitSnapshotOut schema**

在 `# ============ Alarms ============` 注释之前新增：

```python
# ============ Control Limit Snapshots ============

class ControlLimitSnapshotOut(BaseModel):
    snapshot_id: UUID
    ic_id: UUID
    ucl: float
    lcl: float
    cl: float
    r_ucl: Optional[float]
    r_lcl: Optional[float]
    r_cl: Optional[float]
    version_no: int
    is_active: bool
    is_locked: bool
    calculated_at: datetime
    created_at: datetime

    model_config = {"from_attributes": True}
```

- [ ] **Step 4: 扩展 ChartDataResponse — 新增 active_snapshot 字段**

将现有 `ChartDataResponse` 替换为：

```python
class ChartDataResponse(BaseModel):
    chart_type: str
    data_points: List[ChartDataPoint]
    limits: ControlLimits
    total_batches: int
    active_snapshot: Optional["ControlLimitSnapshotOut"] = None
```

将这行放在 `ControlLimitSnapshotOut` 定义**之后**，避免前向引用问题。如果 `ChartDataResponse` 在 `ControlLimitSnapshotOut` 之前，改变两个类的定义顺序，或在 `ChartDataResponse` 末尾加 `model_rebuild()`。

- [ ] **Step 5: 扩展 ControlLimits — 新增计数值变控制线字段**

将现有 `ControlLimits` 替换为：

```python
class ControlLimits(BaseModel):
    ucl: Optional[float] = None
    lcl: Optional[float] = None
    cl: Optional[float] = None
    r_ucl: Optional[float] = None
    r_lcl: Optional[float] = None
    r_cl: Optional[float] = None
    ucl_list: Optional[List[float]] = None   # P/U 图变控制线
    lcl_list: Optional[List[float]] = None   # P/U 图变控制线
```

- [ ] **Step 6: 验证语法**

```bash
cd backend
python -c "from app.schemas.spc import SampleBatchCreate, ControlLimitSnapshotOut, ChartDataResponse; print('OK')"
```

Expected: `OK`

- [ ] **Step 7: Commit**

```bash
git add backend/app/schemas/spc.py
git commit -m "feat(spc): extend schemas for attribute batch data, snapshot versioning, variable control limits"
```

---

## Task 5: 服务层扩展

**Files:**
- Modify: `backend/app/services/spc_service.py`

- [ ] **Step 1: 更新 import — 新增4个计算函数**

找到现有的 import 行：

```python
from app.services.spc_calculation_engine import (
    calculate_xbar_r_limits, calculate_imr_limits,
```

扩展为：

```python
from app.services.spc_calculation_engine import (
    calculate_xbar_r_limits, calculate_imr_limits,
    calculate_histogram_data, evaluate_western_electric,
    calculate_cp_cpk, calculate_pp_ppk, calculate_cm, calculate_ppm,
    get_capability_grade, get_capability_advice,
    calculate_p_limits, calculate_np_limits, calculate_c_limits, calculate_u_limits,
)
```

- [ ] **Step 2: 扩展 add_sample_batch — 支持计数值批次**

在 `add_sample_batch` 函数中，在 `values = data["values"]` 之后、`batch = SampleBatch(...)` 之前添加计数值校验：

```python
    # Attribute chart validation
    attribute_charts = {"p", "np", "c", "u"}
    if ic.chart_type in attribute_charts:
        inspected_count = data.get("inspected_count")
        defect_count = data.get("defect_count")
        if inspected_count is None or defect_count is None:
            raise ValueError(f"计数值图（{ic.chart_type}）必须提供 inspected_count 和 defect_count")
        if defect_count > inspected_count:
            raise ValueError("defect_count 不能超过 inspected_count")
    else:
        inspected_count = None
        defect_count = None
```

然后在 `SampleBatch(...)` 构造中加入新字段：

```python
    batch = SampleBatch(
        ic_id=ic_id,
        batch_no=data["batch_no"],
        sampled_at=data["sampled_at"],
        subgroup_size=len(values) if values else 0,
        inspected_count=inspected_count,
        defect_count=defect_count,
    )
```

- [ ] **Step 3: 扩展 _get_active_control_limits — 支持计数值图**

在 `_get_active_control_limits` 函数中，找到计算 live limits 的部分（if/else 分支），在 `else: # imr` 分支之后新增：

```python
    elif ic.chart_type in {"p", "np", "c", "u"}:
        # For attribute charts: fetch all batches with attribute data
        result = await db.execute(
            select(SampleBatch)
            .where(SampleBatch.ic_id == ic.ic_id)
            .where(SampleBatch.is_locked == False)
            .order_by(SampleBatch.sampled_at)
        )
        attr_batches = result.scalars().all()
        if len(attr_batches) < 2:
            return {}
        batch_data = [
            {"inspected_count": b.inspected_count, "defect_count": b.defect_count}
            for b in attr_batches
            if b.inspected_count is not None and b.defect_count is not None
        ]
        if ic.chart_type == "p":
            return calculate_p_limits(batch_data)
        elif ic.chart_type == "np":
            return calculate_np_limits(batch_data)
        elif ic.chart_type == "c":
            return calculate_c_limits(batch_data)
        else:  # u
            return calculate_u_limits(batch_data)
```

- [ ] **Step 4: 扩展 lock_unlock_control_limits — 新增版本号逻辑**

在 `lock_unlock_control_limits` 函数中，找到 `if locked:` 代码块，将其替换为：

```python
    if locked:
        limits = await _get_active_control_limits(db, ic)
        # Compute next version_no for this ic
        version_result = await db.execute(
            select(func.max(ControlLimitSnapshot.version_no))
            .where(ControlLimitSnapshot.ic_id == ic_id)
        )
        max_version = version_result.scalar() or 0
        next_version = max_version + 1

        # Deactivate all existing snapshots for this ic
        await db.execute(
            update(ControlLimitSnapshot)
            .where(ControlLimitSnapshot.ic_id == ic_id)
            .values(is_active=False)
        )

        snapshot = ControlLimitSnapshot(
            ic_id=ic_id,
            ucl=limits.get("ucl") or 0,
            lcl=limits.get("lcl") or 0,
            cl=limits.get("cl") or 0,
            r_ucl=limits.get("r_ucl"),
            r_lcl=limits.get("r_lcl"),
            r_cl=limits.get("r_cl"),
            is_locked=True,
            version_no=next_version,
            is_active=True,
        )
        db.add(snapshot)
```

确保在文件顶部已导入 `func` 和 `update`：

```python
from sqlalchemy import select, and_, func, update
```

- [ ] **Step 5: 扩展 _compute_chart_data — 支持计数值图数据点**

在 `_compute_chart_data` 函数中，在 `else: # imr` 分支之后新增计数值图分支：

```python
    elif ic.chart_type in {"p", "np", "c", "u"}:
        # Fetch attribute batches directly (no SampleValue rows)
        attr_result = await db.execute(
            select(SampleBatch)
            .where(SampleBatch.ic_id == ic.ic_id)
            .where(SampleBatch.is_locked == False)
            .order_by(SampleBatch.sampled_at)
        )
        attr_batches = attr_result.scalars().all()
        data_points = []
        for i, b in enumerate(attr_batches):
            if b.inspected_count is None or b.defect_count is None:
                continue
            if ic.chart_type in {"p", "u"}:
                stat = b.defect_count / b.inspected_count if b.inspected_count > 0 else 0
            else:
                stat = b.defect_count
            data_points.append({
                "batch_index": i,
                "batch_no": b.batch_no,
                "sampled_at": b.sampled_at,
                "x_value": round(stat, 4),
                "r_value": None,
                "alarm_flags": [],
            })
        batch_list = []  # not used for total_batches below
```

然后修改 return 语句，使 `total_batches` 对计数值图用 `len(data_points)`：

```python
    total = len(batch_list) if ic.chart_type not in {"p", "np", "c", "u"} else len(data_points)
    return {
        "chart_type": ic.chart_type,
        "data_points": data_points,
        "limits": limits,
        "total_batches": total,
    }
```

- [ ] **Step 6: 扩展 _reevaluate_alarms — 计数值图只用规则1**

在 `_reevaluate_alarms` 中，在 `subgroup_stats = [...]` 之后、`alarms = evaluate_western_electric(...)` 之前：

```python
    # Attribute charts: only Rule 1 applies (normality not assumed)
    if ic.chart_type in {"p", "np", "c", "u"}:
        rules_config = {"rule_1": ic.rules_config.get("rule_1", True)}
        for k in ["rule_2", "rule_3", "rule_4", "rule_5", "rule_6", "rule_7", "rule_8"]:
            rules_config[k] = False
    else:
        rules_config = ic.rules_config
    alarms = evaluate_western_electric(subgroup_stats, limits, rules_config)
```

- [ ] **Step 7: 新增 list_snapshots 服务函数**

在 `ingest_external_data` 函数之前追加：

```python
async def list_snapshots(db: AsyncSession, ic_id: uuid.UUID) -> list:
    ic = await get_inspection_characteristic(db, ic_id)
    if not ic:
        raise ValueError("Inspection characteristic not found")
    result = await db.execute(
        select(ControlLimitSnapshot)
        .where(ControlLimitSnapshot.ic_id == ic_id)
        .order_by(ControlLimitSnapshot.version_no.desc())
    )
    return result.scalars().all()


async def activate_snapshot(
    db: AsyncSession, user_id: uuid.UUID, ic_id: uuid.UUID, snapshot_id: uuid.UUID
) -> ControlLimitSnapshot:
    ic = await get_inspection_characteristic(db, ic_id)
    if not ic:
        raise ValueError("Inspection characteristic not found")
    # Verify snapshot belongs to this ic
    snap_result = await db.execute(
        select(ControlLimitSnapshot).where(
            and_(
                ControlLimitSnapshot.snapshot_id == snapshot_id,
                ControlLimitSnapshot.ic_id == ic_id,
            )
        )
    )
    snapshot = snap_result.scalar_one_or_none()
    if not snapshot:
        raise ValueError("Snapshot not found for this characteristic")
    # Deactivate all, then activate the target
    await db.execute(
        update(ControlLimitSnapshot)
        .where(ControlLimitSnapshot.ic_id == ic_id)
        .values(is_active=False)
    )
    snapshot.is_active = True
    await db.commit()
    await db.refresh(snapshot)
    await _create_audit_log(
        db, user_id, "TRANSITION", "control_limit_snapshots", snapshot_id,
        {"action": "activate", "version_no": snapshot.version_no}
    )
    return snapshot
```

- [ ] **Step 8: 验证语法**

```bash
cd backend
python -c "from app.services.spc_service import list_snapshots, activate_snapshot; print('OK')"
```

Expected: `OK`

- [ ] **Step 9: Commit**

```bash
git add backend/app/services/spc_service.py
git commit -m "feat(spc): extend service layer for attribute charts, limit versioning, snapshot management"
```

---

## Task 6: API 路由扩展

**Files:**
- Modify: `backend/app/api/spc.py`

- [ ] **Step 1: 更新 chart_type 校验正则**

找到：

```python
chart_type: str = Field(..., pattern="^(xbar_r|imr|histogram)$")
```

位于 schemas，已在 Task 4 完成。此处确认 API 层无重复校验（无需修改）。

- [ ] **Step 2: 更新批次录入路由，接收计数值字段**

找到 `POST /inspection-characteristics/{ic_id}/batches` 路由，确认它直接调用 `spc_service.add_sample_batch(db, user_id, ic_id, data.model_dump())`。  
`SampleBatchCreate` schema 已扩展，路由无需修改——Pydantic 自动接收 `inspected_count` 和 `defect_count`。

验证路由文件中的批次录入调用签名：

```bash
grep -n "add_sample_batch" backend/app/api/spc.py
```

确认调用方式为 `await spc_service.add_sample_batch(db, current_user.user_id, ic_id, data.model_dump())`。

- [ ] **Step 3: 新增 GET snapshots 路由**

在 `backend/app/api/spc.py` 文件末尾（`ingest_external_data` 路由之前）新增：

```python
@router.get(
    "/inspection-characteristics/{ic_id}/snapshots",
    response_model=List[ControlLimitSnapshotOut],
)
async def get_snapshots(
    ic_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
    current_user=Depends(get_current_user),
):
    try:
        return await spc_service.list_snapshots(db, ic_id)
    except ValueError as e:
        raise HTTPException(status_code=404, detail=str(e))
```

- [ ] **Step 4: 新增 PATCH activate 路由**

```python
@router.patch(
    "/inspection-characteristics/{ic_id}/snapshots/{snapshot_id}/activate",
    response_model=ControlLimitSnapshotOut,
)
async def activate_snapshot(
    ic_id: uuid.UUID,
    snapshot_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
    current_user=Depends(get_current_user),
):
    try:
        return await spc_service.activate_snapshot(db, current_user.user_id, ic_id, snapshot_id)
    except ValueError as e:
        raise HTTPException(status_code=404, detail=str(e))
```

- [ ] **Step 5: 确认 ControlLimitSnapshotOut 已导入**

在 `backend/app/api/spc.py` 的 import 部分，确认包含：

```python
from app.schemas.spc import (
    ...,
    ControlLimitSnapshotOut,
)
```

- [ ] **Step 6: 验证语法**

```bash
cd backend
python -c "from app.api.spc import router; print('OK')"
```

Expected: `OK`

- [ ] **Step 7: Commit**

```bash
git add backend/app/api/spc.py
git commit -m "feat(spc): add snapshot list and activate routes"
```

---

## Task 7: 前端类型更新

**Files:**
- Modify: `frontend/src/types/spc.ts`

- [ ] **Step 1: 扩展 chart_type 联合类型**

将：

```typescript
chart_type: "xbar_r" | "imr" | "histogram";
```

替换为（`InspectionCharacteristic` 和 `CreateICRequest` 两处均需更新）：

```typescript
chart_type: "xbar_r" | "imr" | "histogram" | "p" | "np" | "c" | "u";
```

- [ ] **Step 2: 扩展 SampleBatch 类型**

在 `values: number[];` 之后追加：

```typescript
  inspected_count?: number;
  defect_count?: number;
```

- [ ] **Step 3: 扩展 ControlLimits 类型**

在 `r_cl?: number;` 之后追加：

```typescript
  ucl_list?: number[];
  lcl_list?: number[];
```

- [ ] **Step 4: 新增 ControlLimitSnapshot 接口**

在 `SPCAlarmListResponse` 之后追加：

```typescript
export interface ControlLimitSnapshot {
  snapshot_id: string;
  ic_id: string;
  ucl: number;
  lcl: number;
  cl: number;
  r_ucl?: number;
  r_lcl?: number;
  r_cl?: number;
  version_no: number;
  is_active: boolean;
  is_locked: boolean;
  calculated_at: string;
  created_at: string;
}
```

- [ ] **Step 5: 扩展 ChartDataResponse 类型**

在 `total_batches: number;` 之后追加：

```typescript
  active_snapshot?: ControlLimitSnapshot;
```

- [ ] **Step 6: 验证编译**

```bash
cd frontend
npx tsc --noEmit 2>&1 | head -20
```

Expected: 无 spc.ts 相关错误

- [ ] **Step 7: Commit**

```bash
git add frontend/src/types/spc.ts
git commit -m "feat(spc): extend TypeScript types for attribute charts and snapshot versioning"
```

---

## Task 8: 前端 API 客户端扩展

**Files:**
- Modify: `frontend/src/api/spc.ts`

- [ ] **Step 1: 新增 getSnapshots 函数**

在 `frontend/src/api/spc.ts` 末尾追加：

```typescript
export async function getSnapshots(icId: string): Promise<ControlLimitSnapshot[]> {
  const resp = await client.get(`/spc/inspection-characteristics/${icId}/snapshots`);
  return resp.data;
}

export async function activateSnapshot(icId: string, snapshotId: string): Promise<ControlLimitSnapshot> {
  const resp = await client.patch(`/spc/inspection-characteristics/${icId}/snapshots/${snapshotId}/activate`);
  return resp.data;
}
```

- [ ] **Step 2: 确认 import**

在 `frontend/src/api/spc.ts` 顶部，确认从类型文件导入了 `ControlLimitSnapshot`：

```typescript
import type { ..., ControlLimitSnapshot } from "../types/spc";
```

- [ ] **Step 3: 扩展 addSampleBatch — 支持计数值参数**

找到现有的 `addSampleBatch` 函数，将其 payload 类型扩展为：

```typescript
export async function addSampleBatch(
  icId: string,
  data: {
    batch_no: string;
    sampled_at: string;
    values?: number[];
    inspected_count?: number;
    defect_count?: number;
  }
): Promise<SampleBatch> {
  const resp = await client.post(`/spc/inspection-characteristics/${icId}/batches`, data);
  return resp.data;
}
```

- [ ] **Step 4: 验证编译**

```bash
cd frontend
npx tsc --noEmit 2>&1 | head -20
```

Expected: 无 spc.ts 或 api/spc.ts 相关错误

- [ ] **Step 5: Commit**

```bash
git add frontend/src/api/spc.ts
git commit -m "feat(spc): add getSnapshots, activateSnapshot API functions; extend addSampleBatch"
```

---

## Task 9: 前端 — 扩展 CreateModal（SPCListPage）

**Files:**
- Modify: `frontend/src/pages/spc/SPCListPage.tsx`

- [ ] **Step 1: 扩展 chartTypeLabels**

找到：

```typescript
const chartTypeLabels: Record<string, string> = {
  xbar_r: "X-bar R",
  imr: "I-MR",
  histogram: "直方图",
};
```

替换为：

```typescript
const chartTypeLabels: Record<string, string> = {
  xbar_r: "X-bar R（均值-极差图）",
  imr: "I-MR（单值移动极差图）",
  histogram: "直方图",
  p: "P图（不合格率）",
  np: "NP图（不合格品数）",
  c: "C图（缺陷数）",
  u: "U图（单位缺陷数）",
};
```

- [ ] **Step 2: 在 CreateModal 中 chart_type Select 新增4个选项**

找到 `chart_type` 的 `<Select.Option>` 列表，追加：

```tsx
<Select.Option value="p">P图（不合格率）</Select.Option>
<Select.Option value="np">NP图（不合格品数）</Select.Option>
<Select.Option value="c">C图（缺陷数）</Select.Option>
<Select.Option value="u">U图（单位缺陷数）</Select.Option>
```

- [ ] **Step 3: subgroup_size 字段条件渲染**

在 CreateModal 顶部添加 `chartType` 状态：

```tsx
const [createChartType, setCreateChartType] = useState<string>("xbar_r");
```

在 `chart_type` 的 `<Form.Item>` 中，给 `<Select>` 添加 `onChange`：

```tsx
<Select onChange={(v) => setCreateChartType(v)}>
```

将 `subgroup_size` 的 `<Form.Item>` 包裹在条件中：

```tsx
{!["p", "np", "c", "u"].includes(createChartType) && (
  <Form.Item name="subgroup_size" label="子组大小" initialValue={5}>
    <InputNumber min={1} max={10} style={{ width: "100%" }} />
  </Form.Item>
)}
```

- [ ] **Step 4: NP/C 图固定样本量提示**

在 `subgroup_size` 条件块之后（但仍在 Form 内）添加：

```tsx
{["np", "c"].includes(createChartType) && (
  <Form.Item>
    <Alert
      type="info"
      showIcon
      message="使用 NP图/C图 时，每批次样本量需保持固定一致"
    />
  </Form.Item>
)}
```

确保 `Alert` 已从 antd 导入。

- [ ] **Step 5: 扩展 handleCreate — 计数值图不传 subgroup_size**

将现有 `handleCreate` 中：

```typescript
subgroup_size: values.subgroup_size || (values.chart_type === "xbar_r" ? 5 : 1),
```

替换为：

```typescript
subgroup_size: ["p", "np", "c", "u"].includes(values.chart_type)
  ? 0
  : values.subgroup_size || (values.chart_type === "xbar_r" ? 5 : 1),
```

- [ ] **Step 6: 验证编译**

```bash
cd frontend
npx tsc --noEmit 2>&1 | head -20
```

Expected: 无 SPCListPage 相关错误

- [ ] **Step 7: Commit**

```bash
git add frontend/src/pages/spc/SPCListPage.tsx
git commit -m "feat(spc): extend CreateModal with P/NP/C/U chart types and conditional subgroup_size"
```

---

## Task 10: 前端 — 扩展 AddBatch + 图表渲染（SPCDetailPage）

**Files:**
- Modify: `frontend/src/pages/spc/SPCDetailPage.tsx`

- [ ] **Step 1: 新增计数值录入状态变量**

在现有的 `batchNo`, `sampledAt`, `sampleValues` state 之后追加：

```typescript
const [inspectedCount, setInspectedCount] = useState<string>("");
const [defectCount, setDefectCount] = useState<string>("");
```

- [ ] **Step 2: 扩展 handleAddSample — 支持计数值图**

将现有 `handleAddSample` 函数替换为：

```typescript
const handleAddSample = async () => {
  if (!id || !ic) return;
  if (!batchNo.trim()) {
    message.warning("请输入批次号");
    return;
  }
  const attributeCharts = ["p", "np", "c", "u"];
  const isAttribute = attributeCharts.includes(ic.chart_type);

  try {
    if (isAttribute) {
      const n = parseInt(inspectedCount, 10);
      const d = parseInt(defectCount, 10);
      if (isNaN(n) || n <= 0) { message.warning("请输入有效的检验数量"); return; }
      if (isNaN(d) || d < 0) { message.warning("请输入有效的不合格品/缺陷数"); return; }
      if (d > n) { message.warning("不合格品/缺陷数不能超过检验数量"); return; }
      await addSampleBatch(id, {
        batch_no: batchNo.trim(),
        sampled_at: sampledAt ? sampledAt.format("YYYY-MM-DDTHH:mm:ss") : dayjs().format("YYYY-MM-DDTHH:mm:ss"),
        inspected_count: n,
        defect_count: d,
      });
    } else {
      const values = sampleValues.map((v) => parseFloat(v)).filter((v) => !isNaN(v));
      if (values.length === 0) { message.warning("请输入至少一个样本值"); return; }
      await addSampleBatch(id, {
        batch_no: batchNo.trim(),
        sampled_at: sampledAt ? sampledAt.format("YYYY-MM-DDTHH:mm:ss") : dayjs().format("YYYY-MM-DDTHH:mm:ss"),
        values,
      });
    }
    message.success("样本录入成功");
    setBatchNo("");
    setSampleValues([""]);
    setInspectedCount("");
    setDefectCount("");
    fetchAll();
  } catch {
    message.error("录入失败");
  }
};
```

- [ ] **Step 3: 在录入 UI 区域条件渲染计数值输入框**

找到现有的测量值输入 UI（`sampleValues.map(...)` 部分），将整个输入区域包裹在条件判断中。计量值图保持不变，计数值图显示两个 InputNumber：

```tsx
{["p", "np", "c", "u"].includes(ic?.chart_type || "") ? (
  <Space direction="vertical" style={{ width: "100%" }}>
    <InputNumber
      placeholder="检验数量 n"
      value={inspectedCount ? parseInt(inspectedCount) : undefined}
      onChange={(v) => setInspectedCount(v?.toString() ?? "")}
      min={1}
      style={{ width: "100%" }}
    />
    <InputNumber
      placeholder="不合格品数 / 缺陷数"
      value={defectCount ? parseInt(defectCount) : undefined}
      onChange={(v) => setDefectCount(v?.toString() ?? "")}
      min={0}
      style={{ width: "100%" }}
    />
  </Space>
) : (
  /* 现有的计量值输入区域，保持不变 */
  <>
    {sampleValues.map((v, idx) => (
      // 现有 InputNumber 代码，保持不变
    ))}
  </>
)}
```

- [ ] **Step 4: 扩展控制图渲染 — P/U 图变控制线**

找到 `mainOption` 中 `markLine.data` 数组（现有水平线渲染），将其替换为条件渲染：

```typescript
const isVariableLimit = ["p", "u"].includes(chartData.chart_type);

// 主图 series
const mainSeries: echarts.SeriesOption[] = [
  {
    name: "统计值",
    type: "line",
    data: chartData.data_points.map((dp) => dp.x_value),
    symbol: "circle",
    markLine: isVariableLimit ? undefined : {
      silent: true,
      data: [
        ...(chartData.limits.ucl !== undefined
          ? [{ yAxis: chartData.limits.ucl, name: "UCL", lineStyle: { color: "#ff4d4f" } }]
          : []),
        ...(chartData.limits.lcl !== undefined
          ? [{ yAxis: chartData.limits.lcl, name: "LCL", lineStyle: { color: "#ff4d4f" } }]
          : []),
        ...(chartData.limits.cl !== undefined
          ? [{ yAxis: chartData.limits.cl, name: "CL", lineStyle: { color: "#52c41a", type: "dashed" as const } }]
          : []),
      ],
    },
  },
  // P/U 图：UCL/LCL 作为折线 series 添加
  ...(isVariableLimit && chartData.limits.ucl_list ? [{
    name: "UCL",
    type: "line" as const,
    data: chartData.limits.ucl_list,
    lineStyle: { color: "#ff4d4f", type: "dashed" as const },
    symbol: "none",
  }] : []),
  ...(isVariableLimit && chartData.limits.lcl_list ? [{
    name: "LCL",
    type: "line" as const,
    data: chartData.limits.lcl_list,
    lineStyle: { color: "#ff4d4f", type: "dashed" as const },
    symbol: "none",
  }] : []),
  ...(isVariableLimit && chartData.limits.cl !== undefined ? [{
    name: "CL",
    type: "line" as const,
    data: chartData.data_points.map(() => chartData.limits.cl),
    lineStyle: { color: "#52c41a", type: "dashed" as const },
    symbol: "none",
  }] : []),
];
```

在 `mainOption` 的 `series` 字段使用 `mainSeries`。

- [ ] **Step 5: 计数值图隐藏能力分析面板**

找到渲染 Cp/Cpk 能力分析的 Tab panel 或 Card，在其外层添加：

```tsx
{!["p", "np", "c", "u"].includes(ic?.chart_type || "") && (
  /* 能力分析面板的完整 JSX */
)}
```

- [ ] **Step 6: 更新 chart_type 标签渲染**

找到现有的 `ic.chart_type === "xbar_r" ? "X-bar R" : ic.chart_type === "imr" ? "I-MR" : "直方图"` 表达式，替换为：

```tsx
{chartTypeLabels[ic.chart_type] || ic.chart_type}
```

并在文件顶部（组件外）定义：

```typescript
const chartTypeLabels: Record<string, string> = {
  xbar_r: "X-bar R",
  imr: "I-MR",
  histogram: "直方图",
  p: "P图",
  np: "NP图",
  c: "C图",
  u: "U图",
};
```

- [ ] **Step 7: 验证编译**

```bash
cd frontend
npx tsc --noEmit 2>&1 | head -20
```

Expected: 无 SPCDetailPage 相关错误

- [ ] **Step 8: Commit**

```bash
git add frontend/src/pages/spc/SPCDetailPage.tsx
git commit -m "feat(spc): extend AddBatch for attribute charts, variable control limit rendering, hide capability panel"
```

---

## Task 11: 前端 — VersionPanel 组件

**Files:**
- Create: `frontend/src/pages/spc/VersionPanel.tsx`
- Modify: `frontend/src/pages/spc/SPCDetailPage.tsx`

- [ ] **Step 1: 创建 VersionPanel 组件**

```typescript
// frontend/src/pages/spc/VersionPanel.tsx
import React, { useEffect, useState } from "react";
import { Drawer, List, Tag, Button, message, Badge } from "antd";
import { HistoryOutlined } from "@ant-design/icons";
import dayjs from "dayjs";
import type { ControlLimitSnapshot } from "../../types/spc";
import { getSnapshots, activateSnapshot } from "../../api/spc";

interface VersionPanelProps {
  icId: string;
  onActivated: () => void; // callback to refresh chart after activation
}

const VersionPanel: React.FC<VersionPanelProps> = ({ icId, onActivated }) => {
  const [open, setOpen] = useState(false);
  const [snapshots, setSnapshots] = useState<ControlLimitSnapshot[]>([]);
  const [loading, setLoading] = useState(false);

  const fetchSnapshots = async () => {
    setLoading(true);
    try {
      const data = await getSnapshots(icId);
      setSnapshots(data);
    } catch {
      message.error("加载版本列表失败");
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => {
    if (open) fetchSnapshots();
  }, [open, icId]);

  const handleActivate = async (snapshotId: string) => {
    try {
      await activateSnapshot(icId, snapshotId);
      message.success("已切换控制限版本");
      fetchSnapshots();
      onActivated();
    } catch {
      message.error("切换失败");
    }
  };

  return (
    <>
      <Button icon={<HistoryOutlined />} onClick={() => setOpen(true)}>
        版本管理
      </Button>
      <Drawer
        title="控制限版本"
        open={open}
        onClose={() => setOpen(false)}
        width={400}
      >
        <List
          loading={loading}
          dataSource={snapshots}
          renderItem={(snap) => (
            <List.Item
              style={{ background: snap.is_active ? "#f0f5ff" : undefined, borderRadius: 4 }}
              actions={[
                snap.is_active ? (
                  <Tag color="blue">当前</Tag>
                ) : (
                  <Button size="small" onClick={() => handleActivate(snap.snapshot_id)}>
                    设为当前
                  </Button>
                ),
              ]}
            >
              <List.Item.Meta
                title={
                  <span>
                    <Badge
                      count={`v${snap.version_no}`}
                      style={{ backgroundColor: snap.is_active ? "#1677ff" : "#8c8c8c" }}
                    />
                    <span style={{ marginLeft: 8 }}>
                      UCL: {snap.ucl} / LCL: {snap.lcl}
                    </span>
                  </span>
                }
                description={dayjs(snap.calculated_at).format("YYYY-MM-DD HH:mm")}
              />
            </List.Item>
          )}
          locale={{ emptyText: "暂无锁定的控制限版本" }}
        />
      </Drawer>
    </>
  );
};

export default VersionPanel;
```

- [ ] **Step 2: 在 SPCDetailPage 中引入并使用 VersionPanel**

在 `SPCDetailPage.tsx` 顶部 import 区域添加：

```typescript
import VersionPanel from "./VersionPanel";
```

找到详情页右上角的操作按钮区域（锁定/解锁按钮附近），添加 `VersionPanel`：

```tsx
<VersionPanel icId={id} onActivated={fetchAll} />
```

- [ ] **Step 3: 在控制图标题旁显示当前版本徽标**

在 `chartData` 加载后，读取 `chartData.active_snapshot?.version_no`，在图表标题旁显示：

```tsx
{chartData?.active_snapshot && (
  <Tag color="blue" style={{ marginLeft: 8 }}>
    控制限 v{chartData.active_snapshot.version_no}
  </Tag>
)}
```

- [ ] **Step 4: 验证编译**

```bash
cd frontend
npx tsc --noEmit 2>&1 | head -20
```

Expected: 无错误

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/spc/VersionPanel.tsx frontend/src/pages/spc/SPCDetailPage.tsx
git commit -m "feat(spc): add VersionPanel component for control limit version management"
```

---

## Task 12: 整合验证

**Files:** 无新增文件

- [ ] **Step 1: 后端语法全量检查**

```bash
cd backend
python -c "
from app.models.spc import SampleBatch, ControlLimitSnapshot
from app.services.spc_calculation_engine import calculate_p_limits, calculate_np_limits, calculate_c_limits, calculate_u_limits
from app.services.spc_service import list_snapshots, activate_snapshot
from app.schemas.spc import ControlLimitSnapshotOut, SampleBatchCreate, ChartDataResponse
from app.api.spc import router
print('ALL OK')
"
```

Expected: `ALL OK`

- [ ] **Step 2: 运行全部计算引擎测试**

```bash
cd backend
python -m pytest app/test_spc_engine.py -v
```

Expected: 全部 PASS（原有测试 + Task 3 新增测试）

- [ ] **Step 3: 前端全量编译**

```bash
cd frontend
npx tsc --noEmit 2>&1
```

Expected: 0 errors

- [ ] **Step 4: 更新 ROADMAP**

在 `docs/ROADMAP.md` 中，将：

```
| SPC 计数值控制图 (v1.1) | P1 | 🔲 待开发 | P图 / NP图 / C图 / U图 |
| SPC 控制限多版本 (v1.1) | P1 | 🔲 待开发 | 保存和切换历史控制限版本 |
```

改为：

```
| SPC 计数值控制图 (v1.1) | P1 | ✅ 完成 | P图 / NP图 / C图 / U图 |
| SPC 控制限多版本 (v1.1) | P1 | ✅ 完成 | 自动版本号 + Drawer 切换 |
```

- [ ] **Step 5: Final commit**

```bash
git add docs/ROADMAP.md
git commit -m "docs: mark SPC v1.1 attribute charts and limit versioning as complete"
```

