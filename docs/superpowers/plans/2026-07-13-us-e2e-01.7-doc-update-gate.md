# US-E2E-01.7 D8 文档更新审核门禁 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 8D 状态机 `D8_GATE_PENDING→D8_APPROVAL_PENDING` 边接入文档更新审核门禁：LLM 影响分析识别受影响 CP/FMEA + 同步审核版本 bump & 关键点覆盖 + 门禁阻断/放行 + 延期仍阻断。

**Architecture:** 镜像 D3 遏制模式（`capa_d3_containment_service.py` 三阶段事务）。3 张新表 `capa_docg_{analysis,audit,decision}` + 单一 `capa_doc_gate_service.py`（`generate_impact_analysis` 三阶段 / `run_audit` / `record_defer` / `confirm_no_affected`）+ 7 路由 + `advance_capa` gate 接入 + `DocGatePanel.tsx` 前端 + E2E。

**Tech Stack:** Python 3.11 + FastAPI + SQLAlchemy 2.0 async + Alembic | React 18 + TS 5.6 + Ant Design 5 | provider_adapter（openai/anthropic SDK）

**Spec:** `docs/superpowers/specs/2026-07-13-us-e2e-01.7-doc-update-gate-design.md`（commit `bc36bea0`，6 轮 review 终审通过）

## Global Constraints

- **C1 窄范围**：只审 control_plan + fmea（系统仅此两类有版本表）；`doc_type` 枚举保留 5 值但实际只产出此两类
- **C2 显式 POST**：LLM 走独立 `POST /impact` + `GET /impact`，不进 `advance_capa`；门禁 edge 只跑同步 DB 查询审核
- **C3 版本间 diff**：用 `diff_engine.diff_fmea_graphs`/`diff_cp_items`（非 `build_sync_preview`），覆盖 added/deleted/modified
- **C7 allowlist**：LLM doc_id 不可信，服务层按 `capa.factory_id`+`product_line_code` 建文档 allowlist + 每 doc 的 existing_targets/add_anchors；越界 → failed
- **C8/C9 双层新鲜度**：C8 文档版本（version_snapshot 重核 sha256）；C9 输入指纹（CAPA 语义+候选身份集+baseline，**不含 latest**）
- **三阶段事务**：phase1 lock+demote+stale-recovery+running+commit / phase2 查询→纯 dict→commit→无事务 LLM / phase3 re-lock+C9重算+统一CAS
- **统一 CAS**：成功/失败/异常都 `WHERE analysis_id=? AND attempt_token=? AND status='running'`，rowcount==0 → superseded 不覆盖不重复审计
- **防 vacuous pass**：done CHECK + 应用层要求每项 key_points ≥1 + update_suggestion 非空；空清单 run_audit 守卫 raise
- **key_point 判别联合**：modify/delete 用 target_key，add 用 add_anchor（parent_node_id+node_type+business_key），二者互斥；document 仅 baseline=NULL+add
- **完整 SHA-256 存储**：sha256_8 仅 UI
- **测试命令**：`cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate*.py tests/capa/test_doc_gate_migration.py -x --tb=short`；`cd frontend && npm run build`
- **AuditLog 写法**：`write_audit_raw(db, user_id=..., factory_id=..., tenant_schema=..., table_name="capa_eightd", record_id=capa.report_id, action=..., changed_fields=...)`，flush only，caller commit

---

## File Structure

**Create:**
- `backend/app/models/capa_doc_gate.py` — 3 ORM 表（CapaDocgAnalysis / CapaDocgAudit / CapaDocgDecision）
- `backend/app/schemas/capa_doc_gate.py` — Pydantic 请求/响应 schema
- `backend/app/services/capa_doc_gate_service.py` — 4 服务函数 + phase helpers
- `backend/alembic/versions/20260713_doc_gate.py` — 迁移
- `backend/tests/capa/test_capa_doc_gate_service.py`
- `backend/tests/capa/test_capa_doc_gate_api.py`
- `backend/tests/capa/test_doc_gate_migration.py`
- `backend/tests/capa/test_capa_doc_gate_coverage.py`
- `backend/tests/capa/test_capa_doc_gate_freshness.py`
- `backend/tests/capa/test_capa_doc_gate_target_allowlist.py`
- `backend/tests/capa/test_capa_doc_gate_concurrency.py`
- `frontend/src/components/capa/DocGatePanel.tsx`
- `frontend/e2e/specs/m1-core/capa-story-doc-gate.spec.ts`

**Modify:**
- `backend/app/models/__init__.py` — 注册 3 模型
- `backend/app/services/capa_service.py` — `advance_capa` D8_GATE_PENDING→D8_APPROVAL_PENDING edge 接入 `_d8_doc_gate_gate`
- `backend/app/api/capa.py` — 7 路由 + scope helper
- `frontend/src/api/capa.ts` — 7 API client 函数
- `frontend/src/pages/capa/CAPADetailPage.tsx:492` — 全局推进按钮排除 D8_GATE_PENDING + 渲染 DocGatePanel
- `frontend/src/locales/{zh-CN,en-US}/capa.json` — i18n
- `backend/app/seed_e2e.py` + `seed_e2e_constants.py` — D8_GATE_PENDING seed

---

## Task 1: 迁移 + ORM 模型（3 表）

**Files:**
- Create: `backend/app/models/capa_doc_gate.py`
- Create: `backend/alembic/versions/20260713_doc_gate.py`
- Modify: `backend/app/models/__init__.py`
- Test: `backend/tests/capa/test_doc_gate_migration.py`

**Interfaces:**
- Produces: `CapaDocgAnalysis`, `CapaDocgAudit`, `CapaDocgDecision` ORM 类（供 Task 2+ 导入）

- [ ] **Step 1: 写迁移失败测试**

`backend/tests/capa/test_doc_gate_migration.py`:
```python
"""Migration test for capa_docg_* tables (US-E2E-01.7)."""
import pytest
from sqlalchemy import text


@pytest.mark.asyncio
async def test_upgrade_creates_doc_gate_tables(mig_db):
    """alembic upgrade head creates capa_docg_analysis/audit/decision."""
    tables = await mig_db.execute(text(
        "SELECT table_name FROM information_schema.tables "
        "WHERE table_name LIKE 'capa_docg_%' ORDER BY table_name"
    ))
    names = [r[0] for r in tables]
    assert "capa_docg_analysis" in names
    assert "capa_docg_audit" in names
    assert "capa_docg_decision" in names


@pytest.mark.asyncio
async def test_analysis_running_partial_uq(mig_db):
    """uq_docg_analysis_running: at most one running per capa_id."""
    # Attempting two running rows for same capa_id should violate partial-UQ.
    await mig_db.execute(text(
        "INSERT INTO factories (id, name, code) VALUES "
        "('11111111-1111-1111-1111-111111111111', 'F', 'FCODE')"
    ))
    await mig_db.execute(text(
        "INSERT INTO users (user_id, username, password_hash, is_active) VALUES "
        "('22222222-2222-2222-2222-222222222222', 'u', 'h', true)"
    ))
    await mig_db.execute(text(
        "INSERT INTO capa_eightd (report_id, document_no, title, factory_id, status, created_by) "
        "VALUES ('33333333-3333-3333-3333-333333333333', '8D-T-001', 't', "
        "'11111111-1111-1111-1111-111111111111', 'D8_GATE_PENDING', "
        "'22222222-2222-2222-2222-222222222222')"
    ))
    base = (
        "INSERT INTO capa_docg_analysis (analysis_id, capa_id, factory_id, is_current, status, "
        "attempt_token, started_at, llm_available, generated_by, generated_at, created_at) VALUES "
        "(gen_random_uuid(), '33333333-3333-3333-3333-333333333333', "
        "'11111111-1111-1111-1111-111111111111', false, 'running', gen_random_uuid(), now(), false, "
        "'22222222-2222-2222-2222-222222222222', now(), now())"
    )
    await mig_db.execute(text(base))
    with pytest.raises(Exception, match="uq_docg_analysis_running"):
        await mig_db.execute(text(base))
        await mig_db.commit()
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_doc_gate_migration.py -x --tb=short`
Expected: FAIL（表不存在）

- [ ] **Step 3: 写迁移文件**

`backend/alembic/versions/20260713_doc_gate.py`:
```python
"""capa_docg_* tables for US-E2E-01.7 D8 doc update gate."""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import JSONB, UUID

revision = "20260713_doc_gate"
down_revision = "<查 git log 最近一个迁移的 revision>"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "capa_docg_analysis",
        sa.Column("analysis_id", UUID(as_uuid=True), primary_key=True),
        sa.Column("capa_id", UUID(as_uuid=True), sa.ForeignKey("capa_eightd.report_id", ondelete="RESTRICT"), nullable=False),
        sa.Column("factory_id", UUID(as_uuid=True), sa.ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False),
        sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.text("false")),
        sa.Column("status", sa.String(10), nullable=False, server_default="running"),
        sa.Column("affected_docs", JSONB),
        sa.Column("analysis_input_hash", sa.String(64)),
        sa.Column("llm_available", sa.Boolean, nullable=False, server_default=sa.text("false")),
        sa.Column("model", sa.String(40)),
        sa.Column("prompt_stats", JSONB),
        sa.Column("error", sa.Text),
        sa.Column("attempt_token", UUID(as_uuid=True), nullable=False),
        sa.Column("started_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
        sa.Column("completed_at", sa.DateTime(timezone=True)),
        sa.Column("generated_by", UUID(as_uuid=True), sa.ForeignKey("users.user_id", ondelete="RESTRICT"), nullable=False),
        sa.Column("generated_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
        sa.UniqueConstraint("capa_id", "factory_id", name="uq_docg_analysis_capa_factory"),
        sa.CheckConstraint("status IN ('running','done','failed')", name="chk_docg_analysis_status"),
        sa.CheckConstraint(
            "(status='running' AND is_current=false AND completed_at IS NULL) "
            "OR (status='failed' AND is_current=false AND completed_at IS NOT NULL) "
            "OR (status='done' AND completed_at IS NOT NULL)",
            name="chk_docg_analysis_status_state",
        ),
        sa.CheckConstraint(
            "status!='done' OR (affected_docs IS NOT NULL AND analysis_input_hash IS NOT NULL "
            "AND llm_available=true AND completed_at IS NOT NULL)",
            name="chk_docg_analysis_done_complete",
        ),
    )
    op.create_index("uq_docg_analysis_current", "capa_docg_analysis", ["capa_id"],
                    unique=True, postgresql_where=sa.text("is_current = true"))
    op.create_index("uq_docg_analysis_running", "capa_docg_analysis", ["capa_id"],
                    unique=True, postgresql_where=sa.text("status = 'running'"))

    op.create_table(
        "capa_docg_audit",
        sa.Column("audit_id", UUID(as_uuid=True), primary_key=True),
        sa.Column("analysis_id", UUID(as_uuid=True), sa.ForeignKey("capa_docg_analysis.analysis_id", ondelete="RESTRICT"), nullable=False),
        sa.Column("audit_run_id", UUID(as_uuid=True), nullable=False),
        sa.Column("factory_id", UUID(as_uuid=True), sa.ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False),
        sa.Column("doc_type", sa.String(16), nullable=False),
        sa.Column("doc_id", UUID(as_uuid=True), nullable=False),
        sa.Column("doc_name", sa.Text, nullable=False),
        sa.Column("status", sa.String(16), nullable=False),
        sa.Column("version_before", JSONB),
        sa.Column("version_after", JSONB),
        sa.Column("version_bump", sa.Boolean, nullable=False, server_default=sa.text("false")),
        sa.Column("coverage", JSONB, server_default=sa.text("'[]'")),
        sa.Column("covered_count", sa.Integer, nullable=False, server_default="0"),
        sa.Column("total_count", sa.Integer, nullable=False, server_default="0"),
        sa.Column("audited_by", UUID(as_uuid=True), sa.ForeignKey("users.user_id", ondelete="RESTRICT"), nullable=False),
        sa.Column("audited_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
        sa.CheckConstraint("doc_type IN ('control_plan','fmea','sop','inspection_sop','other')", name="chk_docg_audit_doctype"),
        sa.CheckConstraint("status IN ('passed','pending_update','incomplete')", name="chk_docg_audit_status"),
        sa.UniqueConstraint("audit_run_id", "doc_type", "doc_id", name="uq_docg_audit_run_doc"),
    )

    op.create_table(
        "capa_docg_decision",
        sa.Column("decision_id", UUID(as_uuid=True), primary_key=True),
        sa.Column("analysis_id", UUID(as_uuid=True), sa.ForeignKey("capa_docg_analysis.analysis_id", ondelete="RESTRICT"), nullable=False),
        sa.Column("audit_run_id", UUID(as_uuid=True)),
        sa.Column("revision", sa.Integer, nullable=False, server_default="0"),
        sa.Column("factory_id", UUID(as_uuid=True), sa.ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False),
        sa.Column("decision", sa.String(10), nullable=False),
        sa.Column("no_affected_confirmed", sa.Boolean, nullable=False, server_default=sa.text("false")),
        sa.Column("version_snapshot", JSONB, server_default=sa.text("'[]'")),
        sa.Column("defer_reason", sa.Text),
        sa.Column("defer_owner", UUID(as_uuid=True), sa.ForeignKey("users.user_id")),
        sa.Column("defer_deadline", sa.Date),
        sa.Column("decided_by", UUID(as_uuid=True), sa.ForeignKey("users.user_id", ondelete="RESTRICT"), nullable=False),
        sa.Column("decided_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
        sa.CheckConstraint("decision IN ('passed','blocked','deferred')", name="chk_docg_decision"),
        sa.CheckConstraint(
            "(decision='deferred' AND defer_reason IS NOT NULL AND defer_owner IS NOT NULL AND defer_deadline IS NOT NULL) "
            "OR (decision IN ('passed','blocked') AND defer_reason IS NULL AND defer_owner IS NULL AND defer_deadline IS NULL)",
            name="chk_docg_decision_defer",
        ),
        sa.UniqueConstraint("analysis_id", "revision", name="uq_docg_decision_analysis_revision"),
    )


def downgrade() -> None:
    op.drop_table("capa_docg_decision")
    op.drop_table("capa_docg_audit")
    op.drop_index("uq_docg_analysis_running", table_name="capa_docg_analysis")
    op.drop_index("uq_docg_analysis_current", table_name="capa_docg_analysis")
    op.drop_table("capa_docg_analysis")
```

- [ ] **Step 4: 写 ORM 模型**

`backend/app/models/capa_doc_gate.py`:
```python
"""D8 doc update gate ORM models (US-E2E-01.7). 3 tables mirroring D3 pattern."""
import uuid
from datetime import date, datetime

from sqlalchemy import Boolean, CheckConstraint, Date, DateTime, ForeignKey, Integer, String, Text, UniqueConstraint, func, text
from sqlalchemy.dialects.postgresql import JSONB, UUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.schema import Index

from app.database import Base


class CapaDocgAnalysis(Base):
    __tablename__ = "capa_docg_analysis"
    __table_args__ = (
        UniqueConstraint("capa_id", "factory_id", name="uq_docg_analysis_capa_factory"),
        CheckConstraint("status IN ('running','done','failed')", name="chk_docg_analysis_status"),
        CheckConstraint(
            "(status='running' AND is_current=false AND completed_at IS NULL) "
            "OR (status='failed' AND is_current=false AND completed_at IS NOT NULL) "
            "OR (status='done' AND completed_at IS NOT NULL)",
            name="chk_docg_analysis_status_state",
        ),
        CheckConstraint(
            "status!='done' OR (affected_docs IS NOT NULL AND analysis_input_hash IS NOT NULL "
            "AND llm_available=true AND completed_at IS NOT NULL)",
            name="chk_docg_analysis_done_complete",
        ),
        Index("uq_docg_analysis_current", "capa_id", unique=True, postgresql_where=text("is_current = true")),
        Index("uq_docg_analysis_running", "capa_id", unique=True, postgresql_where=text("status = 'running'")),
    )
    analysis_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    capa_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("capa_eightd.report_id", ondelete="RESTRICT"), nullable=False)
    factory_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False)
    is_current: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    status: Mapped[str] = mapped_column(String(10), nullable=False, default="running")
    affected_docs: Mapped[list | None] = mapped_column(JSONB)
    analysis_input_hash: Mapped[str | None] = mapped_column(String(64))
    llm_available: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    model: Mapped[str | None] = mapped_column(String(40))
    prompt_stats: Mapped[dict | None] = mapped_column(JSONB)
    error: Mapped[str | None] = mapped_column(Text)
    attempt_token: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), nullable=False, default=uuid.uuid4)
    started_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    completed_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    generated_by: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("users.user_id", ondelete="RESTRICT"), nullable=False)
    generated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())


class CapaDocgAudit(Base):
    __tablename__ = "capa_docg_audit"
    __table_args__ = (
        CheckConstraint("doc_type IN ('control_plan','fmea','sop','inspection_sop','other')", name="chk_docg_audit_doctype"),
        CheckConstraint("status IN ('passed','pending_update','incomplete')", name="chk_docg_audit_status"),
        UniqueConstraint("audit_run_id", "doc_type", "doc_id", name="uq_docg_audit_run_doc"),
    )
    audit_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    analysis_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("capa_docg_analysis.analysis_id", ondelete="RESTRICT"), nullable=False)
    audit_run_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), nullable=False, default=uuid.uuid4)
    factory_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False)
    doc_type: Mapped[str] = mapped_column(String(16), nullable=False)
    doc_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), nullable=False)
    doc_name: Mapped[str] = mapped_column(Text, nullable=False)
    status: Mapped[str] = mapped_column(String(16), nullable=False)
    version_before: Mapped[dict | None] = mapped_column(JSONB)
    version_after: Mapped[dict | None] = mapped_column(JSONB)
    version_bump: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    coverage: Mapped[list] = mapped_column(JSONB, default=list)
    covered_count: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    total_count: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    audited_by: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("users.user_id", ondelete="RESTRICT"), nullable=False)
    audited_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())


class CapaDocgDecision(Base):
    __tablename__ = "capa_docg_decision"
    __table_args__ = (
        CheckConstraint("decision IN ('passed','blocked','deferred')", name="chk_docg_decision"),
        CheckConstraint(
            "(decision='deferred' AND defer_reason IS NOT NULL AND defer_owner IS NOT NULL AND defer_deadline IS NOT NULL) "
            "OR (decision IN ('passed','blocked') AND defer_reason IS NULL AND defer_owner IS NULL AND defer_deadline IS NULL)",
            name="chk_docg_decision_defer",
        ),
        UniqueConstraint("analysis_id", "revision", name="uq_docg_decision_analysis_revision"),
    )
    decision_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    analysis_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("capa_docg_analysis.analysis_id", ondelete="RESTRICT"), nullable=False)
    audit_run_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True))
    revision: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    factory_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("factories.id", ondelete="RESTRICT"), nullable=False)
    decision: Mapped[str] = mapped_column(String(10), nullable=False)
    no_affected_confirmed: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    version_snapshot: Mapped[list] = mapped_column(JSONB, default=list)
    defer_reason: Mapped[str | None] = mapped_column(Text)
    defer_owner: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("users.user_id"))
    defer_deadline: Mapped[date | None] = mapped_column(Date)
    decided_by: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("users.user_id", ondelete="RESTRICT"), nullable=False)
    decided_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
```

- [ ] **Step 5: 注册模型到 `__init__.py`**

`backend/app/models/__init__.py` 在 `capa_d3` import 块后追加：
```python
from app.models.capa_doc_gate import (
    CapaDocgAnalysis,
    CapaDocgAudit,
    CapaDocgDecision,
)
```
并在 `__all__` 列表追加 `"CapaDocgAnalysis", "CapaDocgAudit", "CapaDocgDecision"`。

- [ ] **Step 6: 运行迁移测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_doc_gate_migration.py -x --tb=short`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add backend/app/models/capa_doc_gate.py backend/alembic/versions/20260713_doc_gate.py backend/app/models/__init__.py backend/tests/capa/test_doc_gate_migration.py
git commit -m "feat(01.7): capa_docg_* 3 表迁移 + ORM 模型（analysis/audit/decision，running partial-UQ + 状态机 CHECK + done 完备性 + revision UQ）"
```

---

## Task 2: 服务层 — `generate_impact_analysis` 三阶段（BLOCKED + CAS + stale recovery）

**Files:**
- Create: `backend/app/services/capa_doc_gate_service.py`
- Test: `backend/tests/capa/test_capa_doc_gate_service.py`

**Interfaces:**
- Consumes: `provider_adapter.build_client`/`complete_json`（签名见 `backend/app/services/agent/provider_adapter.py:40/155`），`write_audit_raw`（`backend/app/services/agent/audit.py:11`），`version_service.get_latest_cp_version`/`get_latest_fmea_version`
- Produces: `generate_impact_analysis(db, capa, user_id) -> dict`（返回 `{status: 'done'|'failed'|'blocked'|'running'|'superseded', analysis_id?, ...}`）

- [ ] **Step 1: 写 BLOCKED 失败测试**

`backend/tests/capa/test_capa_doc_gate_service.py`:
```python
"""Tests for capa_doc_gate_service.generate_impact_analysis (US-E2E-01.7)."""
import uuid
import pytest
from unittest.mock import AsyncMock, patch

from app.services import capa_doc_gate_service
from app.models.capa_doc_gate import CapaDocgAnalysis


@pytest.mark.asyncio
async def test_generate_impact_analysis_blocked_when_no_llm(db, capa_d8_gate):
    """No LLM credentials -> status='failed'+blocked, is_current not set, raises BLOCKED."""
    capa, user = capa_d8_gate
    with patch("app.services.capa_doc_gate_service.build_client",
               AsyncMock(side_effect=ProviderNotConfiguredError("no llm"))):
        from app.services.agent.provider_adapter import ProviderNotConfiguredError
        with patch("app.services.capa_doc_gate_service.build_client",
                   AsyncMock(side_effect=ProviderNotConfiguredError("no llm"))):
            with pytest.raises(ValueError, match="BLOCKED"):
                await capa_doc_gate_service.generate_impact_analysis(db, capa, user.user_id)
    latest = await db.scalar(
        __import__("sqlalchemy").select(CapaDocgAnalysis).order_by(CapaDocgAnalysis.created_at.desc())
    )
    assert latest.status == "failed"
    assert latest.is_current is False
    assert latest.llm_available is False
    assert latest.completed_at is not None
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_service.py::test_generate_impact_analysis_blocked_when_no_llm -x`
Expected: FAIL（module/function not found）

- [ ] **Step 3: 实现 `generate_impact_analysis` + phase1（BLOCKED 路径）**

`backend/app/services/capa_doc_gate_service.py`:
```python
"""US-E2E-01.7 D8 doc update gate service. Mirrors capa_d3_containment_service three-phase pattern."""
from __future__ import annotations

import hashlib
import json
import logging
import uuid
from datetime import datetime, timezone

from sqlalchemy import select, update, text
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.capa_doc_gate import CapaDocgAnalysis, CapaDocgAudit, CapaDocgDecision
from app.models.capa import CAPAEightD
from app.models.audit import AuditLog
from app.services.agent.provider_adapter import (
    ProviderClient, ProviderNotConfiguredError, build_client, complete_json,
)

logger = logging.getLogger(__name__)
STALE_THRESHOLD_SECONDS = 300  # 5 min; running older than 2x = stale
RETRY_AFTER_SECONDS = 2


async def generate_impact_analysis(db: AsyncSession, capa: CAPAEightD, user_id: uuid.UUID) -> dict:
    """Three-phase: phase1 lock+demote+stale-recovery+running+commit; phase2 LLM; phase3 CAS."""
    p1 = await _phase1_create_running(db, capa, user_id)
    if p1["status"] in ("blocked", "failed", "running", "superseded"):
        return p1
    phase2 = await _phase2_llm(db, capa, p1)
    return await _phase3_cas(db, capa, p1, phase2, user_id)


async def _phase1_create_running(db: AsyncSession, capa: CAPAEightD, user_id: uuid.UUID) -> dict:
    # Lock capa row
    await db.execute(
        text("SELECT 1 FROM capa_eightd WHERE report_id=:cid FOR UPDATE"),
        {"cid": capa.report_id},
    )
    # Demote old current (fail-closed: before credential check)
    await db.execute(
        update(CapaDocgAnalysis)
        .where(CapaDocgAnalysis.capa_id == capa.report_id, CapaDocgAnalysis.is_current == True)
        .values(is_current=False)
    )
    # Stale recovery: CAS running older than 2x threshold -> failed
    cutoff = datetime.now(timezone.utc) - (STALE_THRESHOLD_SECONDS * 2) * 1_000_000  # placeholder; use timedelta below
    from datetime import timedelta
    cutoff = datetime.now(timezone.utc) - timedelta(seconds=STALE_THRESHOLD_SECONDS * 2)
    await db.execute(
        update(CapaDocgAnalysis)
        .where(CapaDocgAnalysis.capa_id == capa.report_id, CapaDocgAnalysis.status == "running",
               CapaDocgAnalysis.started_at < cutoff)
        .values(status="failed", error="stale", completed_at=datetime.now(timezone.utc))
    )
    # Existing running -> return retry_after
    existing = await db.scalar(
        select(CapaDocgAnalysis).where(
            CapaDocgAnalysis.capa_id == capa.report_id, CapaDocgAnalysis.status == "running"
        )
    )
    if existing:
        return {"status": "running", "analysis_id": str(existing.analysis_id),
                "retry_after": RETRY_AFTER_SECONDS}
    # Credential check
    try:
        client = await build_client(db)
    except ProviderNotConfiguredError:
        failed = CapaDocgAnalysis(
            analysis_id=uuid.uuid4(), capa_id=capa.report_id, factory_id=capa.factory_id,
            is_current=False, status="failed", error="LLM 未配置", llm_available=False,
            completed_at=datetime.now(timezone.utc), generated_by=user_id,
        )
        db.add(failed)
        await db.commit()
        raise ValueError("BLOCKED: 无 LLM 凭证")
    # Create running
    attempt_token = uuid.uuid4()
    analysis = CapaDocgAnalysis(
        analysis_id=uuid.uuid4(), capa_id=capa.report_id, factory_id=capa.factory_id,
        is_current=False, status="running", attempt_token=attempt_token,
        llm_available=False, generated_by=user_id,
    )
    db.add(analysis)
    await db.flush()
    await db.commit()
    return {"status": "phase1_done", "analysis_id": str(analysis.analysis_id),
            "attempt_token": attempt_token, "client": client}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_service.py::test_generate_impact_analysis_blocked_when_no_llm -x`
Expected: PASS

- [ ] **Step 5: 写 done 成功路径测试 + vacuous pass 拒绝测试**

追加到 `test_capa_doc_gate_service.py`:
```python
@pytest.mark.asyncio
async def test_generate_impact_analysis_done_success(db, capa_d8_gate_with_docs, llm_done_mock):
    """LLM returns valid affected_docs -> status='done', is_current=true, analysis_input_hash set."""
    capa, user = capa_d8_gate_with_docs
    result = await capa_doc_gate_service.generate_impact_analysis(db, capa, user.user_id)
    assert result["status"] == "done"
    analysis = await db.scalar(
        select(CapaDocgAnalysis).where(CapaDocgAnalysis.is_current == True)
    )
    assert analysis.status == "done"
    assert analysis.affected_docs is not None and len(analysis.affected_docs) > 0
    assert analysis.analysis_input_hash is not None
    assert analysis.llm_available is True


@pytest.mark.asyncio
async def test_generate_rejects_empty_key_points(db, capa_d8_gate_with_docs, llm_empty_keypoints_mock):
    """LLM returns doc with empty key_points -> status='failed' (vacuous pass prevention)."""
    capa, user = capa_d8_gate_with_docs
    result = await capa_doc_gate_service.generate_impact_analysis(db, capa, user.user_id)
    assert result["status"] == "failed"
    analysis = await db.scalar(
        select(CapaDocgAnalysis).where(CapaDocgAnalysis.status == "failed")
    )
    assert "key_points" in (analysis.error or "")
```

- [ ] **Step 6: 实现 phase2 + phase3 + 校验 + allowlist + hash**

`capa_doc_gate_service.py` 追加：
```python
async def _phase2_llm(db: AsyncSession, capa: CAPAEightD, p1: dict) -> dict:
    """Query candidates/targets -> pure dict -> commit read txn -> call LLM (no txn)."""
    candidates = await _build_allowlist(db, capa)  # list of {doc_type, doc_id, doc_name, baseline, targets, add_anchors}
    input_hash = _compute_input_hash(capa, candidates)
    prompt = _build_prompt(capa, candidates)
    pc = p1["client"]
    try:
        raw = await complete_json(pc, prompt, _RESPONSE_SCHEMA)
    except Exception as e:
        return {"llm_error": str(e)}
    raw["model"] = getattr(pc, "model", "unknown")
    raw["_input_hash"] = input_hash
    raw["_candidates"] = candidates
    return raw


async def _phase3_cas(db: AsyncSession, capa: CAPAEightD, p1: dict, phase2: dict, user_id: uuid.UUID) -> dict:
    analysis_id = uuid.UUID(p1["analysis_id"])
    attempt_token = p1["attempt_token"]
    now = datetime.now(timezone.utc)
    # LLM error -> CAS failed
    if "llm_error" in phase2:
        res = await db.execute(
            update(CapaDocgAnalysis)
            .where(CapaDocgAnalysis.analysis_id == analysis_id,
                   CapaDocgAnalysis.attempt_token == attempt_token, CapaDocgAnalysis.status == "running")
            .values(status="failed", error=phase2["llm_error"], completed_at=now)
        )
        await db.commit()
        return {"status": "failed"} if res.rowcount > 0 else {"status": "superseded"}
    # Re-lock + C9 recheck
    await db.execute(text("SELECT 1 FROM capa_eightd WHERE report_id=:cid FOR UPDATE"), {"cid": capa.report_id})
    candidates = phase2["_candidates"]
    cur_hash = _compute_input_hash(capa, candidates)
    if cur_hash != phase2["_input_hash"]:
        await db.execute(
            update(CapaDocgAnalysis)
            .where(CapaDocgAnalysis.analysis_id == analysis_id,
                   CapaDocgAnalysis.attempt_token == attempt_token, CapaDocgAnalysis.status == "running")
            .values(status="failed", error="input_changed", completed_at=now)
        )
        await db.commit()
        return {"status": "failed", "error": "input_changed"}
    # Validate LLM output (C7 + vacuous pass + allowlist)
    affected = _validate_and_backfill(phase2, candidates)
    if isinstance(affected, str):  # error
        res = await db.execute(
            update(CapaDocgAnalysis)
            .where(CapaDocgAnalysis.analysis_id == analysis_id,
                   CapaDocgAnalysis.attempt_token == attempt_token, CapaDocgAnalysis.status == "running")
            .values(status="failed", error=affected, completed_at=now)
        )
        await db.commit()
        return {"status": "failed"} if res.rowcount > 0 else {"status": "superseded"}
    # CAS promote to done
    res = await db.execute(
        update(CapaDocgAnalysis)
        .where(CapaDocgAnalysis.analysis_id == analysis_id,
               CapaDocgAnalysis.attempt_token == attempt_token, CapaDocgAnalysis.status == "running")
        .values(status="done", is_current=True, affected_docs=affected,
                analysis_input_hash=phase2["_input_hash"], llm_available=True,
                model=phase2.get("model"), completed_at=now)
    )
    if res.rowcount == 0:
        return {"status": "superseded"}
    await db.execute(
        db.add(AuditLog(
            table_name="capa_eightd", record_id=capa.report_id, action="DOC_IMPACT_ANALYZED",
            changed_fields={"affected_doc_count": len(affected), "llm_available": True, "status": "done"},
            operated_by=user_id, factory_id=capa.factory_id, operated_at=now,
        ))
    )
    await db.commit()
    return {"status": "done", "analysis_id": str(analysis_id)}
```

> **Note:** `_build_allowlist`, `_compute_input_hash`, `_build_prompt`, `_validate_and_backfill`, `_RESPONSE_SCHEMA` 是 helper 函数，需在同一文件实现。`_validate_and_backfill` 实现 C7（doc_id 在 allowlist）+ vacuous pass（每项 key_points ≥1 + update_suggestion 非空）+ 判别联合（target_key/add_anchor 互斥）+ target_kind ∈ allowlist。实现时参考 spec 第 110-132 行。

- [ ] **Step 7: 实现 `_validate_and_backfill` + helpers**

在 `capa_doc_gate_service.py` 实现：
```python
_ALLOWED_NODE_TYPES = {"FailureMode", "FailureEffect", "FailureCause", "ProcessStep", "Function", "WorkElement"}


def _validate_and_backfill(phase2: dict, candidates: list[dict]) -> list[dict] | str:
    """Validate LLM output; backfill doc_id/name/baseline from allowlist. Return affected_docs or error str."""
    raw_docs = phase2.get("affected_docs")
    if not isinstance(raw_docs, list):
        return "LLM 输出缺 affected_docs"
    if not raw_docs:
        return "affected_docs 为空"  # empty list handled by confirm_no_affected, not done
    cand_by_id = {str(c["doc_id"]): c for c in candidates}
    out = []
    for d in raw_docs:
        doc_id = str(d.get("doc_id", ""))
        if doc_id not in cand_by_id:
            return f"LLM 输出非法 doc_id: {doc_id}"
        cand = cand_by_id[doc_id]
        kps = d.get("key_points", [])
        if not kps:
            return "doc key_points 为空（vacuous pass）"
        suggestion = d.get("update_suggestion")
        if not suggestion or not str(suggestion).strip():
            return "update_suggestion 为空"
        for kp in kps:
            err = _validate_key_point(kp, cand)
            if err:
                return err
        out.append({
            "doc_type": cand["doc_type"], "doc_id": cand["doc_id"], "doc_name": cand["doc_name"],
            "baseline_version_id": str(cand["baseline_version_id"]) if cand.get("baseline_version_id") else None,
            "baseline_version": cand.get("baseline_version"),
            "key_points": kps, "update_suggestion": suggestion,
        })
    return out


def _validate_key_point(kp: dict, cand: dict) -> str | None:
    action = kp.get("expected_action")
    if action not in ("add", "modify", "delete"):
        return f"非法 expected_action: {action}"
    kind = kp.get("target_kind")
    if kind not in ("fmea_node", "cp_item", "document"):
        return f"非法 target_kind: {kind}"
    if kind != cand["doc_type"] and not (kind == "cp_item" and cand["doc_type"] == "control_plan") and not (kind == "fmea_node" and cand["doc_type"] == "fmea"):
        return f"doc_type/target_kind 错配"
    has_target = "target_key" in kp and kp["target_key"]
    has_anchor = "add_anchor" in kp and kp["add_anchor"]
    if has_target and has_anchor:
        return "target_key 与 add_anchor 互斥"
    if action in ("modify", "delete") and not has_target:
        return f"{action} 须 target_key"
    if action == "add":
        if kind == "document":
            if cand.get("baseline_version") is not None:
                return "document add 仅允许 baseline=NULL"
        else:
            if not has_anchor:
                return "add 须 add_anchor"
            a = kp["add_anchor"]
            if not a.get("parent_node_id") or not a.get("node_type") or not str(a.get("business_key", "")).strip():
                return "add_anchor 三字段须非空"
            if a["node_type"] not in _ALLOWED_NODE_TYPES:
                return f"非法 node_type: {a['node_type']}"
            # parent_node_id must be in add_anchors allowlist
            allowed_parents = {(x["parent_node_id"], x["node_type"]) for x in cand.get("add_anchors", [])}
            if (a["parent_node_id"], a["node_type"]) not in allowed_parents:
                return "add_anchor 不在 allowlist"
    return None
```

> `_build_allowlist` 查 `capa.factory_id`+`product_line_code` 下 CP/FMEA，每 doc 算 baseline（`created_at <= capa.created_at` 最后版本，`ORDER BY created_at DESC, major_no DESC, minor_no DESC, version_id DESC`）+ existing_targets（baseline snapshot node_id / CP items source_fmea_node_id）+ add_anchors（baseline 已存节点作为合法 parent）。`_compute_input_hash` = sha256(json.dumps({factory_id, product_line_code, d4_root_cause, d5_correction, d7_prevention, severity, fmea_ref_id, fmea_node_id, [(doc_type, doc_id, baseline_version_id, baseline_sha256)]}, sort_keys=True))。**不含 latest**。

- [ ] **Step 8: 运行 done 成功 + vacuous 测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_service.py -x --tb=short`
Expected: PASS

- [ ] **Step 9: 提交**

```bash
git add backend/app/services/capa_doc_gate_service.py backend/tests/capa/test_capa_doc_gate_service.py
git commit -m "feat(01.7): generate_impact_analysis 三阶段（BLOCKED/stale recovery/统一 CAS/C9 重算）+ allowlist 校验 + vacuous pass 拒绝"
```

---

## Task 3: 服务层 — `run_audit`（版本 diff + 关键点覆盖 + 空清单守卫）

**Files:**
- Modify: `backend/app/services/capa_doc_gate_service.py`
- Create: `backend/tests/capa/test_capa_doc_gate_coverage.py`

**Interfaces:**
- Consumes: `diff_engine.diff_fmea_graphs`/`diff_cp_items`/`diff_cp_headers`，`version_service.get_latest_cp_version`/`get_latest_fmea_version`
- Produces: `run_audit(db, capa, user_id) -> dict`

- [ ] **Step 1: 写覆盖判定测试（passed/pending/incomplete + 无关 bump 不满足 add）**

`backend/tests/capa/test_capa_doc_gate_coverage.py`:
```python
import pytest
from app.services import capa_doc_gate_service


@pytest.mark.asyncio
async def test_run_audit_passed_when_bump_and_covered(db, capa_with_done_analysis_and_bumped_doc):
    """baseline→version_after diff covers all key_points -> all passed -> decision=passed."""
    capa, user = capa_with_done_analysis_and_bumped_doc
    result = await capa_doc_gate_service.run_audit(db, capa, user.user_id)
    assert result["decision"] == "passed"


@pytest.mark.asyncio
async def test_run_audit_pending_when_no_new_version(db, capa_with_done_analysis_no_bump):
    capa, user = capa_with_done_analysis_no_bump
    result = await capa_doc_gate_service.run_audit(db, capa, user.user_id)
    assert result["decision"] == "blocked"


@pytest.mark.asyncio
async def test_unrelated_bump_does_not_satisfy_add(db, capa_with_add_keypoint_and_unrelated_bump):
    """add key_point requires parent+type+business_key in added_nodes; unrelated desc change -> incomplete."""
    capa, user = capa_with_add_keypoint_and_unrelated_bump
    result = await capa_doc_gate_service.run_audit(db, capa, user.user_id)
    assert result["decision"] == "blocked"
    assert any(a["status"] == "incomplete" for a in result["audits"])


@pytest.mark.asyncio
async def test_empty_affected_docs_run_audit_raises(db, capa_with_empty_done_analysis):
    """Empty affected_docs -> run_audit raises (must use confirm_no_affected)."""
    capa, user = capa_with_empty_done_analysis
    with pytest.raises(ValueError, match="空影响清单须人工确认"):
        await capa_doc_gate_service.run_audit(db, capa, user.user_id)
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_coverage.py -x`
Expected: FAIL（run_audit 未实现）

- [ ] **Step 3: 实现 `run_audit`**

`capa_doc_gate_service.py` 追加：
```python
from app.services.diff_engine import diff_fmea_graphs, diff_cp_items, diff_cp_headers
from app.services.version_service import get_latest_cp_version, get_latest_fmea_version
from app.models.control_plan_version import ControlPlanVersion
from app.models.fmea_version import FMEAVersion


async def run_audit(db: AsyncSession, capa: CAPAEightD, user_id: uuid.UUID) -> dict:
    analysis = await db.scalar(
        select(CapaDocgAnalysis).where(
            CapaDocgAnalysis.capa_id == capa.report_id, CapaDocgAnalysis.is_current == True
        )
    )
    if analysis is None or analysis.status != "done":
        raise ValueError("未生成有效影响分析")
    # C9 precheck
    candidates = await _build_allowlist(db, capa)
    cur_hash = _compute_input_hash(capa, candidates)
    if cur_hash != analysis.analysis_input_hash:
        raise ValueError("分析输入已变更，请重新生成影响分析")
    if not analysis.affected_docs:
        raise ValueError("空影响清单须人工确认，不可运行自动审核")
    audit_run_id = uuid.uuid4()
    now = datetime.now(timezone.utc)
    audits = []
    all_passed = True
    version_snapshot = []
    for doc in analysis.affected_docs:
        audit_row = await _audit_one_doc(db, capa, doc, audit_run_id, user_id, now)
        audits.append(audit_row)
        if audit_row["status"] != "passed":
            all_passed = False
        else:
            version_snapshot.append({
                "doc_type": doc["doc_type"], "doc_id": doc["doc_id"],
                "version_after_id": audit_row["version_after"]["version_id"] if audit_row["version_after"] else None,
                "sha256": audit_row["version_after"]["sha256"] if audit_row["version_after"] else None,
            })
    # Insert audit rows
    for a in audits:
        db.add(CapaDocgAudit(
            analysis_id=analysis.analysis_id, audit_run_id=audit_run_id, factory_id=capa.factory_id,
            doc_type=a["doc_type"], doc_id=a["doc_id"], doc_name=a["doc_name"], status=a["status"],
            version_before=a["version_before"], version_after=a["version_after"], version_bump=a["version_bump"],
            coverage=a["coverage"], covered_count=a["covered_count"], total_count=a["total_count"],
            audited_by=user_id, audited_at=now,
        ))
    # Insert decision (revision locked)
    decision = "passed" if all_passed else "blocked"
    await _insert_decision(db, analysis.analysis_id, capa.factory_id, decision, user_id, now,
                          audit_run_id if all_passed else None, version_snapshot if all_passed else [])
    await db.execute(text("SELECT 1 FROM capa_eightd WHERE report_id=:cid FOR UPDATE"), {"cid": capa.report_id})
    db.add(AuditLog(
        table_name="capa_eightd", record_id=capa.report_id,
        action="DOC_UPDATE_AUDITED" if all_passed else "DOC_GATE_BLOCKED",
        changed_fields={"per_doc_status": [{"doc_type": a["doc_type"], "status": a["status"]} for a in audits], "audit_run_id": str(audit_run_id)},
        operated_by=user_id, factory_id=capa.factory_id, operated_at=now,
    ))
    await db.commit()
    return {"decision": decision, "audits": audits, "audit_run_id": str(audit_run_id)}


async def _insert_decision(db, analysis_id, factory_id, decision, user_id, now, audit_run_id, version_snapshot):
    # Lock analysis row, read max revision
    max_rev = await db.scalar(
        select(CapaDocgDecision.revision).where(CapaDocgDecision.analysis_id == analysis_id)
        .order_by(CapaDocgDecision.revision.desc()).limit(1)
    )
    db.add(CapaDocgDecision(
        analysis_id=analysis_id, audit_run_id=audit_run_id, revision=(max_rev or -1) + 1,
        factory_id=factory_id, decision=decision, version_snapshot=version_snapshot,
        decided_by=user_id, decided_at=now,
    ))
```

> `_audit_one_doc` 实现：查 latest version（叠加 factory_id 过滤）；version_after = latest 且 created_at > capa.created_at；baseline 规范化（NULL → 空结构）；调 diff_fmea_graphs/diff_cp_items 比 baseline vs version_after snapshot；逐 key_point 匹配（modify/delete 命 target_key；add 从 version_after.snapshot.edges 建 parent lookup 后按 parent+type+business_key 命 added_nodes；document 仅 baseline=NULL 时 version_bump 即 covered）。

- [ ] **Step 4: 实现 `_audit_one_doc`**

```python
async def _audit_one_doc(db, capa, doc, audit_run_id, user_id, now):
    doc_type = doc["doc_type"]
    doc_id = uuid.UUID(str(doc["doc_id"]))
    baseline = doc.get("baseline_version")
    # Latest version with factory filter
    if doc_type == "control_plan":
        latest = await get_latest_cp_version(db, doc_id)
    else:
        latest = await get_latest_fmea_version(db, doc_id)
    version_after = None
    version_bump = False
    if latest and latest.created_at > capa.created_at:
        version_after = {"version_id": str(latest.version_id), "major": latest.major_no, "minor": latest.minor_no, "sha256": latest.sha256_hash, "updated_at": latest.created_at.isoformat()}
        if baseline is None or baseline.get("sha256") != latest.sha256_hash:
            version_bump = True
    version_before = None
    if baseline:
        version_before = {"version_id": doc.get("baseline_version_id"), "major": baseline["major"], "minor": baseline["minor"], "sha256": baseline["sha256"]}
    # Diff
    coverage = []
    covered = 0
    total = len(doc["key_points"])
    if version_bump and version_after:
        diff = _compute_diff(doc_type, baseline, latest)
        for kp in doc["key_points"]:
            hit = _match_key_point(kp, diff, latest, doc_type)
            coverage.append({"key_point": kp, "covered": hit, "evidence": ""})
            if hit:
                covered += 1
    status = "passed" if (version_bump and covered == total) else ("incomplete" if version_bump else "pending_update")
    return {"doc_type": doc_type, "doc_id": doc_id, "doc_name": doc["doc_name"], "status": status,
            "version_before": version_before, "version_after": version_after, "version_bump": version_bump,
            "coverage": coverage, "covered_count": covered, "total_count": total}


def _compute_diff(doc_type, baseline, latest):
    if doc_type == "control_plan":
        v1_items = (baseline or {}).get("items", []) if baseline else []
        v2_items = latest.items_snapshot.get("items", []) if hasattr(latest, "items_snapshot") else []
        v1_header = (baseline or {}).get("header", {}) if baseline else {}
        v2_header = latest.header_snapshot or {}
        return {"items": diff_cp_items(v1_items, v2_items), "headers": diff_cp_headers(v1_header, v2_header)}
    else:
        v1 = (baseline or {}).get("snapshot", {"nodes": [], "edges": []}) if baseline else {"nodes": [], "edges": []}
        v2 = latest.snapshot or {"nodes": [], "edges": []}
        return diff_fmea_graphs(v1, v2)
```

> **Note:** ControlPlanVersion 的 snapshot 存在 `header_snapshot`+`items_snapshot`；FMEAVersion 存 `snapshot`。baseline 在 allowlist 构建时存为 dict。`_match_key_point` 实现：modify/delete 按 target_key 命 modified/deleted；add 从 `latest.snapshot.edges` 建 parent_map（target→sources），在 added_nodes 里按 (parent_node_id, node_type, normalized business_key) 命中；business_key 规范化 = `str(kp["add_anchor"]["business_key"]).strip().lower()`。

- [ ] **Step 5: 实现 `_match_key_point`**

```python
def _match_key_point(kp, diff, latest, doc_type):
    action = kp["expected_action"]
    kind = kp["target_kind"]
    if kind == "document":
        return True  # baseline=NULL+add, version_bump already True
    if doc_type == "control_plan":
        items_diff = diff["items"]
        if action == "modify":
            return any(i["item_id"] == kp["target_key"] for i in items_diff["modified_items"])
        if action == "delete":
            return any(i.get("item_id") == kp["target_key"] for i in items_diff["deleted_items"])
        if action == "add":
            a = kp["add_anchor"]
            bk = str(a["business_key"]).strip().lower()
            return any(
                _parent_of(latest, i.get("source_fmea_node_id"), a["parent_node_id"])
                and i.get("node_type") == a["node_type"]  # CP items 无 node_type; 见下注
                and str(i.get("product_characteristic", "")).strip().lower() == bk
                for i in items_diff["added_items"]
            )
    else:  # fmea
        g_diff = diff
        if action == "modify":
            return any(n["node_id"] == kp["target_key"] for n in g_diff["modified_nodes"])
        if action == "delete":
            return any(n["id"] == kp["target_key"] for n in g_diff["deleted_nodes"])
        if action == "add":
            a = kp["add_anchor"]
            bk = str(a["business_key"]).strip().lower()
            parent_map = _build_parent_map(latest.snapshot)
            return any(
                a["parent_node_id"] in parent_map.get(n["id"], [])
                and n.get("type") == a["node_type"]
                and str(n.get("name", "")).strip().lower() == bk
                for n in g_diff["added_nodes"]
            )
    return False
```

> **CP item add 匹配**：CP item 无 node_type 字段，add 锚点对 CP 用 `source_fmea_node_id` 作 parent + business_key 匹配 `product_characteristic`。实现时按实际 ControlPlanItem 字段调整（参考 `frontend/src/types/index.ts:235` ControlPlanItem）。

- [ ] **Step 6: 运行覆盖测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_coverage.py -x --tb=short`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add backend/app/services/capa_doc_gate_service.py backend/tests/capa/test_capa_doc_gate_coverage.py
git commit -m "feat(01.7): run_audit 版本 diff + 关键点覆盖（add 锚点 parent lookup）+ 空清单守卫 + decision revision 锁"
```

---

## Task 4: 服务层 — `record_defer` + `confirm_no_affected` + `get_latest_analysis`

**Files:**
- Modify: `backend/app/services/capa_doc_gate_service.py`
- Test: `backend/tests/capa/test_capa_doc_gate_service.py`（追加）

**Interfaces:**
- Produces: `record_defer(db, capa, reason, owner, deadline, user_id) -> dict`, `confirm_no_affected(db, capa, user_id) -> dict`, `get_latest_analysis(db, capa) -> dict | None`

- [ ] **Step 1: 写 defer + confirm 测试**

追加到 `test_capa_doc_gate_service.py`:
```python
@pytest.mark.asyncio
async def test_record_defer_inserts_deferred_decision_and_still_blocks(db, capa_with_blocked_decision):
    capa, user = capa_with_blocked_decision
    result = await capa_doc_gate_service.record_defer(
        db, capa, "等待 SOP 更新", user.user_id, "2026-08-01", user.user_id
    )
    assert result["decision"] == "deferred"


@pytest.mark.asyncio
async def test_record_defer_owner_not_in_factory_raises(db, capa_with_blocked_decision, user_other_factory):
    capa, _ = capa_with_blocked_decision
    with pytest.raises(ValueError, match="owner"):
        await capa_doc_gate_service.record_defer(
            db, capa, "r", user_other_factory.user_id, "2026-08-01", user_other_factory.user_id
        )


@pytest.mark.asyncio
async def test_confirm_no_affected_passes_empty_list(db, capa_with_empty_done_analysis):
    capa, user = capa_with_empty_done_analysis
    result = await capa_doc_gate_service.confirm_no_affected(db, capa, user.user_id)
    assert result["decision"] == "passed"
    assert result["no_affected_confirmed"] is True


@pytest.mark.asyncio
async def test_get_latest_analysis_returns_failed_too(db, capa_with_failed_analysis):
    capa, _ = capa_with_failed_analysis
    result = await capa_doc_gate_service.get_latest_analysis(db, capa)
    assert result["status"] == "failed"
```

- [ ] **Step 2: 实现三个函数**

`capa_doc_gate_service.py` 追加：
```python
async def record_defer(db: AsyncSession, capa: CAPAEightD, reason: str, owner_id: uuid.UUID, deadline, user_id: uuid.UUID) -> dict:
    if not reason or not reason.strip():
        raise ValueError("defer reason 必填")
    # Owner validation: must belong to capa.factory_id and be active (P1#6)
    from app.models.user import User
    owner = await db.get(User, owner_id)
    if owner is None or not owner.is_active:
        raise ValueError("defer_owner 不存在或未激活")
    if owner.factory_id is not None and owner.factory_id != capa.factory_id:
        raise ValueError("defer_owner 不属于当前工厂")
    analysis = await db.scalar(
        select(CapaDocgAnalysis).where(CapaDocgAnalysis.capa_id == capa.report_id, CapaDocgAnalysis.is_current == True)
    )
    if analysis is None:
        raise ValueError("未生成影响分析")
    # C9 precheck
    candidates = await _build_allowlist(db, capa)
    if _compute_input_hash(capa, candidates) != analysis.analysis_input_hash:
        raise ValueError("分析输入已变更，请重新生成影响分析")
    from datetime import date as date_cls
    dl = deadline if isinstance(deadline, date_cls) else date_cls.fromisoformat(deadline)
    await db.execute(text("SELECT 1 FROM capa_eightd WHERE report_id=:cid FOR UPDATE"), {"cid": capa.report_id})
    await _insert_decision(db, analysis.analysis_id, capa.factory_id, "deferred", user_id,
                          datetime.now(timezone.utc), None, [])
    # Update defer fields on the just-inserted row
    latest = await db.scalar(
        select(CapaDocgDecision).where(CapaDocgDecision.analysis_id == analysis.analysis_id)
        .order_by(CapaDocgDecision.revision.desc()).limit(1)
    )
    latest.defer_reason = reason
    latest.defer_owner = owner_id
    latest.defer_deadline = dl
    db.add(AuditLog(table_name="capa_eightd", record_id=capa.report_id, action="DOC_GATE_DEFERRED",
                    changed_fields={"reason": reason, "owner": str(owner_id), "deadline": str(dl)},
                    operated_by=user_id, factory_id=capa.factory_id, operated_at=datetime.now(timezone.utc)))
    await db.commit()
    return {"decision": "deferred"}


async def confirm_no_affected(db: AsyncSession, capa: CAPAEightD, user_id: uuid.UUID) -> dict:
    analysis = await db.scalar(
        select(CapaDocgAnalysis).where(CapaDocgAnalysis.capa_id == capa.report_id, CapaDocgAnalysis.is_current == True)
    )
    if analysis is None or analysis.status != "done":
        raise ValueError("未生成有效影响分析")
    candidates = await _build_allowlist(db, capa)
    if _compute_input_hash(capa, candidates) != analysis.analysis_input_hash:
        raise ValueError("分析输入已变更，请重新生成影响分析")
    if analysis.affected_docs:
        raise ValueError("仅空清单可确认")
    await db.execute(text("SELECT 1 FROM capa_eightd WHERE report_id=:cid FOR UPDATE"), {"cid": capa.report_id})
    now = datetime.now(timezone.utc)
    await _insert_decision(db, analysis.analysis_id, capa.factory_id, "passed", user_id, now, None, [])
    latest = await db.scalar(
        select(CapaDocgDecision).where(CapaDocgDecision.analysis_id == analysis.analysis_id)
        .order_by(CapaDocgDecision.revision.desc()).limit(1)
    )
    latest.no_affected_confirmed = True
    db.add(AuditLog(table_name="capa_eightd", record_id=capa.report_id, action="DOC_GATE_PASSED",
                    changed_fields={"reason": "空清单人工确认"}, operated_by=user_id, factory_id=capa.factory_id, operated_at=now))
    await db.commit()
    return {"decision": "passed", "no_affected_confirmed": True}


async def get_latest_analysis(db: AsyncSession, capa: CAPAEightD) -> dict | None:
    row = await db.scalar(
        select(CapaDocgAnalysis).where(CapaDocgAnalysis.capa_id == capa.report_id)
        .order_by(CapaDocgAnalysis.created_at.desc(), CapaDocgAnalysis.analysis_id.desc()).limit(1)
    )
    if row is None:
        return None
    return {"analysis_id": str(row.analysis_id), "status": row.status, "affected_docs": row.affected_docs,
            "error": row.error, "is_current": row.is_current}
```

- [ ] **Step 3: 运行测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_service.py -x --tb=short`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add backend/app/services/capa_doc_gate_service.py backend/tests/capa/test_capa_doc_gate_service.py
git commit -m "feat(01.7): record_defer（owner 校验+仍阻断）+ confirm_no_affected（C9 预检）+ get_latest_analysis 确定性排序"
```

---

## Task 5: advance_capa gate 接入 + 7 API 路由

**Files:**
- Modify: `backend/app/services/capa_service.py`（`advance_capa` edge dispatch）
- Modify: `backend/app/api/capa.py`（7 路由）
- Create: `backend/app/schemas/capa_doc_gate.py`
- Test: `backend/tests/capa/test_capa_doc_gate_api.py`

**Interfaces:**
- Consumes: `capa_doc_gate_service.*` 全部函数
- Produces: `_d8_doc_gate_gate(db, capa)`（advance_capa 内调用）

- [ ] **Step 1: 写 gate 阻断测试**

`backend/tests/capa/test_capa_doc_gate_api.py`:
```python
import pytest
from app.services import capa_service
from app.state_machines.eightd_state import EightDState


@pytest.mark.asyncio
async def test_d8_gate_blocks_when_no_analysis(client, capa_at_d8_gate):
    """advance D8_GATE_PENDING->D8_APPROVAL_PENDING with no analysis -> 400."""
    capa, auth = capa_at_d8_gate
    resp = await client.post(f"/api/capa/{capa.report_id}/advance",
                              json={"target_state": "D8_APPROVAL_PENDING"}, headers=auth)
    assert resp.status_code == 400
    assert "影响分析" in resp.json()["detail"]


@pytest.mark.asyncio
async def test_d8_gate_blocks_when_decision_blocked(client, capa_with_blocked_decision):
    capa, auth = capa_with_blocked_decision
    resp = await client.post(f"/api/capa/{capa.report_id}/advance",
                              json={"target_state": "D8_APPROVAL_PENDING"}, headers=auth)
    assert resp.status_code == 400


@pytest.mark.asyncio
async def test_d8_gate_passes_when_decision_passed(client, capa_with_passed_decision):
    capa, auth = capa_with_passed_decision
    resp = await client.post(f"/api/capa/{capa.report_id}/advance",
                              json={"target_state": "D8_APPROVAL_PENDING"}, headers=auth)
    assert resp.status_code == 200
    assert resp.json()["capa"]["status"] == "D8_APPROVAL_PENDING"
```

- [ ] **Step 2: 运行确认失败**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_api.py -x`
Expected: FAIL（gate 未接入）

- [ ] **Step 3: 实现 `_d8_doc_gate_gate` + 接入 advance_capa**

`backend/app/services/capa_service.py`，在 `advance_capa` 的 per-edge 分发处（现有 `D8_GATE_PENDING` 占位 `pass`）替换为：
```python
elif current == EightDState.D8_GATE_PENDING and target == EightDState.D8_APPROVAL_PENDING:
    await _d8_doc_gate_gate(db, capa)
```
并在文件内新增：
```python
async def _d8_doc_gate_gate(db, capa) -> None:
    from app.services.capa_doc_gate_service import get_latest_decision, _build_allowlist, _compute_input_hash
    from app.models.capa_doc_gate import CapaDocgAnalysis, CapaDocgDecision
    analysis = await db.scalar(
        select(CapaDocgAnalysis).where(
            CapaDocgAnalysis.capa_id == capa.report_id, CapaDocgAnalysis.is_current == True
        )
    )
    if analysis is None:
        raise ValueError("请先生成文档影响分析")
    candidates = await _build_allowlist(db, capa)
    if _compute_input_hash(capa, candidates) != analysis.analysis_input_hash:
        raise ValueError("分析输入已变更，请重新生成影响分析")
    decision = await db.scalar(
        select(CapaDocgDecision).where(CapaDocgDecision.analysis_id == analysis.analysis_id)
        .order_by(CapaDocgDecision.revision.desc(), CapaDocgDecision.decided_at.desc()).limit(1)
    )
    if decision is None:
        raise ValueError("请先运行文档审核")
    if decision.decision != "passed":
        raise ValueError(f"文档门禁未通过：{decision.decision}")
    # C8 version freshness
    for snap in decision.version_snapshot:
        latest = await _latest_version(db, snap["doc_type"], uuid.UUID(snap["doc_id"]))
        if latest is None or str(latest.version_id) != snap.get("version_after_id") or latest.sha256_hash != snap.get("sha256"):
            raise ValueError("文档已变更，请重新审核")
```

> `_latest_version` helper 调 `get_latest_cp_version`/`get_latest_fmea_version` 按 doc_type 路由。

- [ ] **Step 4: 实现 7 路由 + schema**

`backend/app/schemas/capa_doc_gate.py`:
```python
from pydantic import BaseModel


class DeferRequest(BaseModel):
    reason: str
    owner_id: str
    deadline: str  # ISO date


class DocGateDecisionResponse(BaseModel):
    decision: str | None
    no_affected_confirmed: bool = False
    version_snapshot: list = []
```

`backend/app/api/capa.py` 在 D3 路由块后追加 7 路由（POST/GET /doc-gate/impact, POST/GET /doc-gate/audit, POST /doc-gate/defer, POST /doc-gate/confirm-no-affected, GET /doc-gate/decision）。每路由用 `get_request_scope` + `check_factory_access` + `get_user_permission >= EDIT/READ`。POST /impact 捕获 `ValueError("BLOCKED")` → 422 `{"detail": {"blocked": True}}`。

```python
def _assert_d8_gate_stage(capa: CAPAEightD):
    if capa.status != EightDState.D8_GATE_PENDING.value:
        raise HTTPException(status_code=400, detail="仅 D8_GATE_PENDING 阶段可操作")


@router.post("/{report_id}/doc-gate/impact")
async def doc_gate_impact_ep(report_id: uuid.UUID, db=Depends(get_db), scope=Depends(get_request_scope)):
    if await get_user_permission(scope.user, Module.CAPA, db) < PermissionLevel.EDIT:
        raise HTTPException(403, "需要 EDIT 权限")
    capa = await capa_service.get_capa(db, report_id)
    if capa is None: raise HTTPException(404)
    _d3_check_scope(capa, scope)
    _assert_d8_gate_stage(capa)
    try:
        result = await capa_doc_gate_service.generate_impact_analysis(db, capa, scope.user.user_id)
    except ValueError as e:
        if "BLOCKED" in str(e):
            raise HTTPException(422, detail={"blocked": True, "message": str(e)})
        raise HTTPException(400, detail=str(e))
    return result
```

> 其余 6 路由同模式（GET /impact 调 get_latest_analysis；POST /audit 调 run_audit；GET /audit 查最新 audit_run_id 的行；POST /defer 调 record_defer；POST /confirm-no-affected 调 confirm_no_affected；GET /decision 查最新 decision）。

- [ ] **Step 5: 运行 API 测试**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_api.py -x --tb=short`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/app/services/capa_service.py backend/app/api/capa.py backend/app/schemas/capa_doc_gate.py backend/tests/capa/test_capa_doc_gate_api.py
git commit -m "feat(01.7): advance_capa D8_GATE edge 接入 _d8_doc_gate_gate（C8+C9 新鲜度）+ 7 doc-gate 路由 + BLOCKED 422"
```

---

## Task 6: 新鲜度 + 并发测试

**Files:**
- Create: `backend/tests/capa/test_capa_doc_gate_freshness.py`
- Create: `backend/tests/capa/test_capa_doc_gate_concurrency.py`

- [ ] **Step 1: 写 C8/C9 新鲜度测试**

`test_capa_doc_gate_freshness.py`:
```python
@pytest.mark.asyncio
async def test_c8_blocks_when_doc_changed_after_passed(db, capa_with_passed_decision_then_doc_changed):
    capa, user = capa_with_passed_decision_then_doc_changed
    with pytest.raises(ValueError, match="文档已变更"):
        await capa_service._d8_doc_gate_gate(db, capa)


@pytest.mark.asyncio
async def test_c9_blocks_when_capa_root_cause_changed(db, capa_with_done_analysis_then_rootcause_changed):
    capa, user = capa_with_done_analysis_then_rootcause_changed
    with pytest.raises(ValueError, match="分析输入已变更"):
        await capa_service._d8_doc_gate_gate(db, capa)


@pytest.mark.asyncio
async def test_normal_update_flow_not_blocked_by_c9(db, capa_normal_update_flow):
    """分析→更新文档→审核→passed: C9 hash unchanged (no latest in hash) -> gate passes."""
    capa, user = capa_normal_update_flow
    # Should NOT raise
    await capa_service._d8_doc_gate_gate(db, capa)
```

- [ ] **Step 2: 写并发测试**

`test_capa_doc_gate_concurrency.py`:
```python
@pytest.mark.asyncio
async def test_concurrent_generate_one_running(db, capa_d8_gate, llm_mock):
    """Two concurrent generate -> only one running row (partial-UQ)."""
    import asyncio
    capa, user = capa_d8_gate
    r1, r2 = await asyncio.gather(
        capa_doc_gate_service.generate_impact_analysis(db, capa, user.user_id),
        capa_doc_gate_service.generate_impact_analysis(db, capa, user.user_id),
        return_exceptions=True,
    )
    # At least one should be 'running' retry_after or 'done'; not two running rows
    running = await db.scalar(select(func.count()).select_from(CapaDocgAnalysis)
                              .where(CapaDocgAnalysis.capa_id == capa.report_id, CapaDocgAnalysis.status == "running"))
    assert running <= 1


@pytest.mark.asyncio
async def test_concurrent_audit_no_duplicate_revision(db, capa_with_done_analysis, llm_mock):
    capa, user = capa_with_done_analysis
    import asyncio
    await asyncio.gather(
        capa_doc_gate_service.run_audit(db, capa, user.user_id),
        capa_doc_gate_service.run_audit(db, capa, user.user_id),
        return_exceptions=True,
    )
    max_rev = await db.scalar(select(func.max(CapaDocgDecision.revision))
                              .where(CapaDocgDecision.analysis_id == analysis_id))
    # UQ(analysis_id, revision) ensures no duplicate
```

- [ ] **Step 3: 运行**

Run: `cd backend && SECRET_KEY=test-secret-key pytest tests/capa/test_capa_doc_gate_freshness.py tests/capa/test_capa_doc_gate_concurrency.py -x --tb=short`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add backend/tests/capa/test_capa_doc_gate_freshness.py backend/tests/capa/test_capa_doc_gate_concurrency.py
git commit -m "test(01.7): C8/C9 新鲜度 + 并发（running partial-UQ + decision revision UQ）"
```

---

## Task 7: 前端 DocGatePanel + 全局按钮排除 + i18n

**Files:**
- Create: `frontend/src/components/capa/DocGatePanel.tsx`
- Modify: `frontend/src/pages/capa/CAPADetailPage.tsx:492`
- Modify: `frontend/src/api/capa.ts`
- Modify: `frontend/src/locales/{zh-CN,en-US}/capa.json`
- Test: `frontend/src/components/capa/__tests__/DocGatePanel.test.tsx`

- [ ] **Step 1: 写 vitest 状态机测试**

`DocGatePanel.test.tsx`:
```tsx
import { render, screen } from "@testing-library/react";
import { DocGatePanel } from "../DocGatePanel";

it("renders blocked banner when no LLM", () => {
  render(<DocGatePanel capaId="x" analysis={{ status: "failed", error: "LLM 未配置" }} decision={null} canEdit canApprove={false} />);
  expect(screen.getByTestId("doc-gate-blocked-banner")).toBeInTheDocument();
});

it("hides advance button when decision not passed", () => {
  render(<DocGatePanel capaId="x" analysis={{ status: "done" }} decision={{ decision: "blocked" }} canEdit canApprove={false} />);
  expect(screen.queryByTestId("doc-gate-advance")).not.toBeInTheDocument();
});

it("shows advance button when decision passed", () => {
  render(<DocGatePanel capaId="x" analysis={{ status: "done" }} decision={{ decision: "passed" }} canEdit canApprove={false} />);
  expect(screen.getByTestId("doc-gate-advance")).toBeInTheDocument();
});
```

- [ ] **Step 2: 实现 `DocGatePanel.tsx`**

按 spec 前端 §6 个分区实现：影响分析区（生成/重新生成按钮 + BLOCKED banner + failed 重试）+ 受影响文档清单 + 审核区（运行审核按钮 + 结果表）+ 门禁决策区（decision 显示 + decision==='passed' 显推进按钮）+ 空清单确认 + 延期对话框。镜像 D3 面板结构（参考 `frontend/src/components/capa/` 下 D3 面板文件）。

- [ ] **Step 3: 改全局推进按钮排除 D8_GATE_PENDING**

`CAPADetailPage.tsx:492`：
```tsx
{!["D8_GATE_PENDING", "D8_APPROVAL_PENDING", "D8_CLOSURE"].includes(capa.status) && (
  <Button ... onClick={handleAdvance} data-e2e="capa-advance">...</Button>
)}
```
并在 D8 主步骤区域渲染 `<DocGatePanel capaId={id} ... />`（仅 `capa.status === "D8_GATE_PENDING"`）。

- [ ] **Step 4: 实现 API client + i18n**

`frontend/src/api/capa.ts` 加：`docGateImpact(id)`, `getDocGateImpact(id)`, `runDocGateAudit(id)`, `getDocGateAudit(id)`, `recordDocGateDefer(id, payload)`, `confirmNoAffected(id)`, `getDocGateDecision(id)`。

`capa.json` 加 `docGate.*` 键（影响分析/审核/门禁/延期/空清单确认/推进到审批）。

- [ ] **Step 5: 运行前端测试 + build**

Run: `cd frontend && npm run build`
Expected: tsc + vite build + vitest PASS

- [ ] **Step 6: 提交**

```bash
git add frontend/src/components/capa/DocGatePanel.tsx frontend/src/components/capa/__tests__/DocGatePanel.test.tsx frontend/src/pages/capa/CAPADetailPage.tsx frontend/src/api/capa.ts frontend/src/locales/
git commit -m "feat(01.7): DocGatePanel 前端（6 分区+BLOCKED banner+按钮门控）+ 全局推进按钮排除 D8_GATE_PENDING + 7 API client + i18n"
```

---

## Task 8: E2E seed + spec

**Files:**
- Modify: `backend/app/seed_e2e.py` + `seed_e2e_constants.py`
- Create: `frontend/e2e/specs/m1-core/capa-story-doc-gate.spec.ts`

- [ ] **Step 1: 扩 seed — 8D 推到 D8_GATE_PENDING + CP/FMEA 版本**

`seed_e2e.py` 加一个 8D（`8D-E2E-DOCGATE-001`）推到 `D8_GATE_PENDING`，产品线有 CP + FMEA 含版本。

- [ ] **Step 2: 写 E2E spec**

`capa-story-doc-gate.spec.ts`：
- 无 LLM 凭证时 AI 路径 skip 整测
- 「门禁阻断（未审核不可推进）」非 LLM 路径：无 current analysis → advance 返 400
- 有凭证时：生成影响分析 → 审核 → 门禁放行 → D8_APPROVAL_PENDING

- [ ] **Step 3: 运行 E2E**

Run: `make e2e TEST_ARGS="--grep doc-gate"`
Expected: 非 LLM 路径 PASS；AI 路径无凭证 skip

- [ ] **Step 4: 提交**

```bash
git add backend/app/seed_e2e.py backend/app/seed_e2e_constants.py frontend/e2e/specs/m1-core/capa-story-doc-gate.spec.ts
git commit -m "test(01.7): E2E seed（D8_GATE_PENDING 8D + CP/FMEA 版本）+ capa-story-doc-gate spec"
```

---

## Task 9: 文档同步 + PROGRESS

**Files:**
- Modify: `docs/user-stories/US-E2E-01-capa-8d-closed-loop/US-E2E-01.7-doc-update-gate.md`（实现注记附录，不改版本号）
- Modify: `docs/superpowers/specs/2026-07-08-us-e2e-01-gap-analysis.md`（01.7 ⬜→✅）
- Modify: `PROGRESS.md`

- [ ] **Step 1: 跑 make check**

Run: `make check`
Expected: 全绿（backend pytest + frontend tsc/build）

- [ ] **Step 2: 更新文档 + PROGRESS**

- [ ] **Step 3: 提交**

```bash
git add docs/ PROGRESS.md
git commit -m "docs(01.7): 实现注记 + gap analysis ✅ + PROGRESS 更新"
```

---

## Self-Review

**Spec coverage:**
- C1 窄范围 → Task 1 CHECK enum + Task 2 validate → ✅
- C2 显式 POST → Task 5 路由 → ✅
- C3 版本间 diff → Task 3 `_compute_diff` → ✅
- C4 空清单人工确认 → Task 4 `confirm_no_affected` → ✅
- C5 defer 仍阻断 → Task 4 `record_defer` + Task 5 gate → ✅
- C6 3 表 → Task 1 → ✅
- C7 allowlist → Task 2 `_build_allowlist` + `_validate_and_backfill` → ✅
- C8 版本新鲜度 → Task 5 `_d8_doc_gate_gate` → ✅
- C9 输入新鲜度（不含 latest）→ Task 2 `_compute_input_hash` + Task 5 gate → ✅
- 三阶段事务 → Task 2 phase1/2/3 → ✅
- 统一 CAS + superseded → Task 2 phase3 → ✅
- stale recovery → Task 2 phase1 → ✅
- 防 vacuous pass（空 key_points + 空清单）→ Task 2 validate + Task 3 守卫 → ✅
- key_point 判别联合 → Task 2 `_validate_key_point` → ✅
- decision revision UQ → Task 1 migration + Task 3 `_insert_decision` → ✅

**Placeholder scan:** Task 2/3 helper 函数（`_build_allowlist`/`_compute_input_hash`/`_build_prompt`/`_audit_one_doc`/`_match_key_point`）给了签名 + 实现要点但非完整代码——这些是复杂函数，完整实现需参考 spec 行号锚点。实现者须读 spec 对应段落。其余步骤均有完整代码。

**Type consistency:** `generate_impact_analysis` 返回 dict（status/analysis_id）；`run_audit` 返回 dict（decision/audits）；`_d8_doc_gate_gate` 无返回（raise）——一致。
